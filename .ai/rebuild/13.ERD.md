# ERD — Clean Enrollment Rebuild

## Propósito y fuente oficial
Representa exactamente `12.DATABASE_DESIGN.md`. Solo se omiten campos de auditoría por legibilidad.

```mermaid
erDiagram
    status_types {
        BIGINT UNSIGNED id PK
        VARCHAR(50) code
        VARCHAR(150) name
        VARCHAR(255) description
        SMALLINT UNSIGNED display_order
        TINYINT(1) is_active
    }
    statuses {
        BIGINT UNSIGNED id PK
        BIGINT UNSIGNED status_type_id FK
        VARCHAR(50) code
        VARCHAR(150) name
        VARCHAR(255) description
        SMALLINT UNSIGNED display_order
        TINYINT(1) is_default
        TINYINT(1) is_active
    }
    document_types {
        BIGINT UNSIGNED id PK
        VARCHAR(50) code
        VARCHAR(150) name
        VARCHAR(255) description
        SMALLINT UNSIGNED display_order
        TINYINT(1) is_active
    }
    sexes {
        BIGINT UNSIGNED id PK
        VARCHAR(50) code
        VARCHAR(150) name
        VARCHAR(255) description
        SMALLINT UNSIGNED display_order
        TINYINT(1) is_active
    }
    marital_statuses {
        BIGINT UNSIGNED id PK
        VARCHAR(50) code
        VARCHAR(150) name
        VARCHAR(255) description
        SMALLINT UNSIGNED display_order
        TINYINT(1) is_active
    }
    education_levels {
        BIGINT UNSIGNED id PK
        VARCHAR(50) code
        VARCHAR(150) name
        VARCHAR(255) description
        SMALLINT UNSIGNED display_order
        TINYINT(1) is_active
    }
    relationship_types {
        BIGINT UNSIGNED id PK
        VARCHAR(50) code
        VARCHAR(150) name
        VARCHAR(255) description
        SMALLINT UNSIGNED display_order
        TINYINT(1) is_active
    }
    provinces {
        BIGINT UNSIGNED id PK
        VARCHAR(50) code
        VARCHAR(150) name
        TINYINT(1) is_active
    }
    cantons {
        BIGINT UNSIGNED id PK
        BIGINT UNSIGNED province_id FK
        VARCHAR(50) code
        VARCHAR(150) name
        TINYINT(1) is_active
    }
    parishes {
        BIGINT UNSIGNED id PK
        BIGINT UNSIGNED canton_id FK
        VARCHAR(50) code
        VARCHAR(150) name
        TINYINT(1) is_active
    }
    grades {
        BIGINT UNSIGNED id PK
        VARCHAR(50) code
        VARCHAR(150) name
        SMALLINT UNSIGNED display_order
        TINYINT(1) is_active
    }
    sections {
        BIGINT UNSIGNED id PK
        VARCHAR(50) code
        VARCHAR(150) name
        SMALLINT UNSIGNED display_order
        TINYINT(1) is_active
    }
    academic_periods {
        BIGINT UNSIGNED id PK
        VARCHAR(50) code
        VARCHAR(150) name
        DATE starts_on
        DATE ends_on
        BIGINT UNSIGNED status_id FK
    }
    persons {
        BIGINT UNSIGNED id PK
        BIGINT UNSIGNED document_type_id FK
        VARCHAR(50) document_number
        VARCHAR(100) first_name
        VARCHAR(100) middle_name
        VARCHAR(100) first_surname
        VARCHAR(100) second_surname
        DATE birth_date
        BIGINT UNSIGNED sex_id FK
        BIGINT UNSIGNED marital_status_id FK
        BIGINT UNSIGNED education_level_id FK
        VARCHAR(254) email
        VARCHAR(30) mobile_phone
        VARCHAR(30) landline_phone
        BIGINT UNSIGNED status_id FK
        VARCHAR(120) active_identification_key
    }
    users {
        BIGINT UNSIGNED id PK
        BIGINT UNSIGNED person_id FK
        VARCHAR(100) username
        VARCHAR(255) password_hash
        BIGINT UNSIGNED status_id FK
        TIMESTAMP last_access_at
        TIMESTAMP locked_at
        BIGINT UNSIGNED active_person_key
        VARCHAR(100) active_username_key
    }
    representatives {
        BIGINT UNSIGNED id PK
        BIGINT UNSIGNED person_id FK
        VARCHAR(150) occupation
        VARCHAR(150) company
        VARCHAR(150) position
        VARCHAR(30) work_phone
        VARCHAR(254) work_email
        BIGINT UNSIGNED status_id FK
        BIGINT UNSIGNED active_person_key
    }
    students {
        BIGINT UNSIGNED id PK
        BIGINT UNSIGNED person_id FK
        VARCHAR(30) institutional_code
        DATE admission_date
        BIGINT UNSIGNED status_id FK
        BIGINT UNSIGNED active_person_key
        VARCHAR(30) active_institutional_code_key
    }
    families {
        BIGINT UNSIGNED id PK
        VARCHAR(150) display_name
        BIGINT UNSIGNED status_id FK
    }
    family_representatives {
        BIGINT UNSIGNED id PK
        BIGINT UNSIGNED family_id FK
        BIGINT UNSIGNED representative_id FK
        BIGINT UNSIGNED relationship_type_id FK
        TINYINT(1) is_primary
        TIMESTAMP started_at
        TIMESTAMP ended_at
        BIGINT UNSIGNED status_id FK
        VARCHAR(42) active_family_representative_key
        BIGINT UNSIGNED active_primary_family_key
    }
    family_students {
        BIGINT UNSIGNED id PK
        BIGINT UNSIGNED family_id FK
        BIGINT UNSIGNED student_id FK
        TIMESTAMP started_at
        TIMESTAMP ended_at
        BIGINT UNSIGNED status_id FK
        BIGINT UNSIGNED active_student_id FK
    }
    family_addresses {
        BIGINT UNSIGNED id PK
        BIGINT UNSIGNED family_id FK
        VARCHAR(100) label
        BIGINT UNSIGNED province_id FK
        BIGINT UNSIGNED canton_id FK
        BIGINT UNSIGNED parish_id FK
        VARCHAR(150) main_street
        VARCHAR(30) street_number
        VARCHAR(150) secondary_street
        VARCHAR(150) sector
        VARCHAR(255) reference
        DECIMAL(10,7) latitude
        DECIMAL(10,7) longitude
        BIGINT UNSIGNED status_id FK
        VARCHAR(260) active_family_label_key
    }
    representative_address_assignments {
        BIGINT UNSIGNED id PK
        BIGINT UNSIGNED family_address_id FK
        BIGINT UNSIGNED family_id FK
        BIGINT UNSIGNED representative_id FK
        TIMESTAMP started_at
        TIMESTAMP ended_at
        VARCHAR(42) active_family_representative_key
    }
    student_address_assignments {
        BIGINT UNSIGNED id PK
        BIGINT UNSIGNED family_address_id FK
        BIGINT UNSIGNED family_id FK
        BIGINT UNSIGNED student_id FK
        TIMESTAMP started_at
        TIMESTAMP ended_at
        VARCHAR(42) active_family_student_key
    }
    family_emergency_contacts {
        BIGINT UNSIGNED id PK
        BIGINT UNSIGNED family_id FK
        VARCHAR(200) names
        BIGINT UNSIGNED relationship_type_id FK
        VARCHAR(30) mobile_phone
        VARCHAR(30) phone
        VARCHAR(254) email
        VARCHAR(500) observations
        BIGINT UNSIGNED status_id FK
    }
    emergency_contact_assignments {
        BIGINT UNSIGNED id PK
        BIGINT UNSIGNED family_emergency_contact_id FK
        BIGINT UNSIGNED student_id FK
        TINYINT UNSIGNED priority
        BIGINT UNSIGNED status_id FK
        VARCHAR(42) active_contact_student_key
    }
    family_authorized_pickups {
        BIGINT UNSIGNED id PK
        BIGINT UNSIGNED family_id FK
        VARCHAR(200) names
        BIGINT UNSIGNED relationship_type_id FK
        VARCHAR(30) mobile_phone
        VARCHAR(30) phone
        VARCHAR(50) document_number
        VARCHAR(500) observations
        BIGINT UNSIGNED status_id FK
    }
    authorized_pickup_assignments {
        BIGINT UNSIGNED id PK
        BIGINT UNSIGNED family_authorized_pickup_id FK
        BIGINT UNSIGNED student_id FK
        BIGINT UNSIGNED status_id FK
        VARCHAR(42) active_pickup_student_key
    }
    enrollments {
        BIGINT UNSIGNED id PK
        BIGINT UNSIGNED student_id FK
        BIGINT UNSIGNED family_id FK
        BIGINT UNSIGNED academic_period_id FK
        BIGINT UNSIGNED grade_id FK
        BIGINT UNSIGNED section_id FK
        VARCHAR(200) billing_responsible_name
        VARCHAR(50) billing_identification
        VARCHAR(254) billing_email
        VARCHAR(30) billing_phone
        TEXT medical_information
        TINYINT(1) requires_institutional_transport
        TINYINT(1) is_authorized_to_leave_alone
        BIGINT UNSIGNED status_id FK
        TIMESTAMP started_at
        TIMESTAMP submitted_at
        TIMESTAMP completed_at
        TIMESTAMP cancelled_at
        VARCHAR(42) active_student_period_key
    }
    institutional_documents {
        BIGINT UNSIGNED id PK
        VARCHAR(200) name
        TEXT description
        BIGINT UNSIGNED status_id FK
    }
    institutional_document_versions {
        BIGINT UNSIGNED id PK
        BIGINT UNSIGNED institutional_document_id FK
        VARCHAR(50) version
        VARCHAR(500) file_reference
        TIMESTAMP published_at
        BIGINT UNSIGNED status_id FK
        VARCHAR(260) active_document_version_key
    }
    document_requirements {
        BIGINT UNSIGNED id PK
        BIGINT UNSIGNED institutional_document_version_id FK
        BIGINT UNSIGNED academic_period_id FK
        TINYINT(1) is_required
        VARCHAR(42) active_requirement_key
    }
    document_acceptances {
        BIGINT UNSIGNED id PK
        BIGINT UNSIGNED enrollment_id FK
        BIGINT UNSIGNED institutional_document_version_id FK
        BIGINT UNSIGNED accepted_by_representative_id FK
        TIMESTAMP accepted_at
        VARCHAR(42) active_acceptance_key
    }
    enrollment_submission_snapshots {
        BIGINT UNSIGNED id PK
        BIGINT UNSIGNED enrollment_id FK
        BIGINT UNSIGNED submitted_by_representative_id FK
        TIMESTAMP submitted_at
        BIGINT UNSIGNED active_enrollment_key
    }
    submitted_address_snapshots {
        BIGINT UNSIGNED id PK
        BIGINT UNSIGNED enrollment_submission_snapshot_id FK
        VARCHAR(100) label
        VARCHAR(150) province_name
        VARCHAR(150) canton_name
        VARCHAR(150) parish_name
        VARCHAR(150) main_street
        VARCHAR(30) street_number
        VARCHAR(150) secondary_street
        VARCHAR(150) sector
        VARCHAR(255) reference
    }
    submitted_emergency_contact_snapshots {
        BIGINT UNSIGNED id PK
        BIGINT UNSIGNED enrollment_submission_snapshot_id FK
        VARCHAR(200) names
        VARCHAR(150) relationship_name
        VARCHAR(30) mobile_phone
        VARCHAR(30) phone
        VARCHAR(254) email
        TINYINT UNSIGNED priority
    }
    submitted_authorized_pickup_snapshots {
        BIGINT UNSIGNED id PK
        BIGINT UNSIGNED enrollment_submission_snapshot_id FK
        VARCHAR(200) names
        VARCHAR(150) relationship_name
        VARCHAR(30) mobile_phone
        VARCHAR(30) phone
        VARCHAR(50) document_number
    }
    status_types ||--o{ statuses : relates
    persons ||--o| users : relates
    persons ||--o| representatives : relates
    persons ||--o| students : relates
    families ||--o{ family_representatives : relates
    representatives ||--o{ family_representatives : relates
    families ||--o{ family_students : relates
    students ||--o{ family_students : relates
    families ||--o{ family_addresses : relates
    families ||--o{ representative_address_assignments : relates
    families ||--o{ student_address_assignments : relates
    family_addresses ||--o{ representative_address_assignments : relates
    family_addresses ||--o{ student_address_assignments : relates
    families ||--o{ family_emergency_contacts : relates
    families ||--o{ family_authorized_pickups : relates
    students ||--o{ enrollments : relates
    families ||--o{ enrollments : relates
    academic_periods ||--o{ enrollments : relates
    grades ||--o{ enrollments : relates
    sections ||--o{ enrollments : relates
    institutional_documents ||--o{ institutional_document_versions : relates
    institutional_document_versions ||--o{ document_requirements : relates
    enrollments ||--o{ document_acceptances : relates
    enrollments ||--o| enrollment_submission_snapshots : relates
    enrollment_submission_snapshots ||--o{ submitted_address_snapshots : relates
    enrollment_submission_snapshots ||--o{ submitted_emergency_contact_snapshots : relates
    enrollment_submission_snapshots ||--o{ submitted_authorized_pickup_snapshots : relates
    provinces ||--o{ cantons : relates
    cantons ||--o{ parishes : relates
```

## Invariantes no expresables por FK
Family común y autorización se validan transaccionalmente. Columnas active_* son generated STORED y soportan unicidad activa. Tablas omitidas: ninguna. Columnas omitidas: auditoría solamente.
