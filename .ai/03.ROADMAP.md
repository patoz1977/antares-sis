# ROADMAP

## Purpose

This document defines the official future planning of the project.

It contains only planned work and future milestones. Current state belongs to `02.CURRENT_STATUS.md`; completed history belongs to `04.DEVELOPMENT_LOG.md`.

---

# Planning Principles

The roadmap represents the agreed direction of the project. It organizes future work without using planning sections as a status report or historical log.

---

# Current Planning Baseline

E003.4A completed formal technical review. Architecture, Domain Model, Decisions, Catalogs, Database Design and ERD are the technically approved Enrollment baseline.

E004.1 — Identity & Access Baseline is completed and merged into `main` through PR #3.

E005 — Person Baseline is completed and merged into `main` through PR #9. ADR-0018 remains the approved record of the completed pre-production rebaseline boundary, after which evolutionary migrations and data-preservation safeguards are mandatory.

E006 — Representative & Student Baseline is completed. Phases 1–5 were merged through PR #14, Administrator Delivery was completed through E006.5 Phase 6, and Validation and Closure approved the integrated baseline.

E006.5 — Family Membership Baseline is completed. Phases 1–6 were implemented, validated and merged through PR #20, and Phase 7 completed the integrated validation and closure audit.

E007 — Representative Access Baseline is completed. Phases 1–6 are implemented and merged through PR #27, Phase 7 completed the integrated validation and closure audit, and PR #28 merged the formal closure at `0b6fdab202142237d83d30e72f8c9938685ab6da`.

E008 — Family Resources is the current authorized functional milestone. Phase 1 aligns the permanent documentation and clean pre-production schema baseline; Phases 2–7 remain planned and are not implemented or authorized to start.

TEST-001 — MariaDB Integration Test Infrastructure remains a separate technical milestone and is not a phase of E008 or of the functional MVP sequence.

ADR-0017 excludes automatic session expiration by inactivity from the MVP. It remains an unprogrammed future capability that requires a separately approved milestone and is disabled by default.

---

# Completed Milestones

## E003.4A — Enrollment Domain Documentation Review

**Status:** Completed

### Result

- AUDIT-001 reconstruction passed formal technical review.
- The official Enrollment documentation was approved as the baseline for future implementation planning.
- No production code or migrations were changed.

---

## E004.1 — Identity & Access Baseline

**Status:** Completed and merged

### Result

- The first modular vertical slice provides secure local login and logout.
- User is an independent Aggregate Root linked to Person.
- ADR-0016 is implemented with a configurable lockout policy, generic responses and security events.
- Automated domain, application, persistence, HTTP and real-session validations passed.
- PR #3 was merged into `main`.

---

## E005 — Person Baseline

**Status:** Completed and merged

### Result

- Person Domain, Persistence, Application and the minimal White Label administrator delivery are implemented.
- The 103 local tests, three HTTP/session scenarios and disposable MariaDB 10.4 validation passed.
- Temporary-database cleanup was confirmed.
- PR #9 was merged into `main`.

---

# Completed Functional Milestone

## E006 — Representative & Student Baseline

**Status:** Completed

### Objective

Implement the stable institutional Representative and Student roles over Person, including Domain, Persistence, Application and the minimal White Label web delivery required by the current administrator.

### Functional Scope

**Representative**

- references one existing Person;
- owns optional EmploymentInformation: occupation, company, position, work phone and work email;
- uses ACTIVE or INACTIVE;
- supports create, query and update operations.

**Student**

- references one existing Person;
- owns institutional code and admission date;
- uses ACTIVE or INACTIVE;
- supports create, query and update operations.

### Invariants

- One Person has at most one Representative and at most one Student independently.
- Neither role duplicates personal data owned by Person.
- Deactivating a role does not deactivate or delete Person.
- Student has no User in the MVP.
- Grade and Section belong to Enrollment, not Student.
- Neither role exists without an existing Person.

### General Exclusions

- Family Membership implementation inside E006; its required baseline is governed separately by E006.5.
- Association between Representative and Student.
- User creation for Representative.
- Bulk import and the Representative Portal.
- Enrollment, Grade or Section implementation.
- Addresses, emergency contacts and authorized pickups.
- Reports and definitive Secretaría, DECE or Enfermería roles.
- Physical deletion, new Catalogs and schema changes not required by the approved baseline.
- Institution-specific rules, generic administration infrastructure and TEST-001 work.

### Approved Phases

1. **Documentation alignment and milestone baseline — completed and merged** — E006 scope, exclusions and phases are established.
2. **Representative Domain — completed and merged** — Aggregate, identity, EmploymentInformation, GENERAL_STATUS lifecycle, invariants and unit tests are implemented.
3. **Student Domain — completed and merged** — Aggregate, identity, InstitutionalCode, AdmissionDate, GENERAL_STATUS lifecycle, invariants and unit tests are implemented.
4. **Persistence — completed, MariaDB validated and merged** — minimal contracts and PDO repositories cover both roles with database-enforced uniqueness and local plus disposable MariaDB coverage.
5. **Application — completed and merged** — minimal create, query and update use cases, typed DTOs and specific errors cover both roles without coordinating Family or User.
6. **Administrator delivery — completed through E006.5** — the White Label routes, controller and composite forms are temporarily restricted to authenticated `admin`, reuse the approved Family Membership flow and add no generic administration panel. The implementation and its MariaDB 10.4 validation were merged through PR #20.
7. **Validation and closure — completed** — the final integrated audit approved Domain, Persistence, Application, orchestration, Delivery, wiring, security, White Label, schema parity, exclusions and the complete local suite.

### Immediate Action

E006 has no remaining functional phase in its approved scope. E007 — Representative Access Baseline follows as the current functional milestone.

---

# Completed Intermediate Technical-Functional Milestone

## E006.5 — Family Membership Baseline

**Status:** Completed

### Objective

Implement only the minimum Family Membership core required for the administrative flow to create one Family and associate Representative and Student correctly before E006 Administrator Delivery.

E006.5 was inserted after the merged E006 Phase 5 to establish the membership prerequisite and deliver the approved administrator flow through Phase 6. Its integrated validation and closure also completed the remaining E006 gates.

### Conceptual Correction

Representative and Student do not have a direct association. Family is the administrative grouping unit, and membership follows this approved model:

```text
Representative
→ FamilyRepresentative
→ Family
→ FamilyStudent
→ Student
```

Representative relates to Family through FamilyRepresentative. Student relates to Family through FamilyStudent. No direct RepresentativeStudent table, Aggregate or use case shall be created.

A new Family is valid only when created atomically with its first active primary FamilyRepresentative. No empty, draft or pending Family state is part of the approved model.

### Target Administrative Flow

**Create a new representative**

```text
Create representative
→ Create Person
→ Create Representative
→ Create Family
→ Create active primary FamilyRepresentative
```

The implemented administrator experience presents this coordination as one visible form, while Application coordinates the Aggregates internally.

**Add a student to the Family**

```text
Add student
→ Create Person
→ Create Student
→ Create active FamilyStudent
```

The Student becomes a member of Family, not directly associated with Representative. This flow starts from Family detail or the primary representative context.

**Add another representative later**

```text
Add representative
→ Create or select Person
→ Create or select Representative
→ Create FamilyRepresentative
```

This compatible future behavior is documented but is not implemented during Phase 1.

### Family Membership Invariants

**Family**

- has FamilyId, DisplayName and status;
- retains at least one active representative;
- groups one or more representatives and students;
- prevents members and resources from crossing Family boundaries.

**FamilyRepresentative**

- references Family and Representative;
- requires RelationshipTypeId from the existing approved catalog;
- contains IsPrimary;
- keeps an active Family and Representative relation unique;
- permits only one active primary representative per Family;
- can be ended without deleting Representative or Person;
- allows one Representative to belong to multiple active Family records.

**FamilyStudent**

- references Family and Student;
- permits one active Family for each Student;
- preserves previous membership history;
- treats a Family change as an administrative operation;
- can be ended without deleting Student or Person.

### Planned Phases

1. **Documentation alignment — completed and merged** — scope, flow, invariants, exclusions and the atomic Family creation decision are established through PR #15.
2. **Family Membership Domain — completed and merged** — Family, its membership Entities, typed Value Objects, temporal behavior and approved internal invariants were merged through PR #16.
3. **Family Membership Persistence — completed, MariaDB validated and merged** — the minimal contract and PDO repository persist the complete Aggregate through `families`, `family_representatives` and `family_students`. The 26 SQLite persistence tests and physical MariaDB 10.4 validation approved atomic creation, database-managed AUTO_INCREMENT identities, complete reconstruction, Representative and Student lookups, membership history, active uniqueness, UTC timestamps, exact GENERAL_STATUS mapping and transactional rollback. Disposable partial-creation and final cleanup were confirmed with no temporary database remaining, and PR #17 merged the phase.
4. **Family Membership Application — completed and merged** — seven focused use cases create, query, add and end memberships through Domain contracts. Immutable DTOs expose complete active and historical membership state; specific errors cover missing external entities, RelationshipType and invalid persisted identities. Representative, Student and RelationshipType existence are prevalidated, a Student cannot have more than one active Family, dates are explicit, and Application owns no transaction or infrastructure concern. PR #18 merged the phase.
5. **Composite administrative orchestration — completed, validated and merged** — `TransactionRunner` and `PdoTransactionRunner` provide one outer shared-PDO transaction per operation. CreateRepresentativeFamily coordinates Person → Representative → Family with its primary membership, while CreateStudentInFamily validates an existing Family before coordinating Person → Student → FamilyStudent. Local and MariaDB 10.4 validation approved complete physical commits, total rollback, unchanged error propagation, preservation of the existing Family and disposable cleanup; PR #19 merged the phase.
6. **Administrator Delivery integration — completed, MariaDB validated and merged** — exactly six protected White Label Family routes expose entry/detail, one composite Representative/Family form and one composite Student form. FamilyController invokes only the approved orchestrators, the temporary gate accepts only authenticated `admin`, POSTs use CSRF, strict HTTP validation and safe known-error mapping, and Student Family authority is bound to session. The 302 local tests and three HTTP/session scenarios pass. MariaDB 10.4 physically approved PdoRelationshipTypeLookup, PdoFamilyFormOptionsProvider, active-only RelationshipTypes, exact ACTIVE/INACTIVE FamilyStatus values, complete commits and deliberate rollbacks for both composite flows, UTC safeguards and partial/final disposable cleanup. No `antares_e0065_delivery_tmp%` database remained, production required no correction and PR #20 merged the phase.
7. **Validation and closure — completed** — the final audit against updated `main` confirmed all PR #10–#20 merges, architectural and functional coherence, 183 optimized autoload classes, 302 local tests with 0 failures, three passing HTTP/session scenarios, complete PHP lint, security, White Label, physical-model parity and preserved exclusions. The previously approved Phase 6 MariaDB 10.4 evidence remains valid; no additional closure run was made because the disposable `E0041_DB_*` environment was unavailable.

### Immediate Action

E006 and E006.5 have no remaining approved phase. E007 — Representative Access Baseline follows as the current functional milestone.

### Exclusions

- family addresses and address assignments;
- emergency contacts and assignments;
- authorized pickups and assignments;
- Representative User creation and credentials;
- Representative Portal;
- Enrollment, Grade and Section;
- bulk import and reports;
- definitive Secretaría, DECE and Enfermería roles;
- direct Representative–Student association;
- physical deletion;
- schema changes and new catalogs or seeders.

RelationshipTypeId reuses the approved catalog. No values are invented. The complete Family model remains broader than E006.5; its resources stay reserved for later milestones.

---

# Completed Functional Milestone

## E007 — Representative Access Baseline

**Status:** Completed — Phases 1–6 merged through PR #27; Phase 7 integrated validation and closure approved

### Objective

Establish the secure Representative access boundary required before the Representative Portal: provision and maintain a Representative User, synchronize its document-based login with Person Identification, resolve the Representative role and authorize an effective Family context on the server.

### Authorized Access Chain

```text
User
→ Person
→ Representative
→ active FamilyRepresentative
→ authorized Family
```

User must be enabled under Identity & Access. Person must resolve the Representative role, and Representative must have an active membership in the effective Family. A Family selected by the client never authorizes by itself. One active Family permits direct entry; multiple active Families require selection from the authorized set and later changes only within that set.

### Approved Functional Decisions

- UserId is the stable technical User identity, independent from personal identification and never used as the visible username.
- User belongs to exactly one Person; Person may exist without User, but User cannot exist without Person.
- For Representative User, LoginIdentifier equals the current Person DocumentNumber. UserId, email and aliases are not alternative login identifiers.
- A Person with Representative User must retain complete Identification.
- Updating DocumentNumber synchronizes Person and User atomically while preserving UserId, PersonId, PasswordHash, UserStatus, failed-login state and relationships.
- LoginIdentifier remains globally unique in the institutional database; Application prevalidation complements the physical UNIQUE concurrency guarantee.
- Cross-Aggregate coordination belongs to Application and one TransactionRunner boundary. It is not Controller logic, SQL ad hoc or a Person persistence side effect.
- Secretaría provisions the User for an existing Representative only after validating Person, role ownership, complete Identification, absence of another User for Person and login availability.
- Secretaría supplies the initial password and may replace it administratively without the old password. The minimum is five characters; letters and/or numbers are allowed without mandatory mixture, uppercase or symbol.
- Only PasswordHash is persisted. The SIS does not email, log, report, export or retain readable credentials, and does not require first-login activation or password change.
- Student has no User and no Portal in the MVP.
- Person email remains optional in general, but every Person with Representative role must retain a valid `Person.ContactInformation.email`, whether or not a Representative User exists. Optional `workEmail` never substitutes it.
- Representative Forgot Password is assisted by Secretaría for the MVP. The public SIS reveals no account state and performs no reset; Secretaría verifies identity externally and uses the existing administrative password change. Automated recovery requires a future decision.

### Phase 2 Implemented Scope

The implemented Phase 2 scope covers:

- explicit User Domain creation/reconstitution and changes to LoginIdentifier and PasswordHash;
- UserRepository lookup by LoginIdentifier and PersonId plus persistence of new and authorized changed Users;
- PdoUserRepository INSERT and UPDATE support;
- administrative Representative User provisioning, duplicate-User prevention and document-derived login;
- password hashing and administrative password replacement;
- atomic Identification/LoginIdentifier synchronization and prevention of Identification removal while a Representative User exists;
- understandable duplicate handling with physical UNIQUE as the final concurrency guarantee;
- one TransactionRunner boundary for cross-Aggregate writes;
- Domain, Application, Persistence, Delivery and disposable MariaDB harness coverage;
- exactly three admin-protected Representative User routes for manage, creation and administrative password replacement.
- approved disposable MariaDB 10.4 evidence for database-generated User identity, findByPersonId, normalized-login and per-Person uniqueness, hashing, exact USER_STATUS, administrative password replacement and complete authentication-state preservation;
- approved atomic DocumentNumber/LoginIdentifier synchronization, full conflict rollback, exclusive resolution and authentication through the new login, compatibility of the existing administrator and complete partial/final disposable cleanup.

### Phase 3 Implemented Scope

The implemented Phase 3 scope covers:

- server-side resolution through `Session authenticatedUserId → GetAuthenticatedUser → AuthenticatedUser.personId → RepresentativeRepository.findByPersonId()`;
- a readonly safe identity result containing only UserId, PersonId, RepresentativeId and LoginIdentifier;
- safe absence for no authenticated User and for an authenticated User without Representative;
- explicit rejection of an incoherent persisted Representative identity;
- explicit by-value conversion between the independent IdentityAccess and Representative PersonId Value Objects;
- no inferred RepresentativeStatus denial, because no such authorization rule is documented;
- read-only Application composition without Family context, additional session state, SQL joins, transactions, HTTP or schema changes;
- local Application, architecture and container wiring coverage plus approved disposable MariaDB 10.4 physical resolution, fail-closed, read-only, regression and cleanup scenarios.

### Phase 4 Implemented Scope

The implemented Phase 4 scope covers:

- current authorized Families resolved from the authenticated Representative through active FamilyRepresentative memberships;
- defensive persisted Family identity, duplicate and active-membership validation without returning Aggregates or membership Entities;
- zero Families as fail-closed absence, one Family as automatic context and multiple Families as explicit-selection state;
- selection and later change only within the currently revalidated authorized set, without using `isPrimary`;
- a private session FamilyId proposal that is revalidated on every resolution and removed or replaced when stale;
- login regeneration that prevents Family context inheritance between Users while preserving unrelated session values;
- the established repository semantics in which active membership, not FamilyStatus, authorizes Family context;
- no Portal Delivery, HTTP, Controller, route, view, transaction, new persistence or schema change;
- approved local Application, session/security, architecture and wiring coverage of the complete zero/one/multiple state matrix, plus disposable MariaDB 10.4 validation of migrations 001–009, seeders, Representative access resolution, one/two-Family context, selection/change, unauthorized and historical exclusion, stale invalidation, read-only behavior, accumulated regression and complete cleanup with no `antares_e007_phase4_tmp%` database remaining.

### Phase 5 Implemented Scope

The implemented Phase 5 scope covers:

- authenticated `GET /representative` and CSRF-protected `POST /representative/family` as the only new Portal routes;
- root dispatch that sends a resolved Representative to the Portal while preserving the administrator dashboard for authenticated Users without Representative identity;
- safe 403 behavior for no Representative identity or no authorized Family, direct entry for exactly one Family, and an authorized selector plus later change for multiple Families;
- server-side revalidation through the Phase 4 use cases for every render and selection, including invalid, unauthorized, historical and stale TOCTOU contexts;
- White Label escaped views that expose only Family display names and the existing logout action, without client-supplied PersonId or RepresentativeId;
- authentication, CSRF and administrator-route isolation using the existing middleware and session infrastructure;
- local Delivery, architecture, wiring and session coverage with 213 optimized autoload classes, 384 tests with 0 failures and three HTTP/session scenarios;
- no Domain, Application use case, Persistence, repository, PDO, transaction, migration, schema, seeder, Family resource, Student, Enrollment or Forgot Password implementation;
- no new MariaDB execution because the phase changes only HTTP/session Delivery and container wiring; the approved Phase 4 physical evidence remains valid and no physical or harness boundary changed.

### Phase 6 Implemented Scope

The implemented Phase 6 scope covers:

- the ADR-0020 cross-Aggregate rule that a Representative always requires `Person.ContactInformation.email`, without making email globally mandatory for Person or adding it to Representative;
- Application enforcement during Representative creation, Representative Person update with or without User, and defensive Representative User provisioning, with `workEmail` explicitly excluded as a substitute;
- required personal email in the composite Representative/Family Delivery and complete rollback when Application rejects missing email after Person creation;
- valid Representative email replacement without changing Representative identity, Family memberships, UserId, LoginIdentifier when DocumentNumber is unchanged, PasswordHash, UserStatus, failed attempts, lock or last access;
- public read-only `GET /forgot-password`, linked from login, with generic White Label assisted-recovery guidance and no submitted identity or account-enumeration path;
- continued use of the existing admin-protected `ChangeRepresentativeUserPassword` operation by Secretaría after external identity verification, with no new administrative screen or use case;
- no automated reset, mail provider, token, OTP, verification code, reset persistence, recovery-specific rate limiting, schema, migration, catalog, seeder or Persistence change;
- local Domain/Application/Delivery/security/architecture coverage with 214 optimized autoload classes, 395 tests and three HTTP/session scenarios;
- approved disposable MariaDB 10.4 execution of migrations 001–009, all three seeders and the accumulated physical harness without failures;
- physical approval of Representative personal-email enforcement, `workEmail` non-substitution, complete composite commit/rollback, email-gated User provisioning with AUTO_INCREMENT/hash/uniqueness, administrative password authentication-state preservation and email plus DocumentNumber/LoginIdentifier synchronization/conflict rollback/authentication;
- approved accumulated physical regression for access resolution, Family-context authorization and stale invalidation, exact schema inventory, status seeds, AdminSeeder compatibility, UTC, concurrency, Person, Representative, Student, Family, Family Delivery and composite Student behavior;
- partial harness cleanup and final manual cleanup approved, with `SHOW DATABASES LIKE 'antares_e007_phase6_tmp%';` returning no rows and no temporary Phase 6 database remaining;
- no MariaDB-specific Forgot Password Delivery scenario because public `GET /forgot-password` is read-only, accepts no account data, performs no lookup or Persistence access and changes no state.

### Phases

1. **Documentation and Functional Alignment — completed and merged** — PR #22 established the permanent Representative User identity, credential, login and access decisions through approved ADR-0019.
2. **Representative User Provisioning & Administrative Credential Management — completed, validated and merged** — User lifecycle and PDO persistence support a database-generated positive UserId, findByPersonId, one User per Person, global normalized-login uniqueness and exact USER_STATUS. Password hashing and administrative replacement preserve UserId, PersonId, login, status, failed attempts, lock and last access. Disposable MariaDB 10.4 approved atomic DocumentNumber/LoginIdentifier synchronization, full rollback on conflict, the new login, removal of the old login, existing administrator compatibility and complete cleanup. PR #23 merged the phase at `878ddf9e481fc8f0a7ed47cf86b5d0731ffc9614`.
3. **Representative Access Resolution — completed, validated and merged** — resolve only the authenticated enabled User through its Person identity to Representative, return a safe identity DTO and fail closed for absence. Local and disposable MariaDB 10.4 validation approved exact physical identity, read-only behavior, safe absence, accumulated regression and complete cleanup. PR #24 merged the phase at `04ebf87da316bf2a0afe7d3da292151f8172a7de`.
4. **Family Context Authorization — completed, validated and merged** — resolve authorized Families only through active FamilyRepresentative membership; enforce zero/one/multiple semantics; revalidate every selected context; invalidate stale session state; prevent context inheritance across Users; preserve FamilyStatus-independent repository semantics; and add no Portal Delivery. The local suite passed with 212 optimized autoload classes, 368 tests and three HTTP/session scenarios. Disposable MariaDB 10.4 approved migrations 001–009, seeders, accumulated regression, exact Representative Family context authorization, selection/change, unauthorized and historical exclusion, stale invalidation, read-only behavior and partial/final cleanup with no residual database. PR #25 merged the phase at `7564bc1b870c17b81f63cb5f26ef37c55704c1b9`.
5. **Representative Portal Entry — completed and merged** — expose only the minimum authenticated Representative entry, resolve the Family context server-side, render zero/one/multiple states safely, permit CSRF-protected selection and change within the current authorized set, preserve administrator isolation, and add no unrelated Portal capability. The local suite passed with 213 optimized autoload classes, 384 tests and three HTTP/session scenarios. PR #26 merged the phase at `118747114e6534c0353d7cbde1ea951cd0b6b3f1`.
6. **Forgot Password Assistance & Representative Email Invariant — completed, validated and merged** — require personal Person email for Representative creation, composite creation, administrative Person update with or without User, and defensive User provisioning while preserving generic Person optionality and existing login/authentication state. Expose only public read-only `GET /forgot-password` with generic assisted-recovery guidance and no account lookup, automated reset, mail, token, OTP or reset persistence. The local suite passed with 214 optimized autoload classes, 395 tests and three HTTP/session scenarios. Disposable MariaDB 10.4 approved the persistent Phase 6 rules, accumulated regression and complete cleanup with no residual `antares_e007_phase6_tmp%` database. PR #27 merged the phase at `23e32006af77c5649f36cabfd942ea44efde1193`.
7. **Validation and Closure — completed** — the final integrated audit approved User provisioning, administrative password management, atomic Person/User synchronization, the Representative email invariant, access resolution, Family context authorization, Portal Delivery, assisted recovery, security, White Label boundaries, architecture, schema parity and preserved exclusions. Final local validation passed with 214 optimized autoload classes, 395 tests with 0 failures, three HTTP/session scenarios, lint on all 282 tracked PHP files and `git diff --check`; the approved Phase 6 disposable MariaDB 10.4 execution and cleanup remain the applicable physical evidence.

### Exclusions

- User and Portal for Student;
- FamilyAddress, address assignments, Emergency Contacts and Authorized Pickups;
- Enrollment, Institutional Documents, Document Acceptance, Submission and Submission Snapshot;
- BillingInformation, MedicalInformation, Transportation and AcademicPlacement;
- Grade or Section administration, enrollment autosave, import, reports and payments;
- definitive Secretaría, DECE or Enfermería roles and generic RBAC;
- TEST-001 and direct RepresentativeStudent association;
- credential reports, exports or readable password storage;
- automated Forgot Password tokens, OTP, verification codes, reset links, email/SMS delivery, expiration, reset persistence or recovery-specific rate limiting.

### Immediate Action

E007 has no remaining approved phase. E008 — Family Resources is the current authorized functional milestone.

---

# Current Functional Milestone

## E008 — Family Resources

**Status:** Authorized and in progress — Phase 1 Documentation and Functional Alignment

### Objective

Complete the Family Aggregate with reusable family resources required by the Representative Portal and future annual enrollment. These resources belong to Family; Enrollment will later consume selected values and copy them into EnrollmentSubmissionSnapshot at Submission.

### Functional Scope

1. FamilyAddress.
2. RepresentativeAddressAssignment.
3. StudentAddressAssignment.
4. FamilyEmergencyContact.
5. EmergencyContactAssignment.
6. FamilyAuthorizedPickup.
7. AuthorizedPickupAssignment.

### Simplified Family Address

ADR-0021 approves MainStreet as the only required textual Address component. StreetNumber, SecondaryStreet, Sector, Reference and Geolocation are optional. When Geolocation exists, Latitude and Longitude form one valid pair and retain their approved bounds. FamilyAddress has no ProvinceId, CantonId or ParishId and does not consume geographic catalogs or require geographic selectors in the MVP.

Province, Canton and Parish remain in the existing baseline. SubmittedAddressSnapshot copies only Label and the address values that FamilyAddress actually owns; it contains no derived geographic codes or names.

### Approved Functional Rules

**FamilyAddress and assignments**

- FamilyAddress belongs to one Family; a Family may own multiple addresses and one address may serve multiple members.
- A Representative has at most one active address per Family and a Student has at most one active address.
- A new assignment replaces or ends the current active assignment.
- Address and member belong to the same Family.
- Representative may assign only to self and related Students and cannot modify another Representative's private address.
- An assigned address cannot be deleted.

**Emergency Contact and assignments**

- Emergency Contact is not Person, belongs to Family and may be assigned to several Students of that Family; a Student may have several contacts.
- Active assignment is unique. Optional Priority is positive and cannot repeat among active assignments for one Student.
- An assigned contact cannot be deleted.
- At least one active contact is required later for Submission, not necessarily when the resource is created.

**Authorized Pickup and assignments**

- Authorized Pickup is not Person, belongs to Family and may be assigned to several Students.
- Active assignment is unique and an assigned pickup cannot be deleted.
- DocumentType plus DocumentNumber are required for an active pickup used in Submission.
- The annual `pickup OR leave-alone` rule belongs to Enrollment and is not implemented in E008.

### Phases

1. **Documentation and Functional Alignment — current** — approve E008 and ADR-0021; simplify Address and SubmittedAddressSnapshot; synchronize Discovery, Domain Model, Catalogs, Database Design, ERD, clean migrations and schema/harness tests.
2. **Family Resources Domain — planned** — FamilyAddress, address assignments, FamilyEmergencyContact, EmergencyContactAssignment, FamilyAuthorizedPickup, AuthorizedPickupAssignment, only behavior-bearing Value Objects, invariants and unit tests.
3. **Family Resources Persistence — planned** — extend the minimal Family repository/PDO boundary for complete reconstruction, database-managed AUTO_INCREMENT, temporal history, status, physical constraints and disposable MariaDB validation.
4. **Family Resources Application — planned** — focused use cases, immutable DTOs, business orchestration independent of authorization and only the minimum external lookups.
5. **Administrator Delivery — planned** — minimum Secretaría/admin capability needed for institutional creation and maintenance without expanding RBAC.
6. **Representative Portal Integration — planned** — permit management only within an authorized Family context, preserving self/related-Student restrictions, without implementing Enrollment.
7. **Validation and Closure — planned** — integrated local/MariaDB audit, documentation synchronization and formal closure.

Phases 2–7 are not implemented and cannot start until separately authorized after Phase 1 is reviewed and merged.

### Exclusions

- Enrollment Aggregate and lifecycle, Submission and SubmissionSnapshot implementation;
- BillingInformation, MedicalInformation, TransportInformation, IsAuthorizedToLeaveAlone and AcademicPlacement;
- Institutional Documents and DocumentAcceptance;
- enrollment autosave, Review, Reopen, Complete and Cancel;
- Grade/Section administration, reports, import and payments;
- Student User or Portal and definitive Secretaría/DECE/Enfermería RBAC;
- TEST-001 and geographic selectors for FamilyAddress.

### Immediate Action

Review the Phase 1 documentary and physical baseline, complete disposable MariaDB 10.4 validation and merge it. Phase 2 — Family Resources Domain may be authorized only after that merge.

---

# Planned MVP Sequence after E008

The following milestones are high-level planning only. E009–E013 are not authorized or started, and their detailed phases are intentionally undefined:

1. E009 — Institutional Documents.
2. E010 — Enrollment Core.
3. E011 — Representative Enrollment Portal.
4. E012 — Submission, Review & Completion.
5. E013 — Basic Enrollment Reports.
6. MVP Validation & Closure.

---

# Independent Technical Milestone

## TEST-001 — MariaDB Integration Test Infrastructure

**Status:** Planned

### Objective

Provide an isolated, reproducible and disposable MariaDB 10.4 environment for safely exercising migrations, concurrency, UTC behavior, seeders, preflight checks, repeated execution and cleanup while preventing accidental use of institutional or production databases.

TEST-001 is technical test infrastructure. It remains independent from E006 and does not authorize migration execution against persistent databases.

---

# Development Workflow

Every future milestone shall follow the same lifecycle:

1. Business analysis.
2. Domain validation.
3. Technical design.
4. Implementation.
5. Testing.
6. Technical review.
7. Documentation update.
8. Commit.
9. Repository synchronization.

No milestone is complete until implementation and documentation are synchronized.

---

# Scope Management

During a milestone:

- no future functionality is incorporated;
- architectural changes require documented justification;
- undefined rules are not implemented;
- permanent project knowledge is recorded in the repository;
- database execution requires explicit authorization.

---

# Architectural Governance

- GitHub is the single source of truth.
- `.ai` contains the official project documentation.
- Approved architecture and domain decisions precede implementation.
- `DATABASE_DESIGN.md` governs the resulting physical schema.
- Business rules drive technical design.
- Long-term maintainability has priority over short-term speed.
- Compatibility is preserved whenever reasonably possible, subject to the pre-production boundary established by ADR-0018.

---

# Documentation Synchronization

When a milestone changes state, review:

- `02.CURRENT_STATUS.md`;
- `03.ROADMAP.md`;
- `04.DEVELOPMENT_LOG.md`.

Review specialized documents only when their responsibility is affected.

---

# Quality Criteria

A planned milestone may be marked complete only when:

- its approved objective is achieved;
- required validation and tests pass;
- documentation is synchronized;
- architectural consistency is verified;
- repository changes are reviewed and confirmed.

---

# Future Product Evolution

After the Enrollment MVP, possible future modules include:

- Academic Management;
- Grades;
- Attendance;
- Billing;
- Finance;
- Human Resources;
- Library;
- Inventory;
- Institutional Communication;
- Analytics;
- External Integrations.

These modules remain outside the current planning baseline.

---

# Document Evolution

**Version:** 5.0

This document describes approved future evolution only. Present state and completed history remain in their specialized documents.
