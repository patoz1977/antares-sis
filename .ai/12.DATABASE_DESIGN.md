# DATABASE DESIGN

**VersiÃ³n:** 1.0
**Estado:** En desarrollo
**Ãšltima actualizaciÃ³n:** Julio 2026

---

# 1. Objetivo

Este documento define el diseÃ±o fÃ­sico de la base de datos del Sistema de InformaciÃ³n Escolar (SIS).

Su propÃ³sito es establecer una arquitectura de persistencia consistente, escalable y mantenible que traduzca el modelo de dominio definido en `DOMAIN_MODEL.md` a una implementaciÃ³n relacional en MySQL.

Este documento constituye la referencia oficial para:

- diseÃ±o de tablas;
- claves primarias y forÃ¡neas;
- restricciones;
- Ã­ndices;
- convenciones de nombres;
- tipos de datos;
- reglas de integridad;
- migraciones.

No describe reglas de negocio. Dichas reglas pertenecen exclusivamente al modelo de dominio.

---

# 2. Objetivos del diseÃ±o

El diseÃ±o fÃ­sico de la base de datos debe cumplir los siguientes objetivos:

- representar fielmente el modelo de dominio;
- minimizar la duplicaciÃ³n de datos;
- garantizar la integridad referencial;
- facilitar futuras ampliaciones;
- mantener consistencia entre mÃ³dulos;
- optimizar consultas frecuentes;
- simplificar el mantenimiento.

El diseÃ±o debe privilegiar claridad y mantenibilidad sobre optimizaciones prematuras.

---

# 3. Principios Generales

## 3.1 El dominio gobierna la base de datos

La base de datos implementa el modelo definido en `DOMAIN_MODEL.md`.

Nunca deberÃ¡ modificar el modelo de dominio para adaptarlo a limitaciones tÃ©cnicas.

---

## 3.2 Una tabla por entidad

Cada entidad persistente del dominio tendrÃ¡ su propia tabla.

Ejemplo:

- persons
- users
- representatives
- students

No se utilizarÃ¡n tablas con mÃºltiples responsabilidades.

---

## 3.3 Una tabla por Business Role

Cada Business Role tendrÃ¡ su propia tabla.

Ejemplo:

- representatives
- students

Nunca existirÃ¡n columnas como:

- is_student
- is_representative
- is_teacher

en la tabla `persons`.

Los roles evolucionan independientemente de la identidad.

---

## 3.4 Una sola fuente de identidad

Toda informaciÃ³n personal reside exclusivamente en `persons`.

Ninguna otra tabla podrÃ¡ duplicar informaciÃ³n como:

- nombres;
- apellidos;
- documento;
- fecha de nacimiento;
- direcciÃ³n;
- correo electrÃ³nico.

Las demÃ¡s tablas referenciarÃ¡n a `persons` mediante claves forÃ¡neas.

---

## 3.5 NormalizaciÃ³n

El diseÃ±o seguirÃ¡, como mÃ­nimo, Tercera Forma Normal (3NF).

La desnormalizaciÃ³n Ãºnicamente podrÃ¡ realizarse cuando exista evidencia objetiva de un beneficio significativo de rendimiento.

Toda excepciÃ³n deberÃ¡ documentarse.

---

## 3.6 Integridad Referencial

Toda relaciÃ³n entre entidades deberÃ¡ implementarse mediante claves forÃ¡neas.

No se permitirÃ¡n referencias implÃ­citas ni identificadores "huÃ©rfanos".

---

## 3.7 EvoluciÃ³n

El esquema deberÃ¡ permitir incorporar nuevos mÃ³dulos sin alterar las tablas existentes.

Ejemplos:

- docentes;
- empleados;
- exalumnos;
- biblioteca;
- calificaciones.

---

# 4. Convenciones de Nombres

La consistencia en los nombres es obligatoria.

## Tablas

Todas las tablas utilizarÃ¡n:

- minÃºsculas;
- plural;
- snake_case.

Ejemplos:

```text
persons
users
students
medical_records
student_emergency_contacts
family_students
```

Nunca utilizar:

```text
Persons
tblPersons
studentProfile
```

---

## Columnas

Todas las columnas utilizarÃ¡n:

- minÃºsculas;
- snake_case.

Ejemplos:

```text
first_name
last_name
birth_date
created_at
updated_at
```

---

## Claves Primarias

Todas las tablas utilizarÃ¡n exactamente:

```text
id
```

Nunca:

```text
person_id
student_id
user_id
```

como clave primaria.

---

## Claves ForÃ¡neas

Las claves forÃ¡neas utilizarÃ¡n el patrÃ³n:

```text
tabla_singular_id
```

Ejemplos:

```text
person_id
student_id
family_id
representative_id
```

---

## Tablas Relacionales

Las tablas puente utilizarÃ¡n:

```text
entidad1_entidad2
```

Ejemplos:

```text
family_students

family_representatives

student_authorized_pickups
```

---

# 5. Claves Primarias

Todas las tablas utilizarÃ¡n una clave primaria sustituta.

CaracterÃ­sticas:

- BIGINT UNSIGNED;
- AUTO_INCREMENT;
- PRIMARY KEY.

Ejemplo:

```sql
id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY
```

No se utilizarÃ¡n claves naturales.

---

# 6. Claves ForÃ¡neas

Todas las relaciones utilizarÃ¡n claves forÃ¡neas explÃ­citas.

Ejemplo:

```text
students.person_id
            â†“
persons.id
```

Toda FK deberÃ¡:

- ser NOT NULL cuando la relaciÃ³n sea obligatoria;
- tener Ã­ndice;
- utilizar restricciones FOREIGN KEY.

---

# 7. AuditorÃ­a

Todas las tablas del dominio incluirÃ¡n los siguientes campos:

```text
created_at
updated_at
```

Tipos:

```sql
TIMESTAMP
```

Estos campos serÃ¡n administrados por la aplicaciÃ³n.

---

## Usuario responsable

Las tablas que requieran trazabilidad incluirÃ¡n:

```text
created_by

updated_by
```

Ambos campos referenciarÃ¡n:

```text
users.id
```

Su incorporaciÃ³n dependerÃ¡ de las necesidades funcionales del mÃ³dulo.

---

# 8. Soft Delete

El sistema utilizarÃ¡ eliminaciÃ³n lÃ³gica.

Campo estÃ¡ndar:

```text
deleted_at
```

Tipo:

```sql
TIMESTAMP NULL
```

Una fila con `deleted_at IS NOT NULL` se considerarÃ¡ eliminada.

No se eliminarÃ¡n registros del dominio mediante `DELETE` fÃ­sico, salvo procesos excepcionales de mantenimiento.

---

# 9. Tipos de Datos

Se utilizarÃ¡n tipos consistentes en todo el sistema.

| InformaciÃ³n | Tipo |
|-------------|------|
| PK | BIGINT UNSIGNED |
| FK | BIGINT UNSIGNED |
| Texto corto | VARCHAR |
| Texto largo | TEXT |
| Fecha | DATE |
| Fecha y hora | TIMESTAMP |
| Booleano | BOOLEAN |
| Decimal | DECIMAL |
| JSON | JSON (solo cuando estÃ© justificado) |

---

## Uso de VARCHAR

Como regla general:

- nombres â†’ VARCHAR(100)
- correos â†’ VARCHAR(255)
- documentos â†’ VARCHAR(50)
- telÃ©fonos â†’ VARCHAR(30)

La longitud deberÃ¡ responder al negocio y no utilizar valores arbitrarios.

---

# 10. OrganizaciÃ³n del Esquema

Las tablas se agrupan por contexto funcional.

## Identity

- persons
- users

---

## Family

- families
- representatives
- family_representatives
- family_students

---

## Academic

- students

---

## Family Resources and Enrollment

- medical_records
- student_emergency_contacts
- student_authorized_pickups
- transports

---

## Enrollment

Se incorporarÃ¡ en el mÃ³dulo correspondiente.

---

## Catalogs

Tablas incluidas en este documento para garantizar trazabilidad completa del modelo fÃ­sico:

- status_types
- statuses
- document_types
- genders
- nationalities
- relationship_types
- blood_types
- transport_types

---

# 11. Principios para Nuevas Tablas

Toda nueva tabla deberÃ¡ responder afirmativamente a las siguientes preguntas:

1. Â¿Representa una entidad del dominio?
2. Â¿Existe ya una tabla equivalente?
3. Â¿Debe pertenecer a un Aggregate existente?
4. Â¿Cumple las convenciones de nombres?
5. Â¿Posee claves primarias y forÃ¡neas consistentes?
6. Â¿Respeta la normalizaciÃ³n?
7. Â¿Incluye auditorÃ­a?
8. Â¿Debe soportar Soft Delete?

Ninguna tabla nueva podrÃ¡ incorporarse sin cumplir estos criterios.

---

# 12. ConvenciÃ³n Base para las Tablas del Dominio

Con el objetivo de mantener uniformidad, trazabilidad y facilitar el mantenimiento del sistema, todas las tablas del dominio deberÃ¡n seguir una estructura base comÃºn.

Esta convenciÃ³n aplica a todas las entidades persistentes del negocio, salvo excepciones debidamente justificadas (por ejemplo, tablas temporales o tablas de sistema).

---

## Estructura Base

Todas las tablas del dominio deberÃ¡n incluir los siguientes campos:

| Campo | Tipo | Obligatorio | DescripciÃ³n |
|--------|------|-------------|-------------|
| id | BIGINT UNSIGNED | SÃ­ | Clave primaria sustituta. |
| created_at | TIMESTAMP | SÃ­ | Fecha y hora de creaciÃ³n. |
| updated_at | TIMESTAMP | SÃ­ | Fecha y hora de la Ãºltima modificaciÃ³n. |
| deleted_at | TIMESTAMP NULL | No | Fecha y hora de eliminaciÃ³n lÃ³gica. |
| created_by | BIGINT UNSIGNED NULL | No | Usuario que creÃ³ el registro. |
| updated_by | BIGINT UNSIGNED NULL | No | Usuario que realizÃ³ la Ãºltima modificaciÃ³n. |
| deleted_by | BIGINT UNSIGNED NULL | No | Usuario que realizÃ³ la eliminaciÃ³n lÃ³gica. |

---

## AuditorÃ­a

Los campos de auditorÃ­a permiten reconstruir el historial operativo del sistema.

Cuando un mÃ³dulo no requiera auditorÃ­a de usuarios, los campos:

- created_by
- updated_by
- deleted_by

podrÃ¡n permanecer NULL.

Sin embargo, la estructura deberÃ¡ mantenerse para conservar uniformidad entre todas las tablas.

---

## Integridad Referencial

Los campos:

- created_by
- updated_by
- deleted_by

referenciarÃ¡n:

```text
users.id
```

mediante claves forÃ¡neas cuando sean utilizados.

---

## EliminaciÃ³n LÃ³gica

El sistema no eliminarÃ¡ registros del dominio mediante operaciones DELETE.

En su lugar se actualizarÃ¡n los campos:

```text
deleted_at
deleted_by
```

Un registro serÃ¡ considerado activo cuando:

```sql
deleted_at IS NULL
```

---

## Beneficios

Esta convenciÃ³n proporciona:

- auditorÃ­a uniforme;
- simplificaciÃ³n del cÃ³digo;
- menor cantidad de excepciones;
- facilidad para generar reportes;
- trazabilidad completa;
- soporte para restauraciÃ³n futura;
- consistencia entre todos los mÃ³dulos.

---

## Excepciones

Ãšnicamente podrÃ¡n omitir esta estructura:

- tablas temporales;
- tablas de sesiÃ³n;
- tablas de cachÃ©;
- tablas de migraciones;
- tablas estrictamente tÃ©cnicas.

Toda excepciÃ³n deberÃ¡ documentarse y justificarse.

---

# 13. Principio de Estados

A partir de esta secciÃ³n se describe el diseÃ±o detallado de cada tabla del sistema.

Cada definiciÃ³n incluirÃ¡:

- propÃ³sito;
- columnas;
- claves primarias;
- claves forÃ¡neas;
- restricciones;
- Ã­ndices;
- reglas de integridad;
- observaciones de diseÃ±o.

Las tablas se documentarÃ¡n siguiendo el mismo formato para garantizar consistencia en todo el proyecto.

---

# Principio de Estados

Todas las entidades cuyo ciclo de vida requiera representar un estado deberÃ¡n utilizar un Ãºnico campo:

```text
status_id
```

Este campo referenciarÃ¡ el catÃ¡logo `statuses`.

El catÃ¡logo `statuses` estarÃ¡ organizado mediante `status_types`, permitiendo reutilizar la misma estructura para todos los mÃ³dulos del sistema.

No se utilizarÃ¡n columnas booleanas para representar estados de negocio, tales como:

- is_active
- is_enabled
- is_locked
- is_deleted
- state

La evoluciÃ³n de los estados deberÃ¡ realizarse mediante datos del catÃ¡logo y no mediante cambios en el esquema de la base de datos.

---

# 14. DiseÃ±o FÃ­sico de las Tablas

---

# Tabla: status_types

## PropÃ³sito

Agrupa los diferentes tipos de estados utilizados por el sistema.

Permite reutilizar una Ãºnica estructura para administrar los estados de mÃºltiples mÃ³dulos sin mezclar estados de naturaleza diferente.

---

## Responsabilidad

Clasificar los estados disponibles para cada contexto funcional.

Ejemplos:

- USER_STATUS
- STUDENT_STATUS
- ENROLLMENT_STATUS
- FAMILY_STATUS
- TRANSPORT_STATUS

---

## Columnas

| Campo | Tipo | Null | DescripciÃ³n |
|--------|------|------|-------------|
| id | BIGINT UNSIGNED | No | Clave primaria. |
| code | VARCHAR(50) | No | CÃ³digo Ãºnico del tipo de estado. |
| name | VARCHAR(100) | No | Nombre descriptivo. |
| description | VARCHAR(255) | SÃ­ | DescripciÃ³n opcional. |
| created_at | TIMESTAMP | No | AuditorÃ­a. |
| updated_at | TIMESTAMP | No | AuditorÃ­a. |
| deleted_at | TIMESTAMP | SÃ­ | Soft Delete. |
| created_by | BIGINT UNSIGNED | SÃ­ | Usuario creador. |
| updated_by | BIGINT UNSIGNED | SÃ­ | Usuario modificador. |
| deleted_by | BIGINT UNSIGNED | SÃ­ | Usuario eliminador. |

---

## Clave Primaria

```text
id
```

---

## Claves ForÃ¡neas

```text
created_by â†’ users.id

updated_by â†’ users.id

deleted_by â†’ users.id
```

---

## Restricciones

- code debe ser Ãºnico.
- name debe ser Ãºnico.

---

## Ãndices

- PK(id)
- UK(code)
- UK(name)

---

## Relaciones

Un StatusType puede tener mÃºltiples Status.

RelaciÃ³n:

```text
status_types (1)
        â”‚
        â””â”€â”€â”€â”€â”€â”€< statuses (N)
```

---

## Observaciones

Los registros de esta tabla serÃ¡n administrados exclusivamente por el sistema.

No deberÃ¡n eliminarse cuando existan estados asociados.

---

# Tabla: statuses

## PropÃ³sito

Almacena los estados utilizados por las entidades del sistema.

---

## Responsabilidad

Representar el ciclo de vida de una entidad mediante un catÃ¡logo configurable.

---

## Columnas

| Campo | Tipo | Null | DescripciÃ³n |
|--------|------|------|-------------|
| id | BIGINT UNSIGNED | No | Clave primaria. |
| status_type_id | BIGINT UNSIGNED | No | Tipo de estado. |
| code | VARCHAR(50) | No | CÃ³digo del estado. |
| name | VARCHAR(100) | No | Nombre visible. |
| description | VARCHAR(255) | SÃ­ | DescripciÃ³n. |
| display_order | SMALLINT UNSIGNED | No | Orden de presentaciÃ³n. |
| color | VARCHAR(20) | SÃ­ | Color utilizado por la interfaz. |
| is_default | BOOLEAN | No | Estado inicial del tipo correspondiente. |
| is_terminal | BOOLEAN | No | Indica si representa un estado final. |
| created_at | TIMESTAMP | No | AuditorÃ­a. |
| updated_at | TIMESTAMP | No | AuditorÃ­a. |
| deleted_at | TIMESTAMP | SÃ­ | Soft Delete. |
| created_by | BIGINT UNSIGNED | SÃ­ | Usuario creador. |
| updated_by | BIGINT UNSIGNED | SÃ­ | Usuario modificador. |
| deleted_by | BIGINT UNSIGNED | SÃ­ | Usuario eliminador. |

---

## Clave Primaria

```text
id
```

---

## Claves ForÃ¡neas

```text
status_type_id â†’ status_types.id

created_by â†’ users.id

updated_by â†’ users.id

deleted_by â†’ users.id
```

---

## Restricciones

- (status_type_id, code) debe ser Ãºnico.
- Solo podrÃ¡ existir un registro is_default = TRUE por cada status_type_id.
- display_order debe ser Ãºnico dentro de cada status_type_id.

---

## Ãndices

- PK(id)
- IDX(status_type_id)
- UK(status_type_id, code)
- IDX(display_order)

---

## Relaciones

```text
status_types (1)
        â”‚
        â””â”€â”€â”€â”€â”€â”€< statuses (N)
```

Las entidades del dominio referenciarÃ¡n esta tabla mediante:

```text
status_id
```

---

## Observaciones

No se crearÃ¡n columnas como:

- is_active
- is_enabled
- state

en ninguna tabla del dominio.

Toda entidad con ciclo de vida utilizarÃ¡ un Ãºnico campo:

```text
status_id
```

que referenciarÃ¡ esta tabla.

---

# Tabla: document_types

## PropÃ³sito

Definir los tipos de documento de identidad permitidos para las personas del sistema.

---

## Responsabilidad

Normalizar la clasificaciÃ³n de documentos utilizada por `persons`.

---

## Columnas

| Campo | Tipo | Null | DescripciÃ³n |
|--------|------|------|-------------|
| id | BIGINT UNSIGNED | No | Clave primaria. |
| code | VARCHAR(50) | No | CÃ³digo Ãºnico del tipo de documento. |
| name | VARCHAR(100) | No | Nombre visible. |
| description | VARCHAR(255) | SÃ­ | DescripciÃ³n opcional. |
| display_order | SMALLINT UNSIGNED | SÃ­ | Orden de presentaciÃ³n. |
| created_at | TIMESTAMP | No | AuditorÃ­a. |
| updated_at | TIMESTAMP | No | AuditorÃ­a. |
| deleted_at | TIMESTAMP | SÃ­ | Soft Delete. |
| created_by | BIGINT UNSIGNED | SÃ­ | Usuario creador. |
| updated_by | BIGINT UNSIGNED | SÃ­ | Usuario modificador. |
| deleted_by | BIGINT UNSIGNED | SÃ­ | Usuario eliminador. |

---

## Clave Primaria

```text
id
```

---

## Claves ForÃ¡neas

```text
created_by â†’ users.id

updated_by â†’ users.id

deleted_by â†’ users.id
```

---

## Restricciones

- code debe ser Ãºnico.
- name debe ser Ãºnico.

---

## Ãndices

- PK(id)
- UK(code)
- UK(name)
- IDX(display_order)

---

## Relaciones

```text
document_types (1)
      â”‚
      â””â”€â”€â”€â”€â”€â”€< persons (N)
```

---

## Observaciones

Este catÃ¡logo se utiliza exclusivamente para clasificar el documento de identidad en `persons.document_type_id`.

---

# Tabla: genders

## PropÃ³sito

Definir las opciones de gÃ©nero disponibles para registro de personas.

---

## Responsabilidad

Normalizar el atributo de gÃ©nero utilizado por `persons`.

---

## Columnas

| Campo | Tipo | Null | DescripciÃ³n |
|--------|------|------|-------------|
| id | BIGINT UNSIGNED | No | Clave primaria. |
| code | VARCHAR(50) | No | CÃ³digo Ãºnico. |
| name | VARCHAR(100) | No | Nombre visible. |
| description | VARCHAR(255) | SÃ­ | DescripciÃ³n opcional. |
| display_order | SMALLINT UNSIGNED | SÃ­ | Orden de presentaciÃ³n. |
| created_at | TIMESTAMP | No | AuditorÃ­a. |
| updated_at | TIMESTAMP | No | AuditorÃ­a. |
| deleted_at | TIMESTAMP | SÃ­ | Soft Delete. |
| created_by | BIGINT UNSIGNED | SÃ­ | Usuario creador. |
| updated_by | BIGINT UNSIGNED | SÃ­ | Usuario modificador. |
| deleted_by | BIGINT UNSIGNED | SÃ­ | Usuario eliminador. |

---

## Clave Primaria

```text
id
```

---

## Claves ForÃ¡neas

```text
created_by â†’ users.id

updated_by â†’ users.id

deleted_by â†’ users.id
```

---

## Restricciones

- code debe ser Ãºnico.
- name debe ser Ãºnico.

---

## Ãndices

- PK(id)
- UK(code)
- UK(name)
- IDX(display_order)

---

## Relaciones

```text
genders (1)
    â”‚
    â””â”€â”€â”€â”€â”€â”€< persons (N)
```

---

## Observaciones

La relaciÃ³n con `persons` es opcional a nivel fÃ­sico mediante `persons.gender_id` nullable.

---

# Tabla: nationalities

## PropÃ³sito

Definir las nacionalidades disponibles para las personas registradas en el sistema.

---

## Responsabilidad

Normalizar el atributo de nacionalidad utilizado por `persons`.

---

## Columnas

| Campo | Tipo | Null | DescripciÃ³n |
|--------|------|------|-------------|
| id | BIGINT UNSIGNED | No | Clave primaria. |
| code | VARCHAR(50) | No | CÃ³digo Ãºnico. |
| name | VARCHAR(100) | No | Nombre visible. |
| description | VARCHAR(255) | SÃ­ | DescripciÃ³n opcional. |
| display_order | SMALLINT UNSIGNED | SÃ­ | Orden de presentaciÃ³n. |
| created_at | TIMESTAMP | No | AuditorÃ­a. |
| updated_at | TIMESTAMP | No | AuditorÃ­a. |
| deleted_at | TIMESTAMP | SÃ­ | Soft Delete. |
| created_by | BIGINT UNSIGNED | SÃ­ | Usuario creador. |
| updated_by | BIGINT UNSIGNED | SÃ­ | Usuario modificador. |
| deleted_by | BIGINT UNSIGNED | SÃ­ | Usuario eliminador. |

---

## Clave Primaria

```text
id
```

---

## Claves ForÃ¡neas

```text
created_by â†’ users.id

updated_by â†’ users.id

deleted_by â†’ users.id
```

---

## Restricciones

- code debe ser Ãºnico.
- name debe ser Ãºnico.

---

## Ãndices

- PK(id)
- UK(code)
- UK(name)
- IDX(display_order)

---

## Relaciones

```text
nationalities (1)
      â”‚
      â””â”€â”€â”€â”€â”€â”€< persons (N)
```

---

## Observaciones

La relaciÃ³n con `persons` es opcional a nivel fÃ­sico mediante `persons.nationality_id` nullable.

---

# Tabla: relationship_types

## PropÃ³sito

Definir los tipos de relaciÃ³n interpersonal usados en asociaciones familiares y del perfil del estudiante.

---

## Responsabilidad

Unificar la clasificaciÃ³n de parentescos y relaciones en:

- family_representatives
- student_emergency_contacts
- student_authorized_pickups

---

## Columnas

| Campo | Tipo | Null | DescripciÃ³n |
|--------|------|------|-------------|
| id | BIGINT UNSIGNED | No | Clave primaria. |
| code | VARCHAR(50) | No | CÃ³digo Ãºnico. |
| name | VARCHAR(100) | No | Nombre visible. |
| description | VARCHAR(255) | SÃ­ | DescripciÃ³n opcional. |
| display_order | SMALLINT UNSIGNED | SÃ­ | Orden de presentaciÃ³n. |
| created_at | TIMESTAMP | No | AuditorÃ­a. |
| updated_at | TIMESTAMP | No | AuditorÃ­a. |
| deleted_at | TIMESTAMP | SÃ­ | Soft Delete. |
| created_by | BIGINT UNSIGNED | SÃ­ | Usuario creador. |
| updated_by | BIGINT UNSIGNED | SÃ­ | Usuario modificador. |
| deleted_by | BIGINT UNSIGNED | SÃ­ | Usuario eliminador. |

---

## Clave Primaria

```text
id
```

---

## Claves ForÃ¡neas

```text
created_by â†’ users.id

updated_by â†’ users.id

deleted_by â†’ users.id
```

---

## Restricciones

- code debe ser Ãºnico.
- name debe ser Ãºnico.

---

## Ãndices

- PK(id)
- UK(code)
- UK(name)
- IDX(display_order)

---

## Relaciones

```text
relationship_types (1)
       â”‚
       â”œâ”€â”€â”€â”€â”€â”€< family_representatives (N)
       â”œâ”€â”€â”€â”€â”€â”€< student_emergency_contacts (N)
       â””â”€â”€â”€â”€â”€â”€< student_authorized_pickups (N)
```

---

## Observaciones

Las tres FK hacia `relationship_types` son opcionales a nivel fÃ­sico (nullable) para permitir registros transitorios cuando el tipo de relaciÃ³n aÃºn no se haya especificado.

---

# Tabla: blood_types

## PropÃ³sito

Definir los tipos de sangre disponibles para el expediente mÃ©dico del estudiante.

---

## Responsabilidad

Normalizar el valor de tipo de sangre utilizado por `medical_records`.

---

## Columnas

| Campo | Tipo | Null | DescripciÃ³n |
|--------|------|------|-------------|
| id | BIGINT UNSIGNED | No | Clave primaria. |
| code | VARCHAR(50) | No | CÃ³digo Ãºnico. |
| name | VARCHAR(100) | No | Nombre visible. |
| description | VARCHAR(255) | SÃ­ | DescripciÃ³n opcional. |
| display_order | SMALLINT UNSIGNED | SÃ­ | Orden de presentaciÃ³n. |
| created_at | TIMESTAMP | No | AuditorÃ­a. |
| updated_at | TIMESTAMP | No | AuditorÃ­a. |
| deleted_at | TIMESTAMP | SÃ­ | Soft Delete. |
| created_by | BIGINT UNSIGNED | SÃ­ | Usuario creador. |
| updated_by | BIGINT UNSIGNED | SÃ­ | Usuario modificador. |
| deleted_by | BIGINT UNSIGNED | SÃ­ | Usuario eliminador. |

---

## Clave Primaria

```text
id
```

---

## Claves ForÃ¡neas

```text
created_by â†’ users.id

updated_by â†’ users.id

deleted_by â†’ users.id
```

---

## Restricciones

- code debe ser Ãºnico.
- name debe ser Ãºnico.

---

## Ãndices

- PK(id)
- UK(code)
- UK(name)
- IDX(display_order)

---

## Relaciones

```text
blood_types (1)
     â”‚
     â””â”€â”€â”€â”€â”€â”€< medical_records (N)
```

---

## Observaciones

La relaciÃ³n con `medical_records` es opcional a nivel fÃ­sico mediante `medical_records.blood_type_id` nullable.

---

# Tabla: transport_types

## PropÃ³sito

Definir los tipos de transporte escolar permitidos por el sistema.

---

## Responsabilidad

Normalizar el tipo de transporte utilizado por `transports`.

---

## Columnas

| Campo | Tipo | Null | DescripciÃ³n |
|--------|------|------|-------------|
| id | BIGINT UNSIGNED | No | Clave primaria. |
| code | VARCHAR(50) | No | CÃ³digo Ãºnico. |
| name | VARCHAR(100) | No | Nombre visible. |
| description | VARCHAR(255) | SÃ­ | DescripciÃ³n opcional. |
| display_order | SMALLINT UNSIGNED | SÃ­ | Orden de presentaciÃ³n. |
| created_at | TIMESTAMP | No | AuditorÃ­a. |
| updated_at | TIMESTAMP | No | AuditorÃ­a. |
| deleted_at | TIMESTAMP | SÃ­ | Soft Delete. |
| created_by | BIGINT UNSIGNED | SÃ­ | Usuario creador. |
| updated_by | BIGINT UNSIGNED | SÃ­ | Usuario modificador. |
| deleted_by | BIGINT UNSIGNED | SÃ­ | Usuario eliminador. |

---

## Clave Primaria

```text
id
```

---

## Claves ForÃ¡neas

```text
created_by â†’ users.id

updated_by â†’ users.id

deleted_by â†’ users.id
```

---

## Restricciones

- code debe ser Ãºnico.
- name debe ser Ãºnico.

---

## Ãndices

- PK(id)
- UK(code)
- UK(name)
- IDX(display_order)

---

## Relaciones

```text
transport_types (1)
      â”‚
      â””â”€â”€â”€â”€â”€â”€< transports (N)
```

---

## Observaciones

Cada registro de `transports` debe referenciar exactamente un tipo de transporte mediante `transport_type_id`.

---

# Tabla: persons

## PropÃ³sito

Almacenar la identidad Ãºnica de toda persona registrada en el sistema.

---

## Responsabilidad

Centralizar toda la informaciÃ³n personal independiente de los roles que una persona pueda desempeÃ±ar.

Una Person puede posteriormente convertirse en:

- Representative
- Student
- Teacher
- Employee
- Alumni
- Applicant

sin duplicar informaciÃ³n.

---

## Columnas

| Campo | Tipo | Null | DescripciÃ³n |
|--------|------|------|-------------|
| id | BIGINT UNSIGNED | No | Clave primaria. |
| status_id | BIGINT UNSIGNED | No | Estado de la persona. |
| document_type_id | BIGINT UNSIGNED | No | Tipo de documento. |
| document_number | VARCHAR(50) | No | NÃºmero de documento. |
| first_name | VARCHAR(100) | No | Primer nombre. |
| middle_name | VARCHAR(100) | SÃ­ | Segundo nombre. |
| last_name | VARCHAR(100) | No | Primer apellido. |
| second_last_name | VARCHAR(100) | SÃ­ | Segundo apellido. |
| preferred_name | VARCHAR(100) | SÃ­ | Nombre preferido. |
| birth_date | DATE | SÃ­ | Fecha de nacimiento. |
| gender_id | BIGINT UNSIGNED | SÃ­ | GÃ©nero. |
| nationality_id | BIGINT UNSIGNED | SÃ­ | Nacionalidad. |
| email | VARCHAR(255) | SÃ­ | Correo electrÃ³nico. |
| mobile_phone | VARCHAR(30) | SÃ­ | TelÃ©fono mÃ³vil. |
| home_phone | VARCHAR(30) | SÃ­ | TelÃ©fono convencional. |
| address | VARCHAR(255) | SÃ­ | DirecciÃ³n principal. |
| notes | TEXT | SÃ­ | Observaciones. |
| created_at | TIMESTAMP | No | AuditorÃ­a. |
| updated_at | TIMESTAMP | No | AuditorÃ­a. |
| deleted_at | TIMESTAMP | SÃ­ | Soft Delete. |
| created_by | BIGINT UNSIGNED | SÃ­ | Usuario creador. |
| updated_by | BIGINT UNSIGNED | SÃ­ | Usuario modificador. |
| deleted_by | BIGINT UNSIGNED | SÃ­ | Usuario eliminador. |

---

## Clave Primaria

```text
id
```

---

## Claves ForÃ¡neas

```text
status_id â†’ statuses.id

document_type_id â†’ document_types.id

gender_id â†’ genders.id

nationality_id â†’ nationalities.id

created_by â†’ users.id

updated_by â†’ users.id

deleted_by â†’ users.id
```

---

## Restricciones

- document_number debe ser Ãºnico junto con document_type_id.
- first_name es obligatorio.
- last_name es obligatorio.

---

## Ãndices

- PK(id)
- UK(document_type_id, document_number)
- IDX(last_name)
- IDX(email)
- IDX(status_id)

---

## Relaciones

```text
persons (1)
      â”‚
      â”œâ”€â”€â”€â”€â”€â”€ users
      â”œâ”€â”€â”€â”€â”€â”€ representatives
      â”œâ”€â”€â”€â”€â”€â”€ students
      â”œâ”€â”€â”€â”€â”€â”€ student_emergency_contacts
      â””â”€â”€â”€â”€â”€â”€ student_authorized_pickups
```

---

## Observaciones

Toda informaciÃ³n personal del sistema deberÃ¡ almacenarse exclusivamente en esta tabla.

Ninguna otra entidad podrÃ¡ duplicar datos personales.

---

# Tabla: users

## PropÃ³sito

Administrar las credenciales de autenticaciÃ³n y autorizaciÃ³n del sistema.

---

## Responsabilidad

Representar una cuenta de acceso asociada a una Person.

No almacena informaciÃ³n personal.

---

## Columnas

| Campo | Tipo | Null | DescripciÃ³n |
|--------|------|------|-------------|
| id | BIGINT UNSIGNED | No | Clave primaria. |
| person_id | BIGINT UNSIGNED | No | Persona asociada. |
| status_id | BIGINT UNSIGNED | No | Estado de la cuenta. |
| username | VARCHAR(100) | No | Nombre de usuario. |
| email | VARCHAR(255) | No | Correo utilizado para autenticaciÃ³n. |
| password_hash | VARCHAR(255) | No | ContraseÃ±a cifrada. |
| password_changed_at | TIMESTAMP | SÃ­ | Ãšltimo cambio de contraseÃ±a. |
| last_login_at | TIMESTAMP | SÃ­ | Ãšltimo acceso. |
| failed_login_attempts | SMALLINT UNSIGNED | No | Intentos fallidos. |
| locked_until | TIMESTAMP | SÃ­ | Bloqueo temporal. |
| created_at | TIMESTAMP | No | AuditorÃ­a. |
| updated_at | TIMESTAMP | No | AuditorÃ­a. |
| deleted_at | TIMESTAMP | SÃ­ | Soft Delete. |
| created_by | BIGINT UNSIGNED | SÃ­ | Usuario creador. |
| updated_by | BIGINT UNSIGNED | SÃ­ | Usuario modificador. |
| deleted_by | BIGINT UNSIGNED | SÃ­ | Usuario eliminador. |

---

## Clave Primaria

```text
id
```

---

## Claves ForÃ¡neas

```text
person_id â†’ persons.id

status_id â†’ statuses.id

created_by â†’ users.id

updated_by â†’ users.id

deleted_by â†’ users.id
```

---

## Restricciones

- person_id debe ser Ãºnico.
- username debe ser Ãºnico.
- email debe ser Ãºnico.

---

## Ãndices

- PK(id)
- UK(person_id)
- UK(username)
- UK(email)
- IDX(status_id)

---

## Relaciones

```text
persons (1)
      â”‚
      â””â”€â”€â”€â”€â”€â”€ users (0..1)
```

---

## Observaciones

Una Person puede existir sin User.

Un User nunca puede existir sin Person.

Las contraseÃ±as Ãºnicamente se almacenarÃ¡n mediante algoritmos de hash seguros.

No se almacenarÃ¡n contraseÃ±as en texto plano bajo ninguna circunstancia.

---

# Tabla: families

## PropÃ³sito

Representar la unidad administrativa que agrupa representantes y estudiantes.

---

## Responsabilidad

Servir como Aggregate Root para la administraciÃ³n familiar.

No representa parentescos ni relaciones biolÃ³gicas.

---

## Columnas

| Campo | Tipo | Null | DescripciÃ³n |
|--------|------|------|-------------|
| id | BIGINT UNSIGNED | No | Clave primaria. |
| status_id | BIGINT UNSIGNED | No | Estado de la familia. |
| family_code | VARCHAR(30) | No | CÃ³digo interno Ãºnico. |
| name | VARCHAR(150) | SÃ­ | Nombre descriptivo de la familia. |
| notes | TEXT | SÃ­ | Observaciones. |
| created_at | TIMESTAMP | No | AuditorÃ­a. |
| updated_at | TIMESTAMP | No | AuditorÃ­a. |
| deleted_at | TIMESTAMP | SÃ­ | Soft Delete. |
| created_by | BIGINT UNSIGNED | SÃ­ | Usuario creador. |
| updated_by | BIGINT UNSIGNED | SÃ­ | Usuario modificador. |
| deleted_by | BIGINT UNSIGNED | SÃ­ | Usuario eliminador. |

---

## Clave Primaria

```text
id
```

---

## Claves ForÃ¡neas

```text
status_id â†’ statuses.id

created_by â†’ users.id

updated_by â†’ users.id

deleted_by â†’ users.id
```

---

## Restricciones

- family_code debe ser Ãºnico.

---

## Ãndices

- PK(id)
- UK(family_code)
- IDX(status_id)

---

## Relaciones

```text
families (1)
      â”‚
      â”œâ”€â”€â”€â”€â”€â”€ family_representatives
      â””â”€â”€â”€â”€â”€â”€ family_students
```

---

## Observaciones

La composiciÃ³n de una familia se administra exclusivamente mediante las tablas de relaciÃ³n.

Nunca contendrÃ¡ referencias directas a estudiantes o representantes.

---

# Tabla: representatives

## PropÃ³sito

Representar el Business Role de representante.

---

## Responsabilidad

Habilitar a una Person para participar como representante dentro del dominio.

---

## Columnas

| Campo | Tipo | Null | DescripciÃ³n |
|--------|------|------|-------------|
| id | BIGINT UNSIGNED | No | Clave primaria. |
| person_id | BIGINT UNSIGNED | No | Persona asociada. |
| status_id | BIGINT UNSIGNED | No | Estado del representante. |
| occupation | VARCHAR(150) | SÃ­ | OcupaciÃ³n. |
| company | VARCHAR(150) | SÃ­ | Empresa. |
| work_phone | VARCHAR(30) | SÃ­ | TelÃ©fono laboral. |
| created_at | TIMESTAMP | No | AuditorÃ­a. |
| updated_at | TIMESTAMP | No | AuditorÃ­a. |
| deleted_at | TIMESTAMP | SÃ­ | Soft Delete. |
| created_by | BIGINT UNSIGNED | SÃ­ | Usuario creador. |
| updated_by | BIGINT UNSIGNED | SÃ­ | Usuario modificador. |
| deleted_by | BIGINT UNSIGNED | SÃ­ | Usuario eliminador. |

---

## Clave Primaria

```text
id
```

---

## Claves ForÃ¡neas

```text
person_id â†’ persons.id

status_id â†’ statuses.id

created_by â†’ users.id

updated_by â†’ users.id

deleted_by â†’ users.id
```

---

## Restricciones

- person_id debe ser Ãºnico.

---

## Ãndices

- PK(id)
- UK(person_id)
- IDX(status_id)

---

## Relaciones

```text
persons (1)
      â”‚
      â””â”€â”€â”€â”€â”€â”€ representatives (0..1)

representatives (1)
      â”‚
      â””â”€â”€â”€â”€â”€â”€ family_representatives
```

---

## Observaciones

No almacena informaciÃ³n personal.

Toda la informaciÃ³n personal pertenece a `persons`.

---

# Tabla: family_representatives

## PropÃ³sito

Relacionar representantes con familias.

---

## Responsabilidad

Administrar la pertenencia de representantes a una familia.

---

## Columnas

| Campo | Tipo | Null | DescripciÃ³n |
|--------|------|------|-------------|
| id | BIGINT UNSIGNED | No | Clave primaria. |
| status_id | BIGINT UNSIGNED | No | Estado del registro. |
| family_id | BIGINT UNSIGNED | No | Familia. |
| representative_id | BIGINT UNSIGNED | No | Representante. |
| relationship_type_id | BIGINT UNSIGNED | SÃ­ | RelaciÃ³n con el estudiante (padre, madre, tutor, etc.). |
| is_primary | BOOLEAN | No | Representante principal. |
| receives_notifications | BOOLEAN | No | Recibe comunicaciones institucionales. |
| created_at | TIMESTAMP | No | AuditorÃ­a. |
| updated_at | TIMESTAMP | No | AuditorÃ­a. |
| deleted_at | TIMESTAMP | SÃ­ | Soft Delete. |
| created_by | BIGINT UNSIGNED | SÃ­ | Usuario creador. |
| updated_by | BIGINT UNSIGNED | SÃ­ | Usuario modificador. |
| deleted_by | BIGINT UNSIGNED | SÃ­ | Usuario eliminador. |

---

## Clave Primaria

```text
id
```

---

## Claves ForÃ¡neas

```text
status_id â†’ statuses.id

family_id â†’ families.id

representative_id â†’ representatives.id

relationship_type_id â†’ relationship_types.id

created_by â†’ users.id

updated_by â†’ users.id

deleted_by â†’ users.id
```

---

## Restricciones

- (family_id, representative_id) debe ser Ãºnico.

---

## Ãndices

- PK(id)
- IDX(status_id)
- UK(family_id, representative_id)
- IDX(representative_id)

---

## Relaciones

```text
families (1)
      â”‚
      â””â”€â”€â”€â”€â”€â”€ family_representatives (N)

representatives (1)
      â”‚
      â””â”€â”€â”€â”€â”€â”€ family_representatives (N)
```

---

## Observaciones

Una familia puede tener mÃºltiples representantes.

Un representante puede pertenecer a mÃºltiples familias.

La capa de aplicaciÃ³n deberÃ¡ garantizar que toda Family posea al menos un Representative activo.

---

# Tabla: family_students

## PropÃ³sito

Relacionar estudiantes con familias.

---

## Responsabilidad

Determinar a quÃ© unidad familiar pertenece cada estudiante.

---

## Columnas

| Campo | Tipo | Null | DescripciÃ³n |
|--------|------|------|-------------|
| id | BIGINT UNSIGNED | No | Clave primaria. |
| status_id | BIGINT UNSIGNED | No | Estado del registro. |
| family_id | BIGINT UNSIGNED | No | Familia. |
| student_id | BIGINT UNSIGNED | No | Estudiante. |
| created_at | TIMESTAMP | No | AuditorÃ­a. |
| updated_at | TIMESTAMP | No | AuditorÃ­a. |
| deleted_at | TIMESTAMP | SÃ­ | Soft Delete. |
| created_by | BIGINT UNSIGNED | SÃ­ | Usuario creador. |
| updated_by | BIGINT UNSIGNED | SÃ­ | Usuario modificador. |
| deleted_by | BIGINT UNSIGNED | SÃ­ | Usuario eliminador. |

---

## Clave Primaria

```text
id
```

---

## Claves ForÃ¡neas

```text
status_id â†’ statuses.id

family_id â†’ families.id

student_id â†’ students.id

created_by â†’ users.id

updated_by â†’ users.id

deleted_by â†’ users.id
```

---

## Restricciones

- (family_id, student_id) debe ser Ãºnico.

---

## Ãndices

- PK(id)
- IDX(status_id)
- UK(family_id, student_id)
- IDX(student_id)

---

## Relaciones

```text
families (1)
      â”‚
      â””â”€â”€â”€â”€â”€â”€ family_students (N)

students (1)
      â”‚
      â””â”€â”€â”€â”€â”€â”€ family_students (N)
```

---

## Observaciones

La pertenencia de un estudiante a una familia se administra exclusivamente mediante esta tabla.

No deberÃ¡n existir referencias directas entre `students` y `families`.

La existencia de un Student sin una relaciÃ³n activa en `family_students` constituye un estado invÃ¡lido del dominio.

Esta regla serÃ¡ garantizada por la capa de aplicaciÃ³n durante la creaciÃ³n y mantenimiento del estudiante.

---

# Tabla: students

## PropÃ³sito

Representar el Business Role de estudiante.

---

## Responsabilidad

Habilitar a una Person para participar como estudiante dentro del sistema.

Toda la informaciÃ³n acadÃ©mica serÃ¡ administrada por mÃ³dulos posteriores.

---

## Columnas

| Campo | Tipo | Null | DescripciÃ³n |
|--------|------|------|-------------|
| id | BIGINT UNSIGNED | No | Clave primaria. |
| person_id | BIGINT UNSIGNED | No | Persona asociada. |
| status_id | BIGINT UNSIGNED | No | Estado del estudiante. |
| student_code | VARCHAR(30) | No | CÃ³digo institucional del estudiante. |
| admission_date | DATE | SÃ­ | Fecha de ingreso. |
| created_at | TIMESTAMP | No | AuditorÃ­a. |
| updated_at | TIMESTAMP | No | AuditorÃ­a. |
| deleted_at | TIMESTAMP | SÃ­ | Soft Delete. |
| created_by | BIGINT UNSIGNED | SÃ­ | Usuario creador. |
| updated_by | BIGINT UNSIGNED | SÃ­ | Usuario modificador. |
| deleted_by | BIGINT UNSIGNED | SÃ­ | Usuario eliminador. |

---

## Clave Primaria

```text
id
```

---

## Claves ForÃ¡neas

```text
person_id â†’ persons.id

status_id â†’ statuses.id

created_by â†’ users.id

updated_by â†’ users.id

deleted_by â†’ users.id
```

---

## Restricciones

- person_id debe ser Ãºnico.
- student_code debe ser Ãºnico.

---

## Ãndices

- PK(id)
- UK(person_id)
- UK(student_code)
- IDX(status_id)

---

## Relaciones

```text
persons (1)
      â”‚
      â””â”€â”€â”€â”€â”€â”€ students (0..1)

students (1)
      â”œâ”€â”€â”€â”€â”€â”€ family_students
      â”œâ”€â”€â”€â”€â”€â”€ medical_records
      â”œâ”€â”€â”€â”€â”€â”€ student_emergency_contacts
      â”œâ”€â”€â”€â”€â”€â”€ student_authorized_pickups
      â””â”€â”€â”€â”€â”€â”€ transports
```

---

## Observaciones

No almacena informaciÃ³n personal.

Toda la informaciÃ³n de identidad pertenece a `persons`.

---

# Tabla: medical_records

## PropÃ³sito

Almacenar la informaciÃ³n mÃ©dica relevante del estudiante.

---

## Responsabilidad

Centralizar la informaciÃ³n necesaria para la atenciÃ³n y seguridad del estudiante.

---

## Columnas

| Campo | Tipo | Null | DescripciÃ³n |
|--------|------|------|-------------|
| id | BIGINT UNSIGNED | No | Clave primaria. |
| student_id | BIGINT UNSIGNED | No | Estudiante. |
| blood_type_id | BIGINT UNSIGNED | SÃ­ | Tipo de sangre. |
| allergies | TEXT | SÃ­ | Alergias conocidas. |
| medications | TEXT | SÃ­ | MedicaciÃ³n permanente. |
| medical_conditions | TEXT | SÃ­ | Condiciones mÃ©dicas relevantes. |
| emergency_notes | TEXT | SÃ­ | Observaciones mÃ©dicas. |
| created_at | TIMESTAMP | No | AuditorÃ­a. |
| updated_at | TIMESTAMP | No | AuditorÃ­a. |
| deleted_at | TIMESTAMP | SÃ­ | Soft Delete. |
| created_by | BIGINT UNSIGNED | SÃ­ | Usuario creador. |
| updated_by | BIGINT UNSIGNED | SÃ­ | Usuario modificador. |
| deleted_by | BIGINT UNSIGNED | SÃ­ | Usuario eliminador. |

---

## Clave Primaria

```text
id
```

---

## Claves ForÃ¡neas

```text
student_id â†’ students.id

blood_type_id â†’ blood_types.id

created_by â†’ users.id

updated_by â†’ users.id

deleted_by â†’ users.id
```

---

## Restricciones

- student_id debe ser Ãºnico.

---

## Ãndices

- PK(id)
- UK(student_id)

---

## Relaciones

```text
students (1)
      â”‚
      â””â”€â”€â”€â”€â”€â”€ medical_records (0..1)
```

---

## Observaciones

Cada estudiante podrÃ¡ tener un Ãºnico expediente mÃ©dico vigente.

---

# Tabla: student_emergency_contacts

## PropÃ³sito

Administrar los contactos de emergencia de un estudiante.

---

## Responsabilidad

Relacionar un estudiante con una Person autorizada para ser contactada en caso de emergencia.

---

## Columnas

| Campo | Tipo | Null | DescripciÃ³n |
|--------|------|------|-------------|
| id | BIGINT UNSIGNED | No | Clave primaria. |
| status_id | BIGINT UNSIGNED | No | Estado del registro. |
| student_id | BIGINT UNSIGNED | No | Estudiante. |
| person_id | BIGINT UNSIGNED | No | Persona de contacto. |
| relationship_type_id | BIGINT UNSIGNED | SÃ­ | Parentesco o relaciÃ³n. |
| priority | SMALLINT UNSIGNED | No | Prioridad de contacto. |
| notes | TEXT | SÃ­ | Observaciones sobre el contacto de emergencia. |
| created_at | TIMESTAMP | No | AuditorÃ­a. |
| updated_at | TIMESTAMP | No | AuditorÃ­a. |
| deleted_at | TIMESTAMP | SÃ­ | Soft Delete. |
| created_by | BIGINT UNSIGNED | SÃ­ | Usuario creador. |
| updated_by | BIGINT UNSIGNED | SÃ­ | Usuario modificador. |
| deleted_by | BIGINT UNSIGNED | SÃ­ | Usuario eliminador. |

---

## Clave Primaria

```text
id
```

---

## Claves ForÃ¡neas

```text
status_id â†’ statuses.id

student_id â†’ students.id

person_id â†’ persons.id

relationship_type_id â†’ relationship_types.id

created_by â†’ users.id

updated_by â†’ users.id

deleted_by â†’ users.id
```

---

## Restricciones

- (student_id, person_id) debe ser Ãºnico.

---

## Ãndices

- PK(id)
- IDX(status_id)
- UK(student_id, person_id)
- IDX(person_id)

---

## Relaciones

```text
students (1)
      â”‚
      â””â”€â”€â”€â”€â”€â”€ student_emergency_contacts (N)

persons (1)
      â”‚
      â””â”€â”€â”€â”€â”€â”€ student_emergency_contacts (N)
```

---

## Observaciones

El contacto de emergencia puede o no ser representante del estudiante.

---

# Tabla: student_authorized_pickups

## PropÃ³sito

Administrar las personas autorizadas para retirar al estudiante.

---

## Responsabilidad

Relacionar estudiantes con personas autorizadas para su retiro.

---

## Columnas

| Campo | Tipo | Null | DescripciÃ³n |
|--------|------|------|-------------|
| id | BIGINT UNSIGNED | No | Clave primaria. |
| status_id | BIGINT UNSIGNED | No | Estado del registro. |
| student_id | BIGINT UNSIGNED | No | Estudiante. |
| person_id | BIGINT UNSIGNED | No | Persona autorizada. |
| relationship_type_id | BIGINT UNSIGNED | SÃ­ | RelaciÃ³n con el estudiante. |
| notes | TEXT | SÃ­ | Observaciones sobre el contacto de emergencia. |
| created_at | TIMESTAMP | No | AuditorÃ­a. |
| updated_at | TIMESTAMP | No | AuditorÃ­a. |
| deleted_at | TIMESTAMP | SÃ­ | Soft Delete. |
| created_by | BIGINT UNSIGNED | SÃ­ | Usuario creador. |
| updated_by | BIGINT UNSIGNED | SÃ­ | Usuario modificador. |
| deleted_by | BIGINT UNSIGNED | SÃ­ | Usuario eliminador. |

---

## Clave Primaria

```text
id
```

---

## Claves ForÃ¡neas

```text
status_id â†’ statuses.id

student_id â†’ students.id

person_id â†’ persons.id

relationship_type_id â†’ relationship_types.id

created_by â†’ users.id

updated_by â†’ users.id

deleted_by â†’ users.id
```

---

## Restricciones

- (student_id, person_id) debe ser Ãºnico.

---

## Ãndices

- PK(id)
- IDX(status_id)
- UK(student_id, person_id)
- IDX(person_id)

---

## Relaciones

```text
students (1)
      â”‚
      â””â”€â”€â”€â”€â”€â”€ student_authorized_pickups (N)

persons (1)
      â”‚
      â””â”€â”€â”€â”€â”€â”€ student_authorized_pickups (N)
```

---

## Observaciones

Una persona puede estar autorizada para retirar a mÃºltiples estudiantes.

---

# Tabla: transports

## PropÃ³sito

Administrar la informaciÃ³n del servicio de transporte utilizado por un estudiante.

---

## Responsabilidad

Registrar si un estudiante utiliza transporte escolar y almacenar la informaciÃ³n necesaria para su gestiÃ³n.

---

## Columnas

| Campo | Tipo | Null | DescripciÃ³n |
|--------|------|------|-------------|
| id | BIGINT UNSIGNED | No | Clave primaria. |
| student_id | BIGINT UNSIGNED | No | Estudiante. |
| status_id | BIGINT UNSIGNED | No | Estado del servicio. |
| transport_type_id | BIGINT UNSIGNED | No | Tipo de transporte. |
| provider_name | VARCHAR(150) | SÃ­ | Nombre del proveedor o transportista. |
| driver_name | VARCHAR(150) | SÃ­ | Nombre del conductor. |
| driver_phone | VARCHAR(30) | SÃ­ | TelÃ©fono del conductor. |
| vehicle_description | VARCHAR(150) | SÃ­ | DescripciÃ³n del vehÃ­culo. |
| vehicle_plate | VARCHAR(20) | SÃ­ | Placa del vehÃ­culo. |
| notes | TEXT | SÃ­ | Observaciones. |
| created_at | TIMESTAMP | No | AuditorÃ­a. |
| updated_at | TIMESTAMP | No | AuditorÃ­a. |
| deleted_at | TIMESTAMP | SÃ­ | Soft Delete. |
| created_by | BIGINT UNSIGNED | SÃ­ | Usuario creador. |
| updated_by | BIGINT UNSIGNED | SÃ­ | Usuario modificador. |
| deleted_by | BIGINT UNSIGNED | SÃ­ | Usuario eliminador. |

---

## Clave Primaria

```text
id
```

---

## Claves ForÃ¡neas

```text
student_id â†’ students.id

status_id â†’ statuses.id

transport_type_id â†’ transport_types.id

created_by â†’ users.id

updated_by â†’ users.id

deleted_by â†’ users.id
```

---

## Restricciones

- student_id debe ser Ãºnico.

---

## Ãndices

- PK(id)
- UK(student_id)
- IDX(status_id)
- IDX(transport_type_id)

---

## Relaciones

```text
students (1)
      â”‚
      â””â”€â”€â”€â”€â”€â”€ transports (0..1)
```

---

## Observaciones

Cada estudiante podrÃ¡ tener un Ãºnico registro de transporte vigente.

El detalle operativo del servicio (rutas, paradas, recorridos, control de abordaje, etc.) serÃ¡ administrado por mÃ³dulos posteriores.

---

# 15. Resumen del Modelo FÃ­sico

El modelo fÃ­sico definido para el mÃ³dulo E002.1 estÃ¡ compuesto por las siguientes tablas:

## CatÃ¡logos Base

- status_types
- statuses

## CatÃ¡logos Generales

- document_types
- genders
- nationalities
- relationship_types
- blood_types

## CatÃ¡logos Funcionales

- transport_types

## Identidad y AutenticaciÃ³n

- persons
- users

## Familias

- families
- representatives
- family_representatives
- family_students

## AcadÃ©mico

- students

## InformaciÃ³n del Estudiante

- medical_records
- student_emergency_contacts
- student_authorized_pickups
- transports

---

## Principios del Modelo

- Una Ãºnica identidad por persona (`persons`).
- Los Business Roles se implementan mediante tablas independientes.
- Ninguna tabla duplica informaciÃ³n personal.
- Todas las relaciones utilizan claves forÃ¡neas.
- Todas las tablas del dominio implementan auditorÃ­a y Soft Delete.
- Todas las entidades con ciclo de vida utilizan `status_id`.
- El modelo cumple Tercera Forma Normal (3NF).
- El modelo estÃ¡ preparado para crecimiento modular sin modificaciones estructurales significativas.


---

# 16. Convenciones de ImplementaciÃ³n

Las decisiones descritas en este documento constituyen la referencia oficial para:

- generaciÃ³n del SQL;
- migraciones;
- modelos del dominio;
- repositorios;
- Entity Relationship Diagram (ERD);
- validaciones arquitectÃ³nicas;
- generaciÃ³n asistida por IA.

Toda implementaciÃ³n deberÃ¡ respetar estas definiciones.

Las modificaciones al modelo fÃ­sico deberÃ¡n realizarse primero en este documento antes de implementarse en cÃ³digo.


