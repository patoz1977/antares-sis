# Current Status

## Purpose

This document provides a real-time snapshot of the project.

It is the primary entry point for understanding the current implementation status, the work currently in progress and the immediate priorities.

This document intentionally contains only the present state of the project.

Historical information belongs exclusively to `04.DEVELOPMENT_LOG.md`.

Future planning belongs exclusively to `03.ROADMAP.md`.

---

# Project Status

## Overall Status

The Enrollment Domain Discovery is closed, the AUDIT-001 documentation reconstruction has completed formal technical review, and the temporary User lockout policy required by E004.1 has been approved through ADR-0016.

Architecture, Domain Model, Decisions, Catalogs, Database Design and ERD are **technically approved as the official Enrollment baseline**.

E004.1 — Identity & Access Baseline is implemented, reviewed and technically approved. Two formal reviews closed all BLOCKER and HIGH findings, and the three final MEDIUM observations in the MariaDB harness were corrected. Local validation passes. Full execution of the isolated MariaDB suite is transferred to the independent technical milestone TEST-001 and is not a retroactive closure requirement for E004.1. PR #3 is ready for merge. Migration 014 has not been deployed or executed on `UEAnt` or production.

## Current Branch

`feature/e004-1-identity-access-baseline`

## Current Milestone

E004.1 — Identity & Access Baseline — technically completed; merge and controlled deployment pending

## Last Completed Work

E004.1 — Identity & Access Baseline:

- User is implemented as an independent Aggregate Root linked to Person;
- the active login and logout flow uses the IdentityAccess vertical slice;
- ADR-0016 is enforced through a typed, configurable policy;
- authentication now loads and locks User inside the transaction and performs one password verification per attempt;
- migration 014 is forward-only, preflights incompatible identifiers and preserves active-model state on repeated execution;
- the administrator seeder requires an external initial credential and preserves existing User password and status;
- the isolated MariaDB suite now covers UTC timestamp round trips, strict lock-error classification and partial-creation cleanup;
- local domain, application, persistence, HTTP and real-session validations pass.

## Current Objective

Merge approved PR #3, then plan deployment of migration 014 through the controlled database workflow. TEST-001 proceeds independently.

## Current Risks

- The legacy migration 012 fails while creating a foreign key on MariaDB 10.4; this predates E004.1 and blocks a full empty-database run of 001–014 until addressed separately.
- Migration 014 retains `locked_until` as an unused compatibility column; its future removal requires a separately reviewed cleanup migration.
- The full MariaDB 10.4 harness still requires the isolated, reproducible and disposable infrastructure planned as TEST-001; this does not reopen E004.1.
- Official catalog values not yet supplied remain a data-master concern, not a schema decision.

## Pending Decisions

The temporary User lockout ambiguity is resolved by ADR-0016. The session-inactivity observation is resolved by ADR-0017 and is not a pending E004.1 correction. No additional business or indispensable physical-design decision is recorded as open for the authorized corrections.

## Documentation Status

| Document | Status |
|---|---|
| Project Context | Current |
| Enrollment Domain Discovery | Closed |
| Architecture | Technically approved |
| Domain Model | Technically approved |
| Decisions | Technically approved |
| Catalogs | Technically approved |
| Database Design | Technically approved |
| ERD | Technically approved |
| AUDIT-001 | Completed and technically approved |
| Current Status | Current |
| Roadmap | Current |
| Development Log | Current |

## Repository Status

- Feature branch `feature/e004-1-identity-access-baseline` active.
- ADR-0017 excludes automatic session inactivity expiration from E004.1 while preserving regeneration and complete logout destruction.
- IdentityAccess production code, migration 014, seeders and tests passed two formal reviews; no BLOCKER or HIGH findings remain and the three final MEDIUM harness observations are corrected.
- Composer validation, strict dev autoload, 29 local tests, three HTTP/real-session scenarios, lint of 126 tracked PHP files and diff checks pass.
- Full isolated MariaDB harness execution belongs to TEST-001 and is tracked separately from E004.1 closure.
- Rebuild drafts were promoted to official documents; no draft file remains tracked in Git.
- GitHub remains the single source of truth.

## Immediate Next Action

Merge PR #3 without deploying migration 014 implicitly. Any deployment must follow the controlled database workflow; TEST-001 separately provisions and validates the MariaDB integration infrastructure.

## Last Updated

July 2026
