# Current Status

## Purpose

This document provides a real-time snapshot of the project.

It is the primary entry point for understanding the current implementation status, the work currently in progress and the immediate priorities.

This document intentionally contains only the present state of the project.

Historical information belongs exclusively to `04.DEVELOPMENT_LOG.md`.

Future planning belongs exclusively to `03.ROADMAP.md`.

---

# Project Status

## Overall Status

The Enrollment Domain Discovery is closed and the AUDIT-001 documentation reconstruction is technically approved as the official Enrollment baseline.

E004.1 — Identity & Access Baseline is completed and merged into `main` through PR #3.

E005 — Person Baseline is completed and merged into `main` through PR #9. Person Domain, Persistence, Application and the minimal White Label administrator delivery are implemented. The accumulated 103 local tests and three HTTP/session scenarios passed, as did the disposable MariaDB 10.4 validation and cleanup of its temporary databases.

E006 — Representative & Student Baseline is completed. Representative and Student Domain, Persistence and Application were merged through PR #14; Administrator Delivery was completed through E006.5 Phase 6, and the integrated Validation and Closure audit approved the baseline without a remaining in-scope functional phase.

E006.5 — Family Membership Baseline is completed. Phases 1–6 are implemented, validated and merged through PR #20, and Phase 7 Validation and Closure approved the integrated baseline without a blocking inconsistency.

E007 — Representative Access Baseline is completed and formally closed. Phases 1–6 were implemented, validated and merged through PR #22–#27; Phase 7 Validation and Closure approved the integrated baseline, and PR #28 merged that closure into `main` at `0b6fdab202142237d83d30e72f8c9938685ab6da`.

E008 — Family Resources is completed and formally merged. Phases 1–6 were merged through PR #29–#34, PR #35 merged the Phase 6.1 Representative Portal Authorization Correction, and PR #36 merged the approved Phase 7 closure documentation into `main` at `d128f7dd3dbcf25ba0833af934fe743927143a55`.

E009 — Institutional Acknowledgements is completed and formally validated. PR #37–#43 merged Phases 1–6, PR #44 corrected Requirement post-use Update/Completion concurrency, PR #45 corrected Requirement Create/Completion concurrency and PR #46 merged the final closure baseline into `main` at `05b41f7482fae68bcb3d812f3962aef5424a0468`. Three adversarial Phase 7 audits were performed. The final known lock inversion between AcademicPeriod lifecycle and Requirement Create is accepted for the MVP as a non-blocking operational risk under ADR-0024; no blocking functional, security, architecture or persistence defect remains.

Final E009 validation passed with 387 optimized autoload classes, 591 tests and 0 failures, three HTTP/session scenarios, lint for all 475 tracked PHP files and portable route inventories under LF and CRLF. MariaDB was not rerun for closure: exact Git comparison found no relevant delta from the physically validated PR #45 head. Reused MariaDB 10.4.32 evidence covers migrations 001–009, all seeders, 35 functional tables, 36 physical tables including `migrations`, 58 FKs, both corrected concurrency protocols, accumulated regression and zero residual databases. It does not claim that the ADR-0024 risk is resolved or deadlock-free.

E010 — Enrollment Core is completed and formally validated. PR #47 merged the Documentation and Scope Baseline, PR #48 merged Enrollment Domain, PR #49 merged Enrollment Persistence, PR #50 merged Enrollment Application and PR #51 merged the Active Family Capture Serialization correction into `main` at `f28f1630d7207528df1875f63fef3bdd658663b3`. E010 intentionally contains no Delivery phase.

The second Phase 5 audit approved the then-current integrated E010 baseline. ADR-0025 and the E012 corrective baseline now remove the internal SubmissionSnapshot while preserving the Enrollment Aggregate, lifecycle, annual Value Objects, Repository/PDO persistence, Draft initialization and reads, five Draft-maintenance operations, read-only Grade/Section boundary, root serialization, exact context checks, architecture, security and White Label boundaries.

The first Phase 5 audit ended with `CLOSURE = NO` because active Family capture could race with the end of FamilyStudent membership. PR #51 fully corrected that blocker: `StartEnrollmentDraft` now locks the exact authoritative active FamilyStudent row/key inside its Application-owned transaction, reconstructs and verifies the same membership, and rejects a stale requested Family before persistence. Start-first, membership-change-first, current-Family, rollback/lock-release, cross-Student isolation and Start-vs-Start are approved. MariaDB was not rerun during closure because exact comparison from physically validated PR #51 head `9e1957ce7ad93509a5cd3025dd6be55c0420c9af` to merged `main` found zero file delta. Reused MariaDB 10.4.32 evidence covers migrations 001–009, StatusTypeSeeder, StatusSeeder, AdminSeeder, 35 functional/36 physical tables, 58 FKs, accumulated regression and zero residual `antares_e010_fam_tmp%` databases.

PR #52 merged the E010 closure baseline into `main` at `22d8c9c69beef8b9581495d56760272eb9e7cf1a`. PR #53 merged the E011 Documentation and Scope Baseline, PR #54 merged Application Coordination, PR #55 merged Representative Enrollment Delivery, PR #56 merged Autosave and Navigation UX, and PR #57 merged the formal closure into `main` at `1454e2c7acea66e7db29a606f1227082e91369b8`. E011 — Representative Enrollment Portal is COMPLETED AND FORMALLY VALIDATED with `CLOSURE = YES` and no functional, security, architecture or data-loss blocker.

The approved E011 authority chain is `Authenticated User → Person → Representative → active FamilyRepresentative → selected authorized Family → active FamilyStudent → Student → unique ACTIVE AcademicPeriod → Enrollment(Student, AcademicPeriod)`. Every protected operation re-resolves this chain on the server. A FamilyId, StudentId, AcademicPeriodId or EnrollmentId supplied by the browser is never authority; the existing zero/one/multiple Family-context behavior remains the single selector model.

E011 reuses the existing Institutional Acknowledgements gate and Family Resources capabilities. It adds no second acknowledgement or resource model, no PreviousEnrollment selection or historical annual preloading, and no Domain/Persistence phase unless a later implementation audit proves a real missing invariant. E012 remains responsible for Submission, complete cross-Aggregate validation and post-Draft lifecycle operations; E013 remains responsible for reporting.

Phase 2 adds the trusted Representative/Family/Student/ACTIVE AcademicPeriod coordination, resolve-or-start Draft initialization without nested transactions, Representative-safe Person/contact/employment updates, authorized Student Person updates, Representative-specific annual Enrollment wrappers, a readonly portal state and non-persisted advisory progress. Writes use the deterministic order `AcademicPeriod shared stabilization → FamilyRepresentative → FamilyStudent when applicable → Person/Representative/Enrollment`; identities supplied by a future browser remain stale-context proposals, never authority.

Local validation passed with 488 optimized autoload classes, 671 tests and 0 failures, all three HTTP/session scenarios and lint for all 66 changed/new PHP files. Disposable MariaDB 10.4.32 validation passed migrations 001–009, StatusTypeSeeder, StatusSeeder, AdminSeeder, 35 functional/36 physical tables, 58 FKs, accumulated regression, Person/Representative same-row serialization, unrelated-row isolation, FamilyRepresentative/FamilyStudent revocation in both race orders, shared ACTIVE-period stabilization, lifecycle exclusion, stale-period rejection and rollback lock release. Automatic cleanup passed; the independent `SHOW DATABASES LIKE 'antares_e011_p2_tmp%';` check returned zero rows and no manual cleanup was required.

Phase 3 adds the minimal server-rendered Representative Enrollment delivery: one authenticated GET, CSRF-protected explicit POST actions, PRG redirects, safe validation rendering, current Student navigation, advisory progress, approved Representative/Student/annual Draft forms and read-only Academic Placement. Browser identities remain stale-context proposals and Family Resources are reused only through the existing portal link. The corrective E012 baseline now makes live Representative/Student data maintenance independent from EnrollmentStatus while annual Enrollment-owned data remains read-only outside DRAFT.

Phase 3 local validation passed with 490 optimized autoload classes, 683 tests and 0 failures, all three HTTP/session scenarios, lint for all 11 changed/new PHP files, security/architecture scans and clean diff checks. MariaDB was not rerun because Phase 3 changes no Domain, Persistence, PDO adapter, database core, migration, seeder or physical harness; the merged PR #54 MariaDB 10.4.32 evidence remains applicable and is not presented as a fresh execution.

Phase 4 layers progressive autosave over the exact Phase 3 section POST routes through `Accept: application/json`, compact safe responses and a dedicated dependency-free script. The server-rendered HTML/PRG fallback and fallback Save buttons remain functional without JavaScript. The client maintains form-local dirty/revision state, waits exactly 900 ms, prevents overlapping requests for the same form, safely handles edits made during an in-flight save, flushes controlled navigation sequentially, warns before unsafe exit and makes only a best-effort keepalive attempt. Medical dependent values are cleared and disabled after an explicit No, while advisory progress remains non-authoritative.

Final Phase 5 validation passed with 491 optimized autoload classes, 686 tests and 0 failures, all three HTTP/session scenarios, lint for all 585 tracked PHP files, Node.js 24.19.0 syntax validation and the exact route inventory under LF and CRLF. Authenticated runtime validation in the Codex in-app browser exercised all eight editable sections, 900 ms autosave, `Saving...`/`Saved`/`Save error`, one-time CSRF rotation, an edit during an in-flight save, valid and invalid controlled navigation, server failure and recovery, Medical No clearing, Family/Student navigation and the ordinary no-JavaScript HTML/PRG fallback. Values were confirmed against a synthetic disposable MariaDB fixture and its database, user, sessions and local servers were fully removed.

MariaDB was not rerun through the complete harness because exact Git comparison from the physically validated PR #54 baseline found no later Domain, Application business-rule, Persistence, PDO, database-core, migration, seeder or harness delta. Reused MariaDB 10.4.32 evidence covers migrations 001–009, StatusTypeSeeder, StatusSeeder, AdminSeeder, 35 functional/36 physical tables, 58 FKs, E011 concurrency and revocation scenarios, accumulated regression and zero residual `antares_e011_p2_tmp%` databases. The browser fixture was independent runtime evidence and is not presented as a replacement physical harness run. The best-effort `pagehide` save did not complete during an observed rapid tab close, which is an accepted non-blocking operational risk because controlled navigation and ordinary autosave are the guaranteed persistence paths.

E012 — Enrollment Submission & Lifecycle is COMPLETED, FORMALLY VALIDATED AND MERGED. PR #58 merged Phase 1, PR #59 merged the Corrective Baseline — Remove Submission Snapshot, PR #60 merged Phase 2 — Submission Application Coordination, PR #61 merged Phase 3 — Representative Review & Submission Delivery, PR #62 merged Phase 4 — Administrative Lifecycle Application, PR #63 merged Phase 5 — Administrative Review & Lifecycle Delivery, and PR #64 merged the validated Phase 6 closure into `main` at `82df0459c5948e94ae4316d5b803dfd3ccf886a8`. E012 has no remaining in-scope phase or technical blocker.

ADR-0025 replaces the former snapshot decision. Enrollment now owns only annual information and lifecycle timestamps; Person, Representative, Student and Family remain current live SIS data. EnrollmentStatus controls only Enrollment-owned annual maintenance: Billing, Medical, Transport and leave-alone are DRAFT-only, while authorized Representative Personal/Contact/Employment and permitted Student Personal maintenance remain available after SUBMITTED, COMPLETED or CANCELLED. AcademicPlacement remains read-only to Representative. Authentication, Family authorization and membership revalidation, ACTIVE AcademicPeriod behavior, Institutional Acknowledgements and all field restrictions remain unchanged.

The corrective baseline removes the snapshot Domain model and PDO persistence and adds migration `010_remove_submission_snapshots`, whose rollback recreates the exact migration 009 structure. Fresh validation passed with 483 optimized autoload classes, 660 tests and 0 failures, all three HTTP/session scenarios and complete modified-PHP lint. MariaDB 10.4.32 passed migrations 001–010, all three seeders, 31 functional tables plus `migrations`, 53 FKs, migration 010 rollback/reapply, Enrollment/E010/E011 accumulated regression and automatic cleanup. Independent `SHOW DATABASES LIKE 'antares_e012_snap_tmp%';` returned zero rows; no manual cleanup was required.

Phase 2 provides one deterministic authoritative completeness validator, a side-effect-free Representative Review boundary and one transactional Submit boundary for first Submission and same-Enrollment Resubmission. The proven lock order is `GENERAL_STATUS/ACTIVE shared stabilization → acknowledgement AcademicPeriod shared configuration scope → Family root → active FamilyRepresentative → active FamilyStudent → Person roots by ascending PersonId → Enrollment root`. Local validation passed with 503 optimized autoload classes, 673 tests and 0 failures, all three HTTP/session scenarios and lint for all 38 changed/new PHP files. MariaDB 10.4.32 passed migrations 001–010, all three seeders, 31 functional/32 physical tables, 53 FKs, the complete E012 Submission concurrency matrix, accumulated E009/E010/E011 regression and automatic cleanup. Independent `SHOW DATABASES LIKE 'antares_e012_submit_tmp%';` returned zero rows; no manual cleanup was required. PR #60 merged this validated baseline.

Phase 3 adds authenticated `GET /representative/enrollment/review` and CSRF-protected `POST /representative/enrollment/submit`, reuses the Phase 2 Review/Submit boundaries, renders current live information separately from annual Enrollment-owned information, shows safe actionable readiness, and uses explicit Submit plus PRG confirmation. Browser runtime approved autosave flush, failure/recovery, incomplete Billing, pending acknowledgements completed through E009, stale-context rejection, successful Submission, post-Submission live-data maintenance, annual read-only behavior, XSS escaping and sensitive-data boundaries. The ordinary no-JavaScript flow and valid/missing/invalid/reused CSRF matrix also passed. Fresh local validation passed with 505 optimized classes, 687 tests and 0 failures, all three HTTP/session scenarios and lint for all nine changed/new PHP files.

MariaDB was not rerun through the complete harness because exact comparison against merged PR #60 found zero relevant delta in Domain, Submission Application, Persistence, database core, migrations, seeders or the MariaDB harness. Its approved MariaDB 10.4.32 physical evidence is reused, not presented as a fresh execution. The independent synthetic browser fixture used only `antares_e012_p3_tmp%`; cleanup passed and the final database count was zero. PR #61 merged this validated Phase 3 baseline.

Phase 4 adds side-effect-free administrative Enrollment review plus explicit Reopen, Complete and Cancel Application services. Each mutation receives only EnrollmentId, executes exactly one Application transaction, locks only the authoritative Enrollment root, delegates the transition to the existing Aggregate, persists and verifies the complete reconstructed lifecycle, ownership and annual state. Reopen preserves prior SubmittedAt; Complete and Cancel use the server Clock; none revalidates Submission or changes live Person, Representative, Student, Family or acknowledgement data. The complete local suite passed with 516 optimized classes, 697 tests and 0 failures plus all three HTTP/session scenarios. A fresh disposable MariaDB 10.4.32 run passed migrations 001–010, all three seeders, 31 functional/32 physical tables, 53 FKs, all six same-root lifecycle races, the valid serialized Reopen → Cancel sequence, cross-root isolation, rollback, lock release, UTC, exact annual-state preservation and accumulated regression. Automatic cleanup passed and the independent `antares_e012_admin_tmp%` residual check returned zero databases. PR #62 merged this validated baseline.

Phase 5 adds the operational Submitted queue through a focused `SubmittedEnrollmentIdQuery`, PDO adapter and Application coordinators for the minimal list and complete administrative review context. The exact protected routes are `GET /enrollments`, `GET /enrollments/review`, `POST /enrollments/reopen`, `POST /enrollments/complete` and `POST /enrollments/cancel`. Delivery reuses Phase 4, accepts only EnrollmentId plus one-time CSRF, uses 303 PRG, handles stale transitions safely and distinguishes current live SIS information from annual Enrollment-owned information without inferring a submitter or snapshot. Local validation passed with 528 optimized classes, 709 tests and 0 failures plus all three HTTP/session scenarios. Authenticated browser runtime approved dashboard access, filtering and deterministic order, ACTIVE/INACTIVE period review, XSS/privacy, Reopen/Complete/Cancel, stale GET-to-POST, invalid CSRF and no-JavaScript forms; all synthetic database, session and server fixtures were removed. A fresh MariaDB 10.4.32 run passed migrations 001–010, all three seeders, 31 functional/32 physical tables, 53 FKs, the exact Submitted query across mixed states, ordering, ACTIVE/INACTIVE context, Phase 4 actions and accumulated regression. Automatic cleanup passed and independent checks returned zero `antares_e012_delivery_tmp%` databases; no manual cleanup was required. PR #63 merged this validated baseline.

Phase 6 performed a fresh integrated closure audit from PR #63. Composer validation passed, strict optimized development autoload generated 528 classes, all 709 tests and the three HTTP/session scenarios passed, PHP lint passed for all 630 tracked PHP files, the single JavaScript asset passed Node.js syntax validation and both diff checks passed. A fresh MariaDB 10.4.32 execution with prefix `antares_e012_closure_tmp` passed migrations 001–010, all three seeders, 31 functional/32 physical tables, 53 FKs, the complete Submission and administrative concurrency matrices, rollback, lock release, UTC, annual-state preservation and accumulated regression. Automatic cleanup and the independent residual query both returned zero databases; no manual cleanup was required.

Authenticated browser closure evidence approved the complete acknowledgement → Draft maintenance → Submission → administrative Reopen → annual and live-data edit → same-Enrollment Resubmission → Complete flow. It confirmed stable Enrollment identity, updated SubmittedAt, exact annual-state preservation, post-Submission/Completed annual read-only behavior, independently editable authorized live data, XSS escaping, stale administrative conflict handling, consumed-CSRF rejection and no console errors. The synthetic database, sessions, fixture, server, logs and tabs were removed, and the final `antares_e012_closure_tmp%` count was zero.

E013 — Basic Enrollment Reports is COMPLETED, FORMALLY VALIDATED AND MERGED. PR #65 merged Phase 1, PR #66 merged the locally and physically validated reporting read-model baseline, PR #67 merged Phase 3 Reporting Delivery & CSV, and PR #68 merged the Phase 4 closure into `main` at `d1e34dd02735051aea9115103801d24915f71447`. E013 has no remaining phase.

Final E013 validation passed Composer manifest and strict optimized-autoload checks, all 732 tests with 0 failures, the three official HTTP/session scenarios, lint for all 672 tracked PHP files, syntax validation for the single JavaScript asset and exact route inventories under LF and CRLF. Fresh real-HTTP sessions over an integrated disposable fixture approved anonymous/non-admin/admin authorization, ACTIVE default and explicit INACTIVE selection, all five HTML and CSV representations, current-live versus selected-period annual semantics, valid missing data, XSS escaping, formula-injection protection, privacy, `no-store`, safe 400/404 responses and no-JavaScript operation.

A fresh complete disposable MariaDB 10.4.32 run passed migrations 001–010, StatusTypeSeeder, StatusSeeder, AdminSeeder, 31 functional/32 physical tables, 53 FKs, every E013 reporting projection and the complete accumulated regression. Automatic harness cleanup passed and the independent `antares_e013_closure_tmp%` residual query returned zero databases. No migration 011, reporting schema, SubmissionSnapshot, `submitted_by`, write or lock was introduced.

IdentityAccess revalidates the exact server-side chain `Authenticated User → Person → Representative → active FamilyRepresentative → selected Family`. The protected Representative Portal now exposes that approved context without adding authority: zero authorized Families fail closed, one enters directly, and multiple require explicit CSRF-protected selection from the current authorized set. Session stores only a positive FamilyId proposal, never authority, and stale membership clears or replaces that context after revalidation.

The final E008 baseline passes with 312 optimized autoload classes, 480 tests with 0 failures, three HTTP/session scenarios and lint for all 388 tracked PHP files. The second audit confirmed that the dedicated Family Application boundary owns Representative context and resource authorization, Delivery retains only HTTP concerns, the exact route and session models remain intact, and the route inventory test is portable across LF and CRLF. The approved disposable MariaDB 10.4 Phase 5 evidence remains applicable because PR #35 and the closure audit change no Domain, Persistence, PDO adapter, schema, migration, seeder or physical harness.

E014 — UI/UX & Navigation Baseline is COMPLETED, FORMALLY VALIDATED AND MERGED. PR #69–#73 merged all five phases, with PR #73 merging Responsive, Accessibility & Validation Closure into `main` at `ec3df5292003bb4fddb44a374f0edcc986d0941c`. E014 has no remaining phase.

E015 — Bulk Import Baseline is COMPLETED, FORMALLY VALIDATED AND MERGED. PR #74–#80 merged Phases 1–7, and PR #81 merged the validated Phase 8 closure into `main` at `e2b7067d3b56fd227b8e4f1ba3586f988ab7a012`. The integrated closure audit found no product defect or functional, security, architecture, persistence or cleanup blocker. ADR-0030 removes `family_ref` in favor of Secretaría-supplied FamilyCode as the sole XLSX Family identity and freezes `F` + eight digits, migration 011/backfill, the exact three-sheet contract, OpenSpout 4.28, bounded file security, reupload/digest Preview/Apply, exact matching, per-Family transactions and no staging/history.

Phase 3 implements only the approved permanent FamilyCode boundary. Family owns an immutable required `FamilyCode`; Application outputs expose it; `FamilyRepository` and `PdoFamilyRepository` persist, reconstruct and resolve it exactly, including a transaction-required `findByCodeForUpdate()`. Existing manual Family creation receives candidates from the Application port and Infrastructure `random_int()` adapter with at most five attempts while the database UNIQUE remains final authority. Migration 011 performs the nullable staging column, range guard, deterministic FamilyId backfill, fail-closed validation, `CHAR(9) ascii_bin NOT NULL` target and `uq_families_family_code`, with rollback of the index then column.

Phase 3 local validation passed with 570 optimized autoload classes, 765 tests and 0 failures plus all three HTTP/session scenarios. Fresh disposable MariaDB 10.4.32 validation passed migrations 001–011, StatusTypeSeeder, StatusSeeder and AdminSeeder; 31 functional tables plus `migrations`; 53 FKs; fresh schema parity; legacy 001–010 Family data upgrade with IDs, FKs and memberships preserved; deterministic backfill; range rejection; exact case-sensitive lookup and row lock; physical UNIQUE mapping; complete Aggregate roundtrip; UTC; owned/outer rollback; Family Resources and accumulated regression. Automatic cleanup passed and the independent `antares_e015_phase3_tmp%` check found zero databases, so no manual drop was required.

Phase 4 implements the single reusable `CreateRepresentativeAccess` Application coordinator for Person + Representative + User and makes the existing administrative Representative/Family entrypoint delegate to it inside the one outer `CreateRepresentativeFamily` transaction. New Representative creation now requires complete identification, personal email, initial password and UserStatus; login derivation, uniqueness, credential policy, hashing and safe output remain owned by the existing `CreateRepresentativeUser`. Person generally may still exist without User, Student receives no User, the legacy User administration remains available and no historical backfill is performed. The form keeps the existing route, authorization, CSRF and PRG boundaries and never stores, re-renders or returns the submitted password.

Phase 4 local validation passed with 574 optimized autoload classes, 766 tests and 0 failures plus all three HTTP/session scenarios. The rollback matrix covers Person, Representative, User and Family/membership failures, existing Person User, occupied derived login, password policy, generated identities, hash verification, safe output and no orphans. Fresh disposable MariaDB 10.4.32 validation passed migrations 001–011, all three seeders, 31 functional/32 physical tables, 53 FKs, exact Person/Representative/User/Family commit, AUTO_INCREMENT identities, derived login, USER_STATUS, hashing, password rejection, duplicate-login rollback, post-User Family rollback, IdentityAccess synchronization, FamilyCode and the complete accumulated regression. Automatic cleanup passed; the independent `antares_e015_phase4_tmp%` residual check returned zero databases and no manual cleanup was required.

Phase 5 implements the bounded XLSX infrastructure without persistence or Delivery. Composer pins the reviewed MIT-licensed `openspout/openspout ~4.28.5`; the Infrastructure adapter accepts only a verified regular local XLSX path after a fail-closed ZIP/OOXML preflight, streams typed rows and preserves physical source numbering. The guard enforces the approved workbook, entry, decompression, sheet, row, cell and exact-header limits and rejects traversal, encryption, macros, embedded or external content, unsafe XML, formulas, merged cells and hidden structure. Application maps the three exact sheets to immutable typed rows, UTC dates and bounded safe validation issues while protecting plaintext Representative passwords from serialization, debugging, errors and output.

The static versioned template at `resources/templates/bulk-import/e015-family-import-v1.xlsx` contains exactly the three contract sheets and headers with no data, formula, hidden content, PII or password. Phase 5 local validation passed with 747 optimized autoload classes, 785 tests and 0 failures plus all three official HTTP/session scenarios. Dedicated coverage approves valid reordered/multi-Family workbooks, strict types/normalization/cross-sheet references/duplicates and the complete hostile-file and resource-limit matrix. Composer advisory review found no known security advisories. No route, HTTP, database, repository, migration, seeder, Preview, Apply, matching or persistent staging/history capability was added; MariaDB is not required because this phase performs no database access or physical change.

Phase 6 implements database-aware `PreviewBulkImport` and `ApplyBulkImport` through one shared matching authority. Preview is read-only and returns only bounded password-free safe classifications (`NEW`, `ALREADY_EXISTS`, `CONFLICT`) with masked documents. Apply reparses and revalidates the workbook, resolves active catalogs by stable code, processes FamilyCode in ascending order and commits each Family in one outer transaction after deterministic row locking and current-state revalidation. It creates only missing records, retains existing live data, supports legitimate Person multi-role reuse and never overwrites a User password.

Phase 6 local validation passes with 778 optimized autoload classes, 795 tests and 0 failures plus all three official HTTP/session scenarios. Coverage approves new and exact-reimport paths, inactive/incompatible conflicts, existing Person/Representative/User/Student combinations, shared Representatives, multi-role Persons, Family isolation, rollback without partial writes, safe error output and password clearing. Fresh disposable MariaDB 10.4.32 validation with prefix `antares_e015_phase6_tmp` passes migrations 001–011, StatusTypeSeeder, StatusSeeder, AdminSeeder, the exact 31-functional/32-physical-table inventory, 53 FKs, physical Preview read-only behavior, complete AUTO_INCREMENT/UTC creation, exact idempotent reimport, conflict rollback, password-hash retention and accumulated regression. Automatic cleanup passes and the independent residual check returns zero databases; no manual drop is required.

Phase 7 implements the minimal Spanish server-rendered Administrative Delivery behind AuthenticationMiddleware and the existing exact temporary `admin` gate. It exposes the fixed template download, bounded XLSX Preview, exact-byte reupload Apply, PRG result and formula-safe UTF-8 error CSV through 4 GET and 2 POST routes. Preview uses a cryptographically random, actor/session-bound, one-use 15-minute token containing only SHA-256 digest, issue/expiry times and safe counts; a new Preview invalidates the prior token and every valid-token Apply attempt consumes it. Uploaded files use random process-owned storage outside `public` and are deleted in `finally` on success, validation failure and exception. No matching rule, schema, migration 012, staging/history table, queue, external store or broader authorization model was added.

Phase 7 local validation passes with 805 tests and 0 failures plus all three official HTTP/session scenarios. Dedicated coverage approves exact 36 GET/71 POST inventory, anonymous and Representative denial, admin navigation, CSRF, template fidelity, bounded uploads, Preview read-only behavior, actor binding, token expiry/replay/invalidation, digest mismatch, exact reupload, PRG, safe Spanish/XSS output, CSV formula neutralization, no-JavaScript operation and temporary-file cleanup. Fresh disposable MariaDB 10.4.32 validation with prefix `antares_e015_phase7_tmp` passes migrations 001–011, all three seeders, the 31-functional/32-physical-table inventory, 53 FKs, real HTTP/session template/Preview/Apply/result/CSV, AUTO_INCREMENT, UTC, exact reimport, concurrent conflict without partial writes, rollback and accumulated regression. Automatic database cleanup passes and the independent residual check returns zero databases.

Phase 8 revalidated the complete XLSX → preflight → parse → canonical validation → password-free Preview → matching → actor/session-bound token and digest → exact-byte reupload → Family-atomic Apply → safe PRG result/CSV chain. Final local evidence passes Composer manifest and advisory checks, strict optimized autoload with 791 classes, all 808 tests with 0 failures, all three HTTP/session scenarios, lint for all 769 tracked PHP files and the exact 36 GET/71 POST inventory. The three permanent closure regressions additionally prove missing-CSRF no-write/no-retention behavior, one-use grant consumption and exact-file cleanup under concurrent Apply conflict, and escaped responsive accessible result output with a no-JavaScript functional contract.

Fresh disposable MariaDB 10.4.32 validation with prefix `antares_e015_phase8_tmp` passes migrations 001–011, StatusTypeSeeder, StatusSeeder, AdminSeeder, 31 functional/32 physical tables, 53 FKs, migration 011 legacy backfill/range/uniqueness/rollback, the complete E015 HTTP/session import flow, database-generated identities, UTC, idempotency, concurrency, rollback and the accumulated regression. Harness cleanup and the independent residual query both returned zero databases; no manual database cleanup was required. The graphical browser process could not start because the Codex Windows sandbox helper failed before opening a tab, so no visual-browser PASS is claimed; structural, accessibility, security and real HTTP/session evidence found no blocking presentation defect.

DEPLOY-001 — Manual Shared Hosting Installation Baseline is implemented and validated. PR #82 merged Phase 1 and its supported cPanel/File Manager or FTP/phpMyAdmin layout, production-safe effective environment contract, minimum production error hardening, local Composer preparation, tracked White Label asset directory and permanent manual installation baseline into `main` at `f19677d8cbabb72cefd4a72261c9d914801cdd14`.

Phase 2 implements the reusable test-only external-SQL loader, physical schema inspector and complete two-database installation rehearsal harness. The loader requires the institution-specific `install.sql` outside Git, rejects database/client commands, credential labels and the known plaintext admin password, and fingerprints the artifact. The harness preserves the existing explicit disposable-environment, safe-prefix, UEAnt and cleanup protections; derives the 001–011 ledger from the repository; compares the complete material schema of a migrations-built Base A with an install-only Base B; and validates system statuses, required catalogs, initial admin identity/authentication, White Label, Bulk Import/Enrollment readiness, approved empty data and zero residual databases.

The complete local validation passes Composer manifest/advisory checks, strict optimized autoload with 793 classes, all 820 tests with 0 failures, all three HTTP/session scenarios, lint for every Phase 2 PHP file and clean diff checks. The local XAMPP CLI had `zip` disabled, so validation used the allowed process-only `php -d extension=zip ...` override without changing global configuration.

The approved institutional catalogs, initial AcademicPeriod and administrator Person were represented only in a freshly generated external untracked `install.sql`; no credential, hash, SQL artifact or temporary branding asset was versioned. The final rehearsal passed on a clean isolated MariaDB 10.4.32 instance with its own disposable data directory and port after the existing XAMPP instance exposed corruption in its `mysql.db` privilege table. That local infrastructure fault was not treated as a product defect and the XAMPP data directory was not repaired or modified.

Fresh migrations 001–011 plus StatusTypeSeeder, StatusSeeder and AdminSeeder created Base A, while the autonomous external SQL import created Base B. Physical parity passed with 32 tables including `migrations`, 53 FKs and an exact 11-entry ledger. Real admin authentication and Dashboard access, White Label name/color/logo/favicon, Bulk Import and Enrollment readiness, deliberately empty catalogs, absence of demo data and the post-import no-pending-migrations check all passed. The runtime defect found before the physical rehearsal was corrected by keeping the Bulk Import validation date explicit and deterministic through the existing Clock boundary rather than constructor-injecting native `DateTimeImmutable`; future admission dates remain rejected and routing/container contracts remain unchanged.

The external artifact contained 54,923 bytes with SHA-256 `aae4dc7df9ca909bd350f7ccc2eed7de000d7f5218e5ee8820c3ba83e4d55bc6e`. Final cleanup left zero disposable databases, zero restricted ephemeral accounts and no temporary runtime, branding, SQL or secret material. PR #83 merged the completed and validated DEPLOY-001 baseline into `main` at `69b4a53ec523d290e1da8a596263071146e2fe10`.

MVP-VALIDATION-001 — MVP Validation & Closure is the current milestone. PR #84 merged Phase 1, PR #85 merged the technically accepted Phase 2 installation, and PR #86 merged the completed Phase 3 owner walkthrough into `main` at `df04a381f20c7e3246d5222419ad59ab12ce644a`. Phase 3 passed 14/14 blocks with no Category A or B finding; its register retains 14 Category C presentation findings, seven Category D future enhancements and two Category E new-capability/rule requests. Phase 4 implements the authorized C-001–C-013 corrections, including the Representative Portal redesign. The owner completed redesign review blocks 1–3, accepted the functional structure and requested the focused visual refinement now implemented; final acceptance remains pending and none is formally closed yet. C-014 remains separate and open. The MVP is not formally closed.

The persistent Phase 2 installation uses MariaDB 10.4.32 on the dedicated local review port, an owner-created database populated by manual phpMyAdmin import of the approved external untracked `install.sql`, an external production `.env`, `public/` as the only DocumentRoot and the CRUD-only `antares_mvp_app@127.0.0.1` runtime account. Direct post-import verification approved exact migrations 001–011, 32 physical tables including `migrations`, 53 FKs, complete structural parity with fresh migrations, approved and deliberately empty catalogs, the single administrator Person/User and PHP password hash, no demo/review data and no pending migration discrepancy.

Technical acceptance approved real administrator login, Dashboard, session persistence, CSRF rejection, logout, protected access, generic production errors, all required local CSS/JS/vendor assets, `Colegio Antares`, primary color `#0D6EFD`, safe missing logo/favicon behavior and the main administrative capability boundaries. The Representative Portal route remained reachable and failed closed for the administrator. The application operated with the four approved CRUD privileges and required no runtime DDL. Final regression passed with 793 optimized classes, 820 tests and 0 failures, all three official HTTP/session scenarios, lint for all 774 tracked PHP files and clean diff checks. No Phase 2 technical blocker was identified. The owner subsequently confirmed reachable English feedback during Phase 3 as one consolidated Category C finding; Phase 2 evidence is not a substitute for the now-completed walkthrough.

Phase 2 adds the single Spanish server-rendered shell, server-resolved Administrative/Representative navigation, exact active-state presentation, one CSRF-protected logout, shared feedback and validation components, the administrative Dashboard, basic Public/Auth presentation and trusted installation-level White Label configuration. Bootstrap 5.3.8 and Bootstrap Icons 1.13.1 are pinned and served locally with one application stylesheet and no frontend build pipeline or runtime CDN. `APP_NAME`, optional `APP_LOGO_PATH`/`APP_FAVICON_PATH`, `APP_PRIMARY_COLOR` and `APP_ASSET_VERSION` are validated presentation inputs with generic fail-closed defaults. Logo and favicon paths are accepted only when they resolve to existing regular files contained by the real public directory; missing and escaping assets never generate broken references.

Local validation passed with 563 optimized autoload classes, 744 tests and 0 failures plus all three HTTP/session scenarios. The exact local Bootstrap CSS/JS, Bootstrap Icons CSS/font files and application CSS all returned HTTP 200 with non-empty content. Automated rendering approved Spanish language metadata and Authentication CSRF feedback, escaped White Label values, physical logo/favicon fallback, role-neutral login, distinct admin/Representative navigation, prefix-aware active states, local assets, centralized full-document/logout ownership and the no-JavaScript navigation fallback. The graphical browser runtime was unavailable because the Codex Windows sandbox could not start its browser process; responsive evidence is therefore structural and is not presented as visual-browser validation. MariaDB was not executed because Phase 2 changes no Domain, Application business rule, Persistence, schema, migration, seeder or database configuration semantics.

Phase 3 modernizes the complete existing Administrative UI for Persons, Families, Representative Users, Family Resources, Institutional Acknowledgements, Enrollment administration and all five Enrollment reports. It adds shared breadcrumbs, empty states and status badges, responsive semantic forms and tables, Spanish visible presentation and clearer separation of current live SIS data, annual Enrollment data, configuration lifecycles and consequential actions. Raw Person/Family ID lookup, every existing authority gate, exact GET/POST route inventory, POST payload, CSRF behavior, no-JavaScript operation, report dataset and CSV contract remain unchanged.

Phase 3 local validation passed Composer manifest and strict optimized-autoload checks with 563 classes, all 749 tests with 0 failures, all three HTTP/session scenarios, lint for all 683 PHP files, Node.js syntax validation for the single existing JavaScript asset, exact 32 GET/69 POST inventory, clean diff checks and structural responsive/accessibility assertions. The graphical Codex browser process again could not start because the Windows sandbox helper failed before opening a page; no visual-browser evidence is claimed and integrated graphical validation remains for Phase 5. MariaDB was not executed because Phase 3 changes only Views, shared presentation CSS, presentation tests and status documentation, with no Domain, Application, Persistence, route, schema, migration, seeder or physical harness delta.

Phase 4 modernizes the existing Representative Portal, no-family/forbidden states, Institutional Acknowledgements, Enrollment maintenance, review/submission and Family Resources with Spanish visible presentation, Mobile First structure, shared components, responsive sections, status/empty states and clear consequential actions. Exact routes, POST payloads, CSRF, Authentication, Representative/Family authorization, active membership and AcademicPeriod revalidation, acknowledgements, Application validation and no-JavaScript operation remain unchanged. Authorized live Person/Representative/Student data remains independently editable while Enrollment-owned annual Billing, Medical, Transport and leave-alone data follows `enrollmentDraftMaintenanceEnabled`; AcademicPlacement remains read-only.

Phase 4 local validation passed Composer manifest and strict optimized-autoload checks with 563 classes, the complete 754-test suite with 0 failures, all three HTTP/session scenarios, lint for all 684 tracked/new PHP files and Node.js 24.19.0 syntax validation. Dedicated E014 coverage freezes selection, safe links, consequential POSTs, separate live/annual ownership, all existing autosave contracts, Family Resource identities/endpoints, local assets and the exact 32 GET/69 POST inventory. The temporary local server returned HTTP 200, but the graphical browser process could not start because the Windows sandbox helper exited before opening a page; no visual-browser evidence is claimed. MariaDB was not executed because Phase 4 changes only Views, presentation CSS/JavaScript text, presentation tests and status documentation, with no Domain, Application, Persistence, PDO, route, schema, migration, seeder or harness delta.

Phase 5 completed the integrated Public/Auth, Administrative, Representative, shared-shell, responsive, accessibility, Spanish, White Label, navigation, security/XSS, no-JavaScript, autosave, reports and contract audit. It corrected three presentation defects only: arbitrary valid `APP_PRIMARY_COLOR` values can no longer destroy critical text contrast on custom surfaces, custom resource buttons now expose explicit focus and disabled presentation, and AcademicPeriod status in the administrative selector is shown as `Activo`/`Inactivo` instead of the internal code. The route inventory remains exactly 32 GET and 69 POST.

Final local validation passed with 563 optimized classes, 760 tests and 0 failures, all three HTTP/session scenarios, lint for all 685 tracked PHP files and Node.js 24.19.0 syntax validation. The real temporary HTTP runtime served Login, Forgot Password, application CSS, Bootstrap CSS/JS and Bootstrap Icons CSS/font assets with HTTP 200 and was stopped with zero listener remaining. The in-app graphical browser connection terminated before opening a page on both the normal and diagnostic attempts; therefore no visual-browser, keyboard-only browser or 200% zoom PASS is claimed. This environmental limitation is non-blocking because exhaustive source/DOM-contract checks cover the exact active Views, responsive breakpoints and reflow rules, labels, headings, semantic tables, focus, overflow, no-JavaScript fallbacks and local assets, while the accumulated Delivery and HTTP/session suite exercises authenticated admin/Representative, success, error, empty, read-only, XSS and lifecycle paths. No material unresolved layout question remains.

MariaDB was not executed for Phase 5. Exact scope contains no Domain, Application, Persistence, PDO, route, schema, migration, seeder, database core or physical-harness change, so the previously approved physical evidence remains applicable.

## Current Branch

`feature/mvp-validation-001-phase4`

## Current Milestone

MVP-VALIDATION-001 — MVP Validation & Closure — IN PROGRESS.

## Current Phase

Phase 4 — Shared Presentation Corrections and Representative Portal UX Redesign — the final structural visual review approved the redesign and its four residual presentation refinements are implemented and locally validated. Runtime verification of those refinements and the separate C-014 correction remain pending. Phase 3 was merged through PR #86. Phase 4 and the MVP remain open.

TEST-001 — MariaDB Integration Test Infrastructure remains a separate, independent technical milestone.

## Current Objective

Deploy the four locally validated residual Portal refinements to the persistent review installation and obtain owner verification, then address the separately scoped C-014 correction. C-012 was manually validated with successful valid Billing save and Spanish required-phone feedback. Do not declare formal MVP closure; Category D/E work remains excluded.

## Current Risks

- The destructive authorization is valid only while the target remains pre-production and disposable; it must not be applied to an institutional or production database containing real data.
- The clean migration sequence is destructive by design under ADR-0018 and must never be pointed at an institutional or production database.
- The current post-migration-011 inventory remains 31 functional tables plus `migrations` and 53 FKs. Documented-column parity and the exact FamilyCode schema, legacy backfill, lookup, uniqueness, range protection and rollback pass locally and on the explicitly authorized disposable MariaDB environment.
- The approved initial institutional catalog values are now recorded in the deployment baseline; catalog stewardship remains a data-master concern, not a schema decision. Person forms continue to fail closed when required active sex and GENERAL_STATUS options are unavailable.
- The persistent clean local installation remains available. Phase 3 passed and PR #86 merged. The final structural visual review approved the Phase 4 Portal redesign and narrowed its remaining presentation work to four implemented residual refinements, but Category C findings remain formally open until final owner acceptance; C-014 remains separate. Formal MVP closure still requires runtime verification, the C-014 disposition and explicit acceptance under `19.MVP_VALIDATION_BASELINE.md`.
- Phase 4 translates application-owned feedback. Browser-native constraint messages may still follow the browser/OS locale on required Person and Enrollment controls; no global `novalidate` or validation rewrite was introduced. C-012 now keeps invalid required Billing data dirty, exposes accessible feedback and retries after validity is restored.
- Apparently unreachable legacy UI/controller elements remain non-blocking cleanup unless runtime acceptance demonstrates reachability or impact.
- The temporary exact `admin` gate is intentionally not the definitive institutional role model; broader authorization still requires a separately approved milestone.
- The zero-or-one ACTIVE AcademicPeriod invariant is transactional because the current schema has no clean portable declarative constraint based on a referenced status code. Approved Application transitions serialize on GENERAL_STATUS/ACTIVE and `findActive()` rejects multiplicity, but direct external writes that bypass this boundary remain detectable invalid state.
- ADR-0024 accepts for the MVP the rare administrative deadlock risk between concurrent AcademicPeriod lifecycle and ACTIVE Requirement creation. InnoDB may roll back one transaction, which the administrator can repeat. The risk is known and accepted, not resolved; it does not relax acknowledgement, authorization or atomicity invariants and must be reconsidered if observed operationally or if administrative concurrency increases.
- No direct Representative–Student association exists or is planned. Family is the grouping unit, with Representative membership owned by FamilyRepresentative and Student membership owned by FamilyStudent.

## Pending Decisions

E009 has no pending functional or architectural decision. ADR-0022 defines Representative Institutional Acknowledgements, ADR-0023 defines the server-resolved Active AcademicPeriod context and ADR-0024 records the accepted non-blocking administrative deadlock risk.

Enrollment Domain Discovery remains complete with `Open Business Decisions: None`. E010 and the Active Family Capture Serialization correction introduced no new business or architectural decision: they apply the approved Enrollment Aggregate, lifecycle, annual ownership and migrations 008/009 baseline. E011 introduces no new business or architectural decision and deliberately does not select a PreviousEnrollment or preload historical annual state because no unequivocal selection rule exists. Representative initiation is authorized only through the server-resolved E011 context; final completeness and Submission remain E012 scope.

E013 Phases 1–4 have no unresolved functional or physical-design decision. ADR-0026 is implemented through report-specific read-only projections and one Delivery representation boundary; final local, fresh MariaDB 10.4.32 and integrated HTTP/session evidence confirms the five datasets are produced and delivered from the existing schema without migration 011.

E014 Phase 1 has no unresolved functional decision. The MVP user-visible UI language is Spanish; internal technical identifiers and language-independent catalog codes remain in English, no MVP i18n or language selector is authorized, and future internationalization requires a separate milestone. The Representative initiates Enrollment; Secretaría retains only review, reopen, complete and cancel.

E015 Phases 1–8 have no unresolved functional, technical-design, FamilyCode persistence, Representative/User coordination, XLSX-infrastructure, Preview/Apply Application, Administrative Delivery or closure decision. ADR-0028 is implemented by the shared transactional creation authority and ADR-0030 freezes FamilyCode-only import identity, migration 011/backfill, exact XLSX contract, OpenSpout constraint, limits, Preview/Apply integrity, password lifecycle, comparison, per-Family transaction and no staging/history. Phase 8 formally validates the complete integrated boundary and PR #81 merged it; E015 has no remaining phase.

MVP-VALIDATION-001 introduces no new business or architectural decision. It defines the approved integrated validation, owner acceptance, defect-triage and formal closure procedure in `19.MVP_VALIDATION_BASELINE.md`.

The temporary `admin` gate protects the implemented delivery boundary and does not define the definitive role model. Authorization for Secretaría, DECE and Enfermería remains pending a separately approved milestone outside E007.

## Documentation Status

| Document | Status |
|---|---|
| Project Context | Current |
| Enrollment Domain Discovery | Closed; E014 Enrollment-initiation wording aligned |
| Architecture | Technically approved |
| Domain Model | Technically approved |
| Decisions | Current; ADR-0019 through ADR-0030 approved |
| Catalogs | Technically approved; product-level governance explicitly separated from installation-specific initial values |
| UI/UX Baseline | E014 Phases 1–5 formally validated and merged through PR #73; current 43-view, 36-GET and 71-POST inventory synchronized |
| Bulk Import Baseline | E015 Phases 1–8 completed, formally validated and merged through PR #81 |
| Deployment Baseline | DEPLOY-001 completed, validated and merged through PR #83 |
| MVP Validation Baseline | MVP-VALIDATION-001 Phases 1–2 merged; Phase 3 walkthrough completed with 14/14 blocks, A/B 0, C 14, D 7 and E 2; formal closure pending |
| Database Design | Current migration-011 baseline with implemented and physically validated FamilyCode target |
| ERD | Current schema including implemented migration-011 FamilyCode target |
| AUDIT-001 | Completed and technically approved |
| Current Status | Current |
| Roadmap | Current |
| Development Log | Current |

## Repository Status

- PR #3 was merged into `main` at commit `60a444d`.
- PR #7 merged Person Persistence into `main` at commit `1c936ce` after local and disposable MariaDB 10.4 validation.
- PR #8 merged Person Application into `main` at commit `9587d68`.
- PR #9 merged Person Delivery into `main` at commit `b95876e`.
- PR #10 merged the E006 Representative and Student documentation baseline into `main` at commit `f004940`.
- PR #11 merged Representative Domain into `main` at commit `21e38a8`.
- PR #12 merged Student Domain into `main` at commit `4e797f3`.
- PR #13 merged Representative and Student Persistence into `main` at commit `c960645` after disposable MariaDB 10.4 validation.
- PR #14 merged Representative and Student Application into `main` at commit `c687aa2`.
- PR #15 merged the E006.5 Family Membership documentation baseline into `main` at commit `003c42c`.
- PR #16 merged Family Membership Domain into `main` at commit `9bf7a5c`.
- PR #17 merged the locally and physically validated Family Membership Persistence baseline into `main` at commit `8e659b5`.
- PR #18 merged Family Membership Application into `main` at commit `82accdd`.
- PR #19 merged the locally and physically validated Composite Administrative Orchestration into `main` at commit `f8fedec`.
- PR #20 merged the locally and physically validated Administrator Delivery Integration into `main` at commit `c7a4fe191c9bae215691a4777304c7085e2cb7e6`.
- PR #21 merged the E006/E006.5 Validation and Closure documentation into `main` at commit `94f87fba2ffbbc9a011aa9b0acd938f8ed24abb3`.
- PR #22 merged E007 Phase 1 Documentation and Functional Alignment into `main` at commit `0f04952e5758cb1935efe456608869bfd1fc9433`.
- PR #23 merged the locally and physically validated E007 Phase 2 Representative User Provisioning & Administrative Credential Management into `main` at commit `878ddf9e481fc8f0a7ed47cf86b5d0731ffc9614`.
- PR #24 merged the locally and physically validated E007 Phase 3 Representative Access Resolution into `main` at commit `04ebf87da316bf2a0afe7d3da292151f8172a7de`.
- PR #25 merged the locally and physically validated E007 Phase 4 Family Context Authorization into `main` at commit `7564bc1b870c17b81f63cb5f26ef37c55704c1b9`.
- PR #26 merged E007 Phase 5 Representative Portal Entry into `main` at commit `118747114e6534c0353d7cbde1ea951cd0b6b3f1`.
- PR #27 merged E007 Phase 6 Forgot Password Assistance & Representative Email Invariant into `main` at commit `23e32006af77c5649f36cabfd942ea44efde1193`.
- PR #28 merged E007 Phase 7 Validation and Closure into `main` at commit `0b6fdab202142237d83d30e72f8c9938685ab6da`.
- PR #29 merged E008 Phase 1 Documentation and Functional Alignment into `main` at commit `57a62415a6d292c87264617b20e1bf25be100bf9`.
- PR #30 merged E008 Phase 2 Family Resources Domain into `main` at commit `19cdfd0781ba0dc9b8db6de1df9e8a780963befb`.
- PR #31 merged E008 Phase 3 Family Resources Persistence into `main` at commit `913b2c25418ce91be69acb54a62bfd379dff57d4`.
- PR #32 merged E008 Phase 4 Family Resources Application into `main` at commit `57aa645acfd1197828e2658f4aa4a39afdd0cd50`.
- PR #33 merged the locally and physically validated E008 Phase 5 Administrator Delivery into `main` at commit `7f38ffb5da5a7e7716bad63b5bec5cd0af6446a9`.
- PR #34 merged E008 Phase 6 Representative Portal Integration into `main` at commit `166788e037fc96f8f336c980142d622ae2a7aee1`.
- PR #35 merged E008 Phase 6.1 Representative Portal Authorization Correction into `main` at commit `57cc6cfdd8a3e99b2bcdcddd0b7ffd3f4fc1da20`.
- PR #36 merged E008 Phase 7 Validation and Closure into `main` at commit `d128f7dd3dbcf25ba0833af934fe743927143a55`.
- PR #37 merged the locally and physically validated E009 Phase 1 Documentation and Functional Alignment into `main` at commit `d1a1091603a2eac5fcb4279fb32456959c20c93e`.
- PR #38 merged the locally validated E009 Phase 2 Domain into `main` at commit `3d6ceaf4359a5d872f1dfc7d8d26c2186390d3cd`.
- PR #39 merged the locally and physically validated E009 Phase 3 Persistence into `main` at commit `f777967b16488dd6eeb656459996e752d6610c61`.
- PR #40 merged E009 Phase 4 Application into `main` at commit `b3e065bad22c1ec69f746dd3b173ab01b274b89f`.
- PR #41 merged E009 Phase 5 Administrator Delivery into `main` at commit `7de0b7f26f6aadf2c8488e8d7e1c51dbb5d7ad26`.
- PR #42 merged E009 Phase 5.1 Active AcademicPeriod Baseline into `main` at commit `291ced50279060d69bd96fa7289b5469c6d2841f`.
- PR #43 merged E009 Phase 6 Representative Portal Integration into `main` at commit `d06250c83d7408a14c2962211579adcb42ad7efc`.
- PR #44 merged the locally and physically validated E009 Requirement Post-use Concurrency Serialization correction into `main` at commit `d97dd48cd2b3a1e03854c872ac802f9170fca938`.
- PR #45 merged the locally and physically validated E009 Requirement Creation/Completion Serialization correction into `main` at commit `5921c62cdd1eb771ac544c3bdd31944c60fb0b46`.
- PR #46 merged the final E009 Validation and Closure baseline into `main` at commit `05b41f7482fae68bcb3d812f3962aef5424a0468`.
- PR #47 merged the E010 Phase 1 Documentation and Scope Baseline into `main` at commit `b45581f96bcc9945c2fa282c79704f1f16952a8d`.
- PR #48 merged the locally validated E010 Phase 2 Enrollment Domain into `main` at commit `612c76ef8c190f1a0ad32e81123cff286126a835`.
- PR #49 merged the locally and physically validated E010 Phase 3 Enrollment Persistence into `main` at commit `14eb7a3289aadbd782737341e2876313822cf09b`.
- PR #50 merged the locally and physically validated E010 Phase 4 Enrollment Application into `main` at commit `2f9d9fbafa4f4143ef662f28d9326250ef27f2b0`.
- PR #51 merged the locally and physically validated E010 Active Family Capture Serialization correction into `main` at commit `f28f1630d7207528df1875f63fef3bdd658663b3`.
- PR #52 merged the E010 Validation and Closure baseline into `main` at commit `22d8c9c69beef8b9581495d56760272eb9e7cf1a`.
- PR #53 merged the E011 Phase 1 Documentation and Scope Baseline into `main` at commit `5b8c2e1d4606b417bfaab5ec5d6b3f85a5472b8e`.
- PR #54 merged the locally and physically validated E011 Phase 2 Application Coordination into `main` at commit `ff198ed3b10f78ee771409a5a54ce96fbaedd7f2`.
- PR #55 merged the locally validated E011 Phase 3 Representative Enrollment Delivery into `main` at commit `0e75b9ab276a7b38b32132d93e820124767e33f9`.
- PR #56 merged the locally validated E011 Phase 4 Autosave and Navigation UX into `main` at commit `7a7e115a470863b33ee37fb88d7b68a0cc3b7a91`.
- PR #57 merged the formally validated E011 Phase 5 closure baseline into `main` at commit `1454e2c7acea66e7db29a606f1227082e91369b8`.
- PR #64 merged the formally validated E012 Phase 6 closure baseline into `main` at commit `82df0459c5948e94ae4316d5b803dfd3ccf886a8`.
- PR #73 merged the formally validated E014 Phase 5 closure into `main` at commit `ec3df5292003bb4fddb44a374f0edcc986d0941c`.
- PR #74 merged E015 Phase 1 Discovery & Scope Baseline into `main` at commit `d65a63fb4999f3e9f47ca73a8728700c2f785457`.
- PR #75 merged E015 Phase 2 Bulk Import Technical Design into `main` at commit `ad9da7579c86dfdf18ed3a54c8a6a5c0f904e8b4`.
- PR #76 merged the locally and physically validated E015 Phase 3 FamilyCode Domain and Persistence into `main` at commit `fe12ec38ca4d76cfccfc4956ed457480ba0e3ceb`.
- PR #77 merged the locally and physically validated E015 Phase 4 Mandatory Representative/User Creation Coordination into `main` at commit `7963906511a2b3427a580c0201fbb8b0881a68f0`.
- PR #78 merged the locally validated E015 Phase 5 XLSX Infrastructure, Template and Validation Model into `main` at commit `0cd701e5117f3ffa3deff049bb6414d8760ecab2`.
- PR #79 merged the locally and physically validated E015 Phase 6 Preview, Matching and Apply Application into `main` at commit `2d73fbe3ec3d77aa4aec340e26ff15861f2c8250`.
- PR #80 merged the locally and physically validated E015 Phase 7 Administrative Delivery into `main` at commit `e13ca761ee70c000bc35c58e5813055c9debf4c6`.
- PR #81 merged the formally validated E015 Phase 8 closure into `main` at commit `e2b7067d3b56fd227b8e4f1ba3586f988ab7a012`.
- PR #82 merged DEPLOY-001 Phase 1 Manual Installation Baseline & Production Hardening into `main` at commit `f19677d8cbabb72cefd4a72261c9d914801cdd14`.
- PR #83 merged the completed and validated DEPLOY-001 Phase 2 and formal milestone baseline into `main` at commit `69b4a53ec523d290e1da8a596263071146e2fe10`.
- E004.1 remains functionally unchanged.
- IdentityAccess persistence and its tests remain aligned with the official `users` columns.
- User now represents both a new instance without database identity and a reconstructed instance with stable UserId. PersonId remains immutable, while explicit Domain behavior changes LoginIdentifier or PasswordHash without changing status or authentication state. Authentication rejects an unpersisted User explicitly.
- UserRepository now includes lookup by IdentityAccess PersonId and returns the persisted Aggregate from save. PdoUserRepository inserts without an ID, uses database AUTO_INCREMENT and lastInsertId, reloads the complete Aggregate, persists every approved mutable field on update, preserves PersonId and participates in an outer transaction without opening one internally.
- PasswordHasher now supports secure generation and verification. NativePasswordHasher delegates generation to `password_hash` with `PASSWORD_DEFAULT`; the reusable Representative Application policy enforces only the approved five-character minimum.
- CreateRepresentativeUser verifies Representative and Person, requires complete Identification, derives the globally unique login exclusively from DocumentNumber, hashes the supplied password and returns only safe administrative output. ChangeRepresentativeUserPassword replaces the hash without altering identity, login, status, failed attempts, lock or last access.
- UpdatePersonWithRepresentativeUserSync reuses UpdatePerson inside one TransactionRunner boundary. A Person without a Representative User keeps optional Identification behavior; a Person with a Representative User must retain complete Identification, and a changed DocumentNumber atomically updates LoginIdentifier while preserving User identity, credentials and authentication state.
- The existing Person edit Delivery now uses the synchronization orchestrator and maps known Identification/login conflicts to safe HTTP 422 responses. Three exact admin-protected Representative User routes provide manage, create and administrative password replacement with CSRF and a server-side trusted RepresentativeId. Password fields are never restored after errors.
- The approved physical schema already supports E007 without change: UserId is AUTO_INCREMENT, PersonId and both login fields exist, and physical UNIQUE constraints protect one User per Person plus global normalized login uniqueness.
- Migrations 002–009 historically defined the clean 35-table functional baseline; migration 010 now removes the four replaced snapshot tables, while migration 001 and the runner-owned `migrations` table remain technical metadata.
- ADR-0021 simplifies `family_addresses` to Family, label, required MainStreet, optional text components, optional valid coordinate pair, GENERAL_STATUS and timestamps. Province, Canton and Parish remain in the baseline but are no longer referenced by FamilyAddress.
- Under the former baseline, `snapshot_addresses` mirrored only values that exist in FamilyAddress and contained no Province, Canton or Parish codes or names. Migration 010 now removes that obsolete table without changing FamilyAddress.
- Schema parity tests and the disposable MariaDB harness continue to verify the exact simplified FamilyAddress columns, absence of geographic FKs/indexes and preservation of coordinate checks within the current 31-table baseline.
- On 2026-08-10, the authorized disposable MariaDB 10.4 validation passed with prefix `antares_e008_phase1_tmp`: migrations 001–009, all three seeders, the complete harness and accumulated physical regression completed successfully.
- Physical inspection approved the exact ADR-0021 FamilyAddress columns, absence of ProvinceId/CantonId/ParishId and their retired indexes/FKs, preservation of Family/Status relationships and all coordinate-pair/range checks. The later ADR-0025 baseline removes SubmittedAddressSnapshot entirely.
- Province, Canton and Parish remain present with their internal hierarchy and are only decoupled from FamilyAddress. The current post-migration-010 baseline contains 31 functional tables plus migration metadata and exactly 53 FKs.
- Partial cleanup and final manual cleanup passed. `SHOW DATABASES LIKE 'antares_e008_phase1_tmp%';` returned no rows, so no E008 Phase 1 disposable database remains.
- Family remains the sole Aggregate Root for Family Resources. Phase 2 adds FamilyAddress, RepresentativeAddressAssignment, StudentAddressAssignment, FamilyEmergencyContact, EmergencyContactAssignment, FamilyAuthorizedPickup and AuthorizedPickupAssignment with typed nullable identities, resource lifecycle and explicit temporal history.
- ADR-0021 is preserved in Domain: Address requires only MainStreet, keeps the four approved optional text components, supports an optional exact Geolocation pair and introduces no Province, Canton or Parish dependency.
- Family reconstruction now validates all nine internal collections, known resource/member references, active-membership and resource requirements, address uniqueness, emergency contact/priority uniqueness, pickup uniqueness, temporal coherence and duplicate persisted identities while preserving valid ended history and collection encapsulation.
- Phase 2 local validation passed with 238 optimized autoload classes, 427 tests with 0 failures, three HTTP/session scenarios, complete modified-PHP lint and `git diff --check`. No Persistence, Application, Delivery, repository contract, migration, schema, seeder or physical harness changed, so MariaDB was not executed.
- Phase 3 extends only `PdoFamilyRepository` and its tests/harness: the unchanged FamilyRepository contract reconstructs and synchronizes FamilyAddress, both address assignments, FamilyEmergencyContact and assignment, and FamilyAuthorizedPickup and assignment as one complete Aggregate.
- Every new row omits `id`, uses database AUTO_INCREMENT and a positive `lastInsertId()`. Persisted identities are indexed and validated against their Family; persisted history cannot be omitted, resources are updated without deletion, and assignment identity/resource/member/start/priority remain immutable while ended history permits only the approved transition.
- SQLite coverage includes the seven physical tables, composite ownership, active uniqueness, checks, strict UTC seconds, UTF-8/apostrophes, exact Geolocation, GENERAL_STATUS, identical writes, missing/cross-Family identities and owned/outer transaction rollback. Local validation passes with 238 optimized classes, 437 tests, 0 failures and three HTTP/session scenarios.
- The disposable MariaDB 10.4 execution approved migrations 001–009, StatusTypeSeeder, StatusSeeder, AdminSeeder, the exact `PASS MySQL Family Resources complete roundtrip AUTO_INCREMENT UTC constraints and rollback` scenario and the complete accumulated physical regression.
- Physical validation confirmed complete Family Resources persistence/reconstitution, database-managed AUTO_INCREMENT identities, exact UTC seconds, generated uniqueness, composite same-Family FKs, checks, owned rollback and outer rollback.
- Partial cleanup and final manual cleanup passed. `SHOW DATABASES LIKE 'antares_e008_phase3_tmp%';` returned no rows, confirming that no E008 Phase 3 temporary database remains.
- Phase 4 adds an immutable complete Family Resources query plus explicit create/update/activate/deactivate and assign/end cases for addresses, emergency contacts and authorized pickups. Every successful command invokes one Aggregate operation, performs exactly one `FamilyRepository::save()` and verifies the returned Family identity, child identity and requested persisted state without relying on array order or generated ID sequences.
- RelationshipType continues through its existing minimal active-reference lookup. Phase 5 supplies the productive prepared `PdoDocumentTypeLookup` and active, name/id-ordered RelationshipType/DocumentType form options through the shared ConnectionManager; inactive, unknown and non-positive references fail closed.
- All timestamps are supplied as explicit `DateTimeImmutable` inputs. Application reads no clock, session, authenticated identity or HTTP request and adds no authorization rule; existing Domain exceptions continue to enforce membership, same-Family ownership, lifecycle, priority, replacement and identification-pair invariants.
- Phase 5 exposes one GET and 20 explicit POST routes behind AuthenticationMiddleware then the existing temporary FamilyAdministrationMiddleware gate. Browser FamilyId is only a tamper check; POST authority is the positive FamilyId pulled from `_family_resources_trusted_family_id`, and every child/resource selection is revalidated from current Application output.
- The Family Resources view renders all resources and active/ended assignment history, escapes output, uses CSRF on every mutation, accepts explicit timestamps, provides only the approved address/contact/pickup fields and introduces no Enrollment, Submission, geographic selector, Representative Portal route, hard delete, AJAX or new role model.
- Local Phase 5 validation passed with 297 optimized autoload classes, 456 tests with 0 failures and three HTTP/session scenarios. Composer validation, complete modified-PHP lint and `git diff --check` passed.
- The authorized disposable MariaDB 10.4 run with prefix `antares_e008_phase5_tmp` executed migrations 001–009, StatusTypeSeeder, StatusSeeder and AdminSeeder, then approved the accumulated physical regression and the exact `PASS MySQL Family Resources complete roundtrip AUTO_INCREMENT UTC constraints and rollback` and `PASS MySQL Family delivery active catalogs and Application persistence` scenarios.
- Physical validation confirmed the productive DocumentType lookup behavior covered for active, inactive and unknown values; active RelationshipType/DocumentType option loading in defined order; real Administrator Delivery/Application integration with `PdoFamilyRepository`; complete Family Resources creation, persistence and reconstruction; database-managed AUTO_INCREMENT; UTC; physical constraints; and rollback.
- The harness reported `PASS MySQL partial disposable database creation cleanup`. One residual temporary database from an identity scenario was removed manually; the final `SHOW DATABASES LIKE 'antares_e008_phase5_tmp%';` returned no rows, so Phase 5 physical cleanup is complete.
- Phase 6 added exactly one GET and 20 explicit POST routes under `/representative/resources`; PR #34 merged that integration without changing the existing `/families/resources` administrator boundary.
- The first Phase 7 audit rejected formal closure because the Portal Controller performed significant contextual authorization and its route inventory test mixed normalized and original line endings, producing 467 tests with one failure on Windows.
- Phase 6.1 moves actor/context/resource authorization into `Family/Application/RepresentativeResources`: every command resolves E007 context, compares the browser FamilyId only as `expectedFamilyId`, derives self Representative, authorizes active related Students and child resources, and delegates to the existing Phase 4 case.
- The authorized read model contains only the selected Family display, permitted Family resources, active Student options, own Representative Address history and histories for authorized Students. The Controller and View receive no unrestricted memberships or other-Representative history.
- Address update/deactivation protection for active use by another Representative now belongs to Application and returns the same non-enumerating message. CSRF and structural HTTP validation remain in Delivery; no new session authority was introduced.
- The route test normalizes CRLF once immediately after reading and verifies equivalent LF/CRLF input while preserving the exact one-GET/twenty-POST inventory and middleware assertions.
- Local Phase 6.1 validation passes with 312 optimized autoload classes, 480 tests with 0 failures and three HTTP/session scenarios. Domain, Persistence, PDO adapters, schema, migrations, seeders and the MariaDB harness are unchanged, so the approved Phase 5 physical evidence remains applicable.
- The second Phase 7 audit started from synchronized `main` at the PR #35 merge and independently revalidated Domain, Persistence, all 21 Application cases, both Delivery boundaries, E007 context/session integration, security, White Label, schema parity and exclusions. The two first-audit blockers are resolved and no new functional, security, architecture or persistence blocker remains.
- E008 closure validation passed with a valid Composer manifest, 312 optimized classes, 480 tests with 0 failures, three HTTP/session scenarios, all 388 tracked PHP files linted without failure and clean diff checks. Its approved physical evidence covered the then-current 36-table/60-FK baseline.
- The approved Phase 5 MariaDB 10.4 evidence was reused after exact Git comparison confirmed no subsequent change to Family Domain/Persistence, database migrations/seeders, database core or the MariaDB harness. That evidence includes migrations 001–009, all seeders, accumulated regression, Family Resources roundtrip and Delivery/Application integration, AUTO_INCREMENT, UTC, constraints, rollback, partial cleanup and final cleanup with no `antares_e008_phase5_tmp%` database remaining.
- ADR-0022 replaces SIS-managed documents, versions and Enrollment-owned DocumentAcceptance with `AcknowledgementRequirement`, `RepresentativeAcknowledgementCompletion` and its internal `RepresentativeAcknowledgement` set. Zero requirements is valid; Completion is unique by Representative + AcademicPeriod and remains valid after later configuration changes.
- ADR-0018 remains applicable because no first environment with real data has been declared and every recorded MariaDB execution is disposable. Migrations 007/008 create the Institutional Acknowledgements baseline directly; the current sequence is 001–010 after the E012 corrective migration.
- The active physical baseline contains 31 functional tables and 53 FKs. Old institutional document/version/acceptance tables, storage/version columns and replaced Enrollment snapshot tables are absent; Enrollment has no acknowledgement table or FK.
- Phase 1 local validation passes with 312 optimized classes, 481 tests with 0 failures, three HTTP/session scenarios and lint for the modified harness. Schema tests enforce exact tables, columns, types, nullability, keys, same-period ownership, zero old schema and unchanged migration sequence.
- Two harness-only defects found by the first physical attempts were corrected without changing schema: E009 table/FK sets are normalized with `SORT_STRING`, and the zero-requirements native PDO query uses two distinct named placeholders. The complete rerun passed on disposable MariaDB 10.4.32 after migrations 001–009, StatusTypeSeeder, StatusSeeder and AdminSeeder executed. It approved the exact 35-table functional inventory plus `migrations`, the physically observed 58 FKs, all three acknowledgement tables, absence of the retired model, AUTO_INCREMENT, UTC, GENERAL_STATUS, text checks/nullability, zero requirements, both uniqueness rules, same-period composite ownership, rollback and the accumulated regression.
- Automatic and manual final verification confirmed no `antares_e009_phase1_tmp%` database remained; no manual drop was required. No `.env`, UEAnt, institutional or persistent database was used.
- Phase 2 adds only `App\InstitutionalDocuments\Domain`: `AcknowledgementRequirement` and `RepresentativeAcknowledgementCompletion` as separate Aggregate Roots, immutable `RepresentativeAcknowledgement` children, the context-specific ACTIVE/INACTIVE status, one Domain exception and eight small Value Objects. No Repository, PDO adapter, Application case, Controller, route, view, UI, document storage, Enrollment/Submission implementation or institutional seed data was added.
- Requirement Title/URL/OfficialReference enforce only documented normalization, presence and length rules. AcademicPeriod and persisted identities remain immutable; status remains independently mutable, while post-acknowledgement updates reject Title or OfficialReference changes before any mutation and continue to allow URL changes.
- Completion creation requires a non-empty set of persisted ACTIVE Requirements from the same AcademicPeriod, rejects duplicate Requirement identities and creates new nullable-identity children internally using the explicit `DateTimeImmutable` supplied by the caller. Historical reconstitution requires persisted child identities and rejects duplicate child or Requirement identities without loading current Requirements or requiring current ACTIVE status.
- Phase 2 local validation passes with 325 optimized classes, 501 tests with 0 failures, three HTTP/session scenarios, lint for all new/modified PHP files and clean diff checks. MariaDB was not executed because Phase 2 changes no Persistence, schema, migration, seeder or physical harness.
- Phase 3 adds exactly two repository contracts and two PDO adapters. Requirement persistence supports generated identity, lookup, AcademicPeriod history, exact GENERAL_STATUS reconstruction, post-use detection and approved mutable-field updates; Completion persistence atomically inserts and reconstructs the immutable root with all historical children.
- Every insert omits physical IDs and validates positive `lastInsertId()` values. Requirement writes and complete Completion writes own their transaction only when no outer transaction exists; otherwise they participate without committing or rolling back the caller's transaction. UTC timestamps are stored and parsed at exact second precision, prepared statements are used throughout and malformed persistence fails closed.
- Phase 3 local validation passes with 329 optimized classes, 520 tests with 0 failures and three HTTP/session scenarios. Composer validation, complete modified-PHP lint, architecture scans and diff checks pass; no Phase 2 Domain model, migration, schema or seeder changed.
- The E009 Phase 3 disposable MariaDB 10.4.32 execution historically completed migrations 001–009, StatusTypeSeeder, StatusSeeder and AdminSeeder. It confirmed the then-current 35-table functional inventory plus `migrations`, 58-FK schema parity and all six exact E009 FKs. The later E012 corrective execution validates the current migration-010 baseline separately.
- Productive Phase 3 coverage passed for Requirement UTF-8/nullability, ACTIVE/INACTIVE GENERAL_STATUS, generated identities, complete roundtrip, update, stable AcademicPeriod ordering and `hasAcknowledgements()` false/true. Completion coverage passed for generated root/child identities, multiple children, exact UTC seconds, historical reconstruction after Requirement change/deactivation, persisted-save rejection, physical uniqueness, composite same-period ownership, owned rollback and outer-transaction participation/rollback.
- The complete accumulated physical regression passed. Automatic cleanup left zero `antares_e009_phase3_tmp%` databases, the independent final `SHOW DATABASES LIKE 'antares_e009_phase3_tmp%';` returned zero rows and no manual cleanup was required. No credential is recorded.
- Phase 4 adds focused Application cases for Requirement list/create/update/activate/deactivate, Representative state, exact atomic confirmation and the explicit satisfaction boundary. Update obtains `hasAcknowledgements()` and Domain retains the protected post-use invariant.
- Confirmation re-resolves the current ACTIVE set server-side, requires exact positive unique integer IDs, treats zero ACTIVE as satisfied without persistence and uses one `TransactionRunner` boundary plus one complete Aggregate save. Caller-supplied `DateTimeImmutable` and persisted root/child state are verified at UTC-second semantics.
- Phase 4 local validation passes with 352 optimized classes, 539 tests with 0 failures, three HTTP/session scenarios, lint for 27 changed PHP files and clean architecture/diff checks. MariaDB was not rerun because no physical boundary changed.
- PR #40 merged the locally validated Phase 4 Application into `main` at `b3e065bad22c1ec69f746dd3b173ab01b274b89f`.
- Phase 5 adds exactly one GET and four CSRF-protected POST routes behind `AuthenticationMiddleware` and the context-specific temporary exact `admin` gate. The server stores only the positive selected AcademicPeriodId, revalidates it through the Delivery provider and uses hidden period input only for stale/tampering detection; no current period is selected automatically and all AcademicPeriods remain available.
- Requirement create, update, activate and deactivate use the Phase 4 cases. Update and status mutations now verify the expected AcademicPeriod in Application before history lookup, mutation or save, so cross-period requests fail closed without enumeration. Domain post-use protection remains unchanged.
- The new prepared PDO AcademicPeriod options provider is limited to Delivery context. The controller contains no SQL, PDO, Repository or Domain mutation rule; every successful POST uses PRG 303 and flash, errors are safely mapped, arbitrary stored URLs render only as escaped text, and the White Label dashboard exposes the administrator entry.
- Phase 5 local validation passes with 358 optimized classes, 550 tests with 0 failures, three HTTP/session scenarios, lint for all 21 changed PHP files and clean architecture/diff checks.
- Disposable MariaDB 10.4.32 passed migrations 001–009, StatusTypeSeeder, StatusSeeder, AdminSeeder, the complete accumulated regression and `PASS MySQL Institutional Acknowledgements administrator AcademicPeriod provider context hardening and Application persistence`. Physical coverage confirms all-period ordering and lookup including INACTIVE, Requirement list/create/update/status, AUTO_INCREMENT, GENERAL_STATUS, same-period success, cross-period rejection and unchanged rollback guarantees.
- Automatic cleanup passed. The independent final `SHOW DATABASES LIKE 'antares_e009_phase5_tmp%';` returned zero rows; no manual drop was required and no credential is recorded.
- PR #41 merged the locally and physically validated Phase 5 Administrator Delivery into `main` at `7de0b7f26f6aadf2c8488e8d7e1c51dbb5d7ad26`.
- PR #42 merged the locally and physically validated Phase 5.1 Active AcademicPeriod Baseline into `main` at `291ced50279060d69bd96fa7289b5469c6d2841f`.
- Phase 5.1 adds only the minimum `App\AcademicCore` lifecycle boundary for AcademicPeriod: an Aggregate with immutable identity/code/name/date range, ACTIVE/INACTIVE GENERAL_STATUS, `AcademicPeriodRepository`, prepared PDO adapter and the reusable `GetActiveAcademicPeriod`, `ActivateAcademicPeriod` and `DeactivateAcademicPeriod` cases. It adds no Grade/Section administration, Enrollment, Representative UI or parallel current-period concept.
- Zero or one AcademicPeriod may be ACTIVE. Activation serializes on the stable GENERAL_STATUS/ACTIVE row, deactivates the previous ACTIVE and activates the target in one `TransactionRunner`; deactivation may leave zero. Dates never infer state. `findActive()` returns null/one and fails closed for multiplicity or incompatible persisted status.
- The existing E009 administrator selector still exposes every AcademicPeriod independently of status. Two additional CSRF-protected POST routes use the existing authentication and temporary exact `admin` gate, display the exact status and apply flash + PRG 303. No Representative route or period selector was added.
- Phase 5.1 local validation passes with 376 optimized classes, 566 tests with 0 failures and all three HTTP/session scenarios. Composer validation, strict optimized autoload, lint for every new/modified PHP file, architecture scans and both diff checks pass.
- Disposable MariaDB 10.4.32 approved migrations 001–009, StatusTypeSeeder, StatusSeeder, AdminSeeder, 35 functional tables plus `migrations`, 58 physical FKs and the complete accumulated regression. The dedicated scenario approved exact GENERAL_STATUS mapping, zero/one ACTIVE, A→B atomic transition, B→zero deactivation, multiplicity and wrong-status fail-closed behavior, rollback after partial transition, and row-lock serialization across two connections with exactly one final ACTIVE period.
- Harness partial/final automatic cleanup passed. The independent final `SHOW DATABASES LIKE 'antares_e009_phase51_tmp%';` returned zero rows; no manual drop was required, all temporary variables were removed and no credential is recorded.
- Phase 6 resolves the authenticated Representative and the unique ACTIVE AcademicPeriod exclusively server-side. The Representative sends neither identity nor period input, and no Representative, AcademicPeriod, Requirement-set, Completion or satisfaction authority is stored in session.
- The Portal exposes one authenticated GET and one CSRF-protected completion POST with per-requirement checkboxes, exact ACTIVE-set revalidation, PRG 303, escaped output and active links only for explicit HTTP/HTTPS URLs. Pending, completed, zero-requirement and no-active-period states are represented by Application output.
- `RequireRepresentativeAcknowledgementSatisfaction` is the reusable Application boundary for Representative data-maintenance capabilities. Family Resources invokes it after resolving Family context and before loading resources; pending acknowledgements and missing ACTIVE periods fail closed, while zero requirements and an existing Completion permit access without reconfirmation.
- Local Phase 6 validation passes with 386 optimized classes, 584 tests with 0 failures and all three HTTP/session scenarios. Modified-PHP lint, architecture scans and both diff checks pass. MariaDB was not rerun because Phase 6 changes no Domain, Persistence, PDO adapter, schema, migration, seeder or physical harness; the merged Phase 5.1 MariaDB 10.4.32 evidence remains applicable.
- Legacy Person/Family delivery incompatible with the approved model remains retired; the current controllers, views and routes are module-scoped and reuse approved Application cases.
- Seeders now provide only approved status types and statuses. No institutional catalog value or synthetic Person is invented; optional administrator creation requires an existing Person identifier and an external initial password.
- Local PHP, Composer, schema-parity, application-bootstrap, IdentityAccess, Person Domain, Person Persistence, Person Application and Person Delivery validations passed.
- Person Domain now contains only the Person Aggregate Root, PersonId, PersonalName, Identification, ContactInformation, GENERAL_STATUS lifecycle and domain exceptions.
- Person unit tests cover valid construction, optional identity/contact data, Value Object equality, invalid formats and bounds, non-future birth date, Catalog references, atomic identity replacement, contact replacement and activation lifecycle.
- PersonRepository exposes only lookup by PersonId, lookup by Identification and save. PdoPersonRepository maps every approved `persons` column, validates GENERAL_STATUS, preserves nullable fields, inserts new Persons through database-generated AUTO_INCREMENT identities and performs exact-row updates.
- Local persistence tests cover complete reconstruction, present and absent parameterized identification lookups, optional fields, ACTIVE and INACTIVE, invalid status ownership/code, insertion, update, null removal, normalized identification uniqueness and a disappearing update target.
- MariaDB connections explicitly use `utf8mb4` with `utf8mb4_unicode_ci`. Person identification lookup compares the bare indexed `identification_key` with one prepared key parameter instead of constructing a connection-collated SQL expression.
- The disposable MariaDB harness now includes real Person insert, ID and identification lookup, update, GENERAL_STATUS mapping, normalized uniqueness, connection/column collation verification, safe collation diagnostics and cleanup coverage without institutional seed data.
- Person Application contains only CreatePerson, GetPerson and UpdatePerson, one typed input per write use case, one common immutable output and specific not-found, duplicate-identification and invalid-persisted-result errors.
- Create and Update receive an explicit current date, use only PersonRepository, preserve domain exceptions and perform no transaction, locking, retry or infrastructure work.
- Local Application coverage uses a test-only in-memory repository and verifies creation, complete output, repository-generated identity, lookup, full replacement, optional-data removal, status changes, duplicate identification handling, identity preservation and atomic domain failure.
- Person Delivery provides exactly six protected routes for index, create, detail and update, with no list, name search, pagination, delete, import, API or report flow.
- Every Person route runs AuthenticationMiddleware followed by the temporary Person administrator middleware. Authenticated non-admin users receive HTTP 403, and only `admin` sees the dashboard navigation entry.
- PersonController adapts HTTP to CreatePerson, GetPerson and UpdatePerson, validates only request shape, protects POST with the existing CSRF service, preserves safe form input, binds the edited identity to server-side session state and maps known application/domain errors without exposing SQL or internal details.
- Person form options are read by a specific PDO infrastructure provider from active document type, sex, marital status and education level rows plus active GENERAL_STATUS ACTIVE/INACTIVE values. Required catalog absence disables persistence.
- White Label HTML views cover Person index, create/edit, detail and safe not-found output. No institution is hardcoded.
- The accumulated local suite contains 191 passing tests plus three HTTP/session scenarios.
- The disposable MariaDB 10.4 validation passed for the clean baseline, Person persistence and form-option loading. Cleanup confirmed that no temporary database remained.
- No User creation, Family, Enrollment, import, report or generic administration framework was added.
- E005 is completed. Representative and Student are no longer pending phases of E005; they are the independently governed scope of E006.
- E006 covers stable Representative and Student roles over existing Person records, with separate Domain, Persistence, Application and minimal administrator Delivery phases.
- The approved `representatives` and `students` tables already enforce one role of each type per Person. Student institutional code is unique, both roles use GENERAL_STATUS, and neither duplicates personal data.
- Representative Domain contains only Representative, RepresentativeStatus, InvalidRepresentativeState, its own RepresentativeId and PersonId, and optional immutable EmploymentInformation.
- Representative supports new and reconstructed identities, an immutable Person reference, whole-value employment replacement/removal and the ACTIVE/INACTIVE lifecycle. It does not validate Person existence, uniqueness or cross-aggregate relationships.
- Representative Domain tests cover positive immutable identities, Value Object normalization/equality/bounds, free-format work phone, email validity, optional employment absence, aggregate construction, immutable references, atomic invalid replacement, lifecycle and structural isolation.
- Student Domain contains only Student, StudentStatus, InvalidStudentState, its own StudentId and PersonId, InstitutionalCode and AdmissionDate.
- Student supports new and reconstructed identities, an immutable Person reference, administratively replaceable InstitutionalCode and AdmissionDate, and the ACTIVE/INACTIVE lifecycle.
- InstitutionalCode is required, trimmed, case- and format-preserving, and limited to 100 characters. AdmissionDate is required, normalized to midnight and cannot be future relative to an explicit current date.
- Student Domain tests cover positive immutable identities, code normalization/equality/bounds, deterministic dates, required construction, atomic administrative replacement, lifecycle and structural isolation without Grade or Section.
- RepresentativeRepository exposes only find by identity, find by PersonId and save. PdoRepresentativeRepository reconstructs optional EmploymentInformation, resolves exact GENERAL_STATUS values and uses database-generated identities.
- StudentRepository exposes only find by identity, PersonId or InstitutionalCode and save. PdoStudentRepository uses direct indexed code comparison, strict UTC admission-date reconstruction, exact GENERAL_STATUS values and database-generated identities.
- Both repositories update directly without an existence probe or internal transaction, preserve PersonId, accept a zero-row update only for identical persisted state and fail if the target disappears.
- SQLite persistence coverage validates lookups, reconstruction, nullable employment data, dates, state ownership/codes, AUTO_INCREMENT, update behavior, uniqueness, prepared SQL and absence of internal transactions.
- The disposable MariaDB 10.4 harness passed physical role identities, lookups, updates, uniqueness, official collation and exact status mapping; cleanup confirmed that no temporary database remained.
- Representative Application contains only CreateRepresentative, GetRepresentative and UpdateRepresentative, typed full-state write inputs, one immutable output and specific errors for missing roles or Person, per-Person duplication and invalid persisted results.
- Student Application contains only CreateStudent, GetStudent and UpdateStudent, typed full-state write inputs, one immutable output and specific errors for missing roles or Person, per-Person duplication, InstitutionalCode duplication and invalid persisted results.
- Role creation verifies the referenced persisted Person through PersonRepository and performs understandable prechecks for per-Person and InstitutionalCode uniqueness. Database foreign keys and UNIQUE constraints remain the final concurrency guarantees.
- Student create and update receive an explicit DateTimeImmutable current date and normalize it to midnight before constructing AdmissionDate. No system clock, Clock abstraction, transaction, lock or retry was added.
- Role Application coverage uses test-only in-memory repositories and verifies creation, lookup, full replacement, optional employment removal, state changes, generated identities, immutable identifiers, deterministic dates, duplicate checks, domain failure atomicity and dependency isolation.
- Representative and Student have no standalone administration framework; their only current delivery is the approved composite Family flow. No User, Enrollment, import or report flow has been implemented.
- The approved membership path is `Representative → FamilyRepresentative → Family → FamilyStudent → Student`; no table, Aggregate or use case named RepresentativeStudent belongs to the model.
- The administrative creation flow coordinates Person, Representative or Student and Family memberships through the approved Application orchestrators while Delivery presents each operation as one form.
- E006.5 is intentionally limited to Family, FamilyRepresentative and FamilyStudent membership. Addresses, contacts, authorized pickups and the remaining Family resources stay planned for later milestones.
- Family Domain contains only the Family Aggregate Root, FamilyStatus, FamilyRepresentative, FamilyStudent, InvalidFamilyState and typed identifiers plus DisplayName.
- New Family creation is atomic: it always creates an unpersisted Family with one unpersisted active primary FamilyRepresentative and no required Student membership. There is no empty, draft or pending Family state.
- Family reconstruction validates at least one active Representative, exactly one active primary Representative and no duplicate active Representative or Student within the Aggregate. Historical ended memberships remain preserved.
- Membership dates are explicit, normalized to second precision without changing timezone, and never read from the system clock. Ended memberships cannot be reactivated or ended twice.
- Family behavior covers display-name and ACTIVE/INACTIVE lifecycle changes, additional non-primary Representatives, Students, permitted membership termination, active views and primary-Representative lookup. Primary replacement and cross-Family Student uniqueness remain outside this phase.
- Family Domain coverage adds 29 unit tests for Value Objects, atomic creation, reconstruction, membership Entities, invariants, temporal behavior, atomic failures, immutable collection exposure and module isolation. The accumulated local suite contains 220 passing tests plus three HTTP/session scenarios.
- FamilyRepository now exposes only complete Aggregate lookup by FamilyId, active membership lookup by RepresentativeId or StudentId, and save.
- PdoFamilyRepository reconstructs all active and historical memberships in stable order, validates exact GENERAL_STATUS ownership and codes, parses persisted timestamps strictly in UTC and delegates Aggregate validity to Family Domain.
- A new Family, its initial active primary Representative and any additional new memberships are inserted with database-generated AUTO_INCREMENT identities inside one transaction. No ID, generated column or catalog ID is calculated or hard-coded.
- Existing Family synchronization updates only DisplayName, FamilyStatus and allowed `ended_at` transitions, inserts new memberships and rejects omitted, duplicated, unknown, cross-Family or immutably changed persisted membership identities. No history is deleted or replaced.
- The repository starts, commits and rolls back its own transaction only when no outer transaction exists. When called inside an existing transaction it participates without commit, rollback or savepoint.
- Representative lookup returns every Family with an active Representative membership without filtering FamilyStatus. Student lookup returns at most one complete Family and reports persisted multiplicity as corruption.
- Family Persistence adds 26 SQLite tests for contract shape, complete reconstruction, corrupted persistence, searches, generated identities, rollback, outer-transaction ownership, updates, immutable fields, history preservation and SQL isolation. The accumulated local suite contains 246 passing tests plus three HTTP/session scenarios.
- The disposable MariaDB harness now covers physical Family AUTO_INCREMENT creation, complete reload, membership searches and history, UTC timestamps, exact status mapping, active uniqueness, RelationshipType FK and rollback without orphan rows.
- The disposable MariaDB 10.4 validation passed after migrations 001–009 and seeders ran successfully. It approved atomic Family creation and complete Aggregate reconstruction, Representative and Student active lookups and history, physical active uniqueness, UTC timestamps, exact GENERAL_STATUS mapping and transactional rollback.
- The first physical run exposed only that the harness inspection connection inherited `SYSTEM` (`America/Bogota`) while reading `TIMESTAMP`; production connections already used UTC correctly. The harness now applies `SET time_zone = '+00:00'` to that session.
- Disposable cleanup passed, including partial-creation cleanup, and no `antares_e0065_family_tmp%` database remained.
- Family Membership Application contains exactly CreateFamily, GetFamily, GetFamilyMembership, AddRepresentativeToFamily, AddStudentToFamily, EndRepresentativeMembership and EndStudentMembership.
- Application validates Representative and Student existence through their owner repository contracts and RelationshipType existence through the minimal `RelationshipTypeLookup` port. Family Domain remains isolated from those modules.
- Student membership creation prevents a second active Family through `FamilyRepository::findActiveByStudentId()` while preserving ended history and leaving the physical unique constraint as the final concurrency guarantee.
- Membership timestamps are supplied explicitly, retain their received timezone and second precision through Domain behavior, and are never read from the system clock in Application.
- One immutable `FamilyOutput`, with nested Representative and Student membership outputs, exposes complete active and historical membership state without returning Domain Entities.
- The 32 Family Membership Application tests cover creation, complete queries, external existence checks, active-Student protection, additions, terminations, generated persisted identities, history, atomic failures and dependency isolation. The accumulated local suite contains 278 passing tests plus three HTTP/session scenarios.
- Family Application opens no transactions and depends on no PDO, concrete repository, Infrastructure, HTTP, Person, User, Enrollment or direct Representative–Student association. No Domain, Persistence, schema, migration, seeder or delivery code changed.
- Composite Administrative Orchestration contains exactly CreateRepresentativeFamily and CreateStudentInFamily. The first coordinates CreatePerson, CreateRepresentative and CreateFamily; the second validates the existing Family and coordinates CreatePerson, CreateStudent and AddStudentToFamily.
- `TransactionRunner::run(callable): mixed` defines one callback boundary in Core Application. `PdoTransactionRunner` owns exactly one outer transaction through the shared ConnectionManager, rejects a pre-existing transaction, checks begin/commit/rollback results, uses no savepoint or retry and propagates the original failure after rollback.
- Composite outputs reuse PersonOutput, RepresentativeOutput, StudentOutput and FamilyOutput rather than duplicating fields or returning Aggregates. Every generated identity is required to be positive before the next step is invoked.
- `today` remains an explicit calendar-validation value for Person and Student, while membership `startedAt` remains an independent explicit timestamp whose timezone is unchanged by Application.
- Unit coverage uses a snapshot-based in-memory TransactionRunner to restore every participating repository after any failure. PDO coverage uses SQLite to verify callback execution inside a transaction, exact result return, commit, rollback, original-exception propagation and rejection of an existing transaction.
- The MariaDB harness exercises both composite flows with real PDO repositories and PdoTransactionRunner. MariaDB 10.4 approved the complete physical commits and total rollbacks after partial writes, including preservation of the existing Family in the Student failure scenario.
- The accumulated local suite contains 289 passing tests plus three HTTP/session scenarios. Migrations 001–009, seeders and all physical MariaDB scenarios passed; partial and final disposable cleanup left no `antares_e0065_orchestration_tmp%` database.
- The first physical attempt failed only because the harness used a future fixed Student admission date. The fixture was aligned to fixed valid dates; production required no correction.
- Phase 6 exposes exactly six `/families` routes for entry, composite Representative/Family creation, detail and composite Student addition. Every route runs AuthenticationMiddleware followed by the exact temporary `admin` gate.
- FamilyController depends explicitly on CreateRepresentativeFamily, CreateStudentInFamily, GetFamily, the existing CSRF/session services and form-option providers. POST actions invoke only the two composite orchestrators and map only known functional failures; unexpected failures still reach the global handler.
- The Representative form creates Person, Representative, Family and its active primary membership atomically. The Student form creates Person, Student and FamilyStudent for an existing Family, with the authoritative Family ID pulled from session and the browser value used only for consistency.
- PdoRelationshipTypeLookup and PdoFamilyFormOptionsProvider read only active physical RelationshipTypes, expose safe ordered labels and exact ACTIVE/INACTIVE FamilyStatus values, and share the registered ConnectionManager.
- Both POST flows use the existing CSRF mechanism, strict scalar/date/timestamp/email/catalog validation, safe form-state preservation and escaped White Label views. No credentials, portal, User, Enrollment, additional Family resources or direct Representative–Student association were added.
- The current local suite contains 341 passing tests plus three HTTP/session scenarios. Composer validation passed, strict optimized PSR autoload generated 200 classes, and PHP lint plus diff validation pass.
- The authorized disposable MariaDB 10.4 validation passed on 2026-08-10 after migrations 001–009 and seeders executed successfully. It physically approved Representative User INSERT with database-generated positive UserId, findByPersonId, normalized login, password hashing, exact USER_STATUS, one User per Person and global normalized-login uniqueness.
- Administrative password replacement physically preserved UserId, PersonId, LoginIdentifier, UserStatus, failed-login attempts, lockedAt and lastAccessAt. Atomic Person.DocumentNumber/User.LoginIdentifier synchronization preserved identity, password, status and authentication state; login conflicts rolled back both sides, the new DocumentNumber authenticated successfully, the previous login stopped resolving and existing administrator compatibility remained intact.
- Partial-creation and final disposable cleanup passed. `SHOW DATABASES LIKE 'antares_e007_phase2_tmp%';` returned no rows, and the physical run required no production correction.
- Phase 3 adds `GetAuthenticatedRepresentative` and its readonly safe identity DTO. The use case receives no external identity, reuses `GetAuthenticatedUser`, converts IdentityAccess PersonId explicitly by value and resolves only through `RepresentativeRepository::findByPersonId()`.
- Resolution is read-only and fail-closed. It returns absence for no authenticated User or a User without Representative, rejects an incoherent persisted Representative result, applies no inferred ACTIVE/INACTIVE authorization rule, and stores no PersonId, RepresentativeId or DTO in session.
- Phase 3 adds no Family, Student, Infrastructure, PDO, SQL, transaction, HTTP, Controller, middleware, route, view, Portal or schema dependency. The container reuses the existing `GetAuthenticatedUser` and `RepresentativeRepository` registrations.
- Local Phase 3 validation passed with 203 optimized autoload classes, 351 tests with 0 failures and three HTTP/session scenarios. PHP lint and `git diff --check` passed.
- The authorized disposable MariaDB 10.4 Phase 3 validation passed on 2026-08-10. It approved exact UserId, PersonId, RepresentativeId and LoginIdentifier resolution, fail-closed absence for an authenticated User without Representative, and strict read-only behavior with no persisted mutation.
- Before the E009 physical baseline change, the accumulated disposable evidence approved migrations 001–009, the then-current 36-table baseline plus migration metadata, seeders, AdminSeeder compatibility, UTC and concurrent User locking, Person, Representative, Student, Family, Family Delivery, composite orchestration and Representative User capabilities. The final E009 run revalidated that accumulated regression against the new 35-table functional baseline.
- Partial disposable-database cleanup and final cleanup passed. `SHOW DATABASES LIKE 'antares_e007_phase3_tmp%';` returned no rows.
- Phase 4 adds `GetAuthorizedFamilies`, `ResolveFamilyContext` and `SelectAuthorizedFamily` with safe readonly identity/context outputs. Every operation begins from `GetAuthenticatedRepresentative` and resolves current active FamilyRepresentative memberships through the existing FamilyRepository contract.
- `findActiveByRepresentativeId()` retains its established semantics: active means membership `ended_at IS NULL`, results are ordered by FamilyId and FamilyStatus does not filter authorization. `isPrimary` does not choose among multiple Families.
- Zero authorized Families return no context; one Family auto-resolves and persists only its positive FamilyId for continuity; multiple Families require explicit selection. Selection and change accept only a Family in the freshly resolved authorized set.
- Every context read revalidates User, Representative and active membership. Stale context is removed; zero, one and multiple remaining-Family cases are reevaluated. Login regeneration removes the prior Family context while preserving unrelated session values, and logout already destroys it with the complete session.
- SessionManager now supports persistent `get()` and `remove()` operations while retaining `put()` and `pull()`. Session never stores UserId or RepresentativeId as Family context, DTOs, Aggregates, membership Entities, Family lists or Student identities.
- Local Phase 4 validation passed with 212 optimized autoload classes, 368 tests with 0 failures and three HTTP/session scenarios. PHP lint, Composer validation and `git diff --check` passed.
- The authorized disposable MariaDB 10.4 Phase 4 validation passed on 2026-08-10 after migrations 001–009 and StatusTypeSeeder, StatusSeeder and AdminSeeder completed. It approved Representative access resolution and the physical Family context scenario for one and two active Families, automatic single-Family context, authorized selection and change, rejection of an unauthorized Family, historical exclusion, stale-context invalidation, current-membership revalidation, administrator absence and strict read-only Aggregate snapshots. The local suite remains the approved coverage for the complete zero/one/multiple state matrix and User-session isolation.
- Partial disposable-database cleanup passed, final cleanup was confirmed manually, and `SHOW DATABASES LIKE 'antares_e007_phase4_tmp%';` returned no rows. No temporary database with that prefix remained.
- Phase 5 adds the minimum protected Representative Portal Delivery through authenticated `GET /representative` and CSRF-protected `POST /representative/family`. The root dashboard redirects a resolved Representative to the Portal while retaining the administrator dashboard for a non-Representative authenticated User.
- `RepresentativePortalController` delegates identity and Family authority exclusively to `GetAuthenticatedRepresentative`, `ResolveFamilyContext` and `SelectAuthorizedFamily`. It performs no repository, PDO, SQL, transaction, Domain or schema work and never accepts PersonId or RepresentativeId from the client.
- Portal behavior preserves the approved zero/one/multiple model: no authorized Family returns a safe 403; one Family renders directly; multiple Families render only the freshly authorized selector; selection or change is revalidated and redirects with 303. Invalid CSRF, invalid input, another Representative's Family, historical membership and stale TOCTOU membership fail closed without acquiring or retaining unauthorized context.
- The three White Label Portal views expose only safe Family display names, escape dynamic output, reuse the existing one-time CSRF mechanism and provide the existing logout action. They add no institution-coded identity, Student data, Enrollment, Family resources, generic administration framework or Phase 6 recovery behavior.
- Local Phase 5 validation passed with 213 optimized autoload classes, 384 tests with 0 failures and three HTTP/session scenarios. Composer validation, PHP lint and `git diff --check` passed.
- No Phase 5 MariaDB run was executed. The approved Phase 4 MariaDB 10.4 evidence remains applicable because Phase 5 changes only HTTP/session Delivery and container wiring; no migration, schema, repository, Persistence implementation, connection or MariaDB harness changed.
- PR #26 merged Phase 5 into `main` at `118747114e6534c0353d7cbde1ea951cd0b6b3f1` before the Phase 6 branch was created from that exact baseline.
- Phase 6 formalizes the cross-Aggregate invariant that every Person with Representative role retains a valid `Person.ContactInformation.email`, whether or not a User exists. Generic Person email remains optional, `Representative.EmploymentInformation.workEmail` remains optional and never substitutes the personal email, and no physical nullability or schema changed.
- `CreateRepresentative` rejects missing personal email before role persistence; the composite Representative/Family Delivery requires it and preserves total transaction rollback; `UpdatePersonWithRepresentativeUserSync` rejects its removal for every Representative while preserving the existing Identification/LoginIdentifier rule; and Representative User provisioning fails closed for historical inconsistent data.
- The public White Label `GET /forgot-password` route renders only the generic instruction to contact Secretaría and a return-to-login link. It accepts no username, document or email, performs no account lookup or state change, and introduces no POST, token, OTP, mail integration, reset persistence or automated recovery. Secretaría continues to use the existing admin-protected `ChangeRepresentativeUserPassword` operation after external verification.
- Local Phase 6 validation passed with 214 optimized autoload classes, 395 tests with 0 failures and three HTTP/session scenarios. Composer validation, complete modified-PHP lint and `git diff --check` passed; security and architecture scans confirmed no reset infrastructure, schema change, Persistence cross-aggregate join or global Person email requirement.
- The authorized Phase 6 physical validation completed without failures on disposable MariaDB 10.4 at `127.0.0.1:3306`, user `antares_test`, prefix `antares_e007_phase6_tmp` and `E0041_DB_ALLOW_DISPOSABLE=1`. Migrations 001–009, StatusTypeSeeder, StatusSeeder, AdminSeeder and the complete harness passed; no password is recorded.
- Phase 6 physically approved the Representative personal-email invariant and work-email non-substitution; atomic composite Representative/Person/role/Family commit and rollback; email-gated Representative User AUTO_INCREMENT provisioning, lookup, hashing and uniqueness; administrative password change with authentication-state preservation; and email plus DocumentNumber/LoginIdentifier synchronization, conflict rollback and authentication.
- Before the E009 physical baseline change, accumulated disposable regression also approved authenticated Representative read-only resolution and fail-closed behavior, Family-context authorization/selection/stale invalidation, the then-current 36-table domain baseline plus migration metadata, status and AdminSeeder behavior, UTC and concurrent User locking, Person/Representative/Student/Family persistence, Family Delivery, composite Student flow and transactional rollback. The final E009 run revalidated the complete regression against 35 functional tables plus migration metadata.
- Partial harness cleanup passed, final manual cleanup passed and `SHOW DATABASES LIKE 'antares_e007_phase6_tmp%';` returned no rows. No Phase 6 temporary database remains. The public read-only Forgot Password Delivery required no independent MariaDB scenario because it performs no account lookup, Persistence access or state change.
- The authorized disposable MariaDB 10.4 validation passed after migrations 001–009 and seeders executed. PdoRelationshipTypeLookup and PdoFamilyFormOptionsProvider physically exposed only active RelationshipTypes and exact ACTIVE/INACTIVE FamilyStatus values.
- Both productive composite flows retained their approved physical behavior: Representative + Family and Student + existing Family committed completely on success and rolled back every partial write on deliberate failure. Existing UTC and physical-integrity checks remained approved.
- Partial-creation cleanup and final cleanup passed. `SHOW DATABASES LIKE 'antares_e0065_delivery_tmp%';` returned no rows, and no production correction was required.
- No existing Domain, Application case, orchestration DTO, repository, Persistence implementation, ConnectionFactory, ConnectionManager, migration, schema or seeder changed. The only pre-existing test adjusted outside the new delivery suite was the bootstrap assertion that previously prohibited any Family route.
- GitHub remains the single source of truth.

## Immediate Next Action

Deploy and verify the four residual Phase 4 Portal refinements, then complete the separate C-014 correction and owner acceptance. Do not claim formal MVP closure or implement Category D/E requests.

## Last Updated

2026-09-21
