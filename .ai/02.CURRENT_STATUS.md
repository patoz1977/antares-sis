# Current Status

## Purpose

This document provides a real-time snapshot of the project.

It is the primary entry point for understanding the current implementation status, the work currently in progress and the immediate priorities.

This document intentionally contains only the present state of the project.

Historical information belongs exclusively to `04.DEVELOPMENT_LOG.md`.

Future planning belongs exclusively to `03.ROADMAP.md`.

---

# Project Status

## Overall Status

The Enrollment Domain Discovery is closed and the AUDIT-001 documentation reconstruction is technically approved as the official Enrollment baseline.

E004.1 — Identity & Access Baseline is completed and merged into `main` through PR #3.

E005 — Person Baseline is the current functional milestone. Phase 1 established ADR-0018 and the pre-production rebaseline boundary. Phase 2 is in progress through the completed IdentityAccess persistence alignment subphase; the general schema rebaseline remains pending.

IdentityAccess persistence now targets the official `users` structure without `deleted_at` or `locked_until`. Its domain, application, HTTP, session, CSRF and authentication behavior remain unchanged.

## Current Branch

`docs/e005-preproduction-rebaseline`

## Current Milestone

E005 — Person Baseline — Phase 2 technical cleanup in progress; IdentityAccess alignment completed

TEST-001 — MariaDB Integration Test Infrastructure remains a separate, independent technical milestone.

## Current Objective

Prepare Phase 2B — general technical cleanup and schema rebaseline — without executing the current migration sequence or connecting to a database.

## Current Risks

- The destructive authorization is valid only while the target remains pre-production and disposable; it must not be applied to an institutional or production database containing real data.
- The current repository contains legacy code, migrations and data that may not match the approved documentary baseline.
- Migration 005 now defines the official `users` table directly and migration 014 was retired because its sole purpose was incremental legacy conversion. The remaining active sequence still contains legacy structures outside this subphase and must not be executed until Phase 2B completes the general rebaseline.
- Phase 2B must verify which remaining components are compatible and reusable before rebuilding the active migration sequence.
- Official catalog values not yet supplied remain a data-master concern, not a schema decision.

## Pending Decisions

No additional decision is pending for the IdentityAccess persistence alignment. Functional or physical-design ambiguities discovered in later phases must be resolved through the official analysis workflow before implementation.

## Documentation Status

| Document | Status |
|---|---|
| Project Context | Current |
| Enrollment Domain Discovery | Closed |
| Architecture | Technically approved |
| Domain Model | Technically approved |
| Decisions | Current; ADR-0018 approved |
| Catalogs | Technically approved |
| Database Design | Technically approved |
| ERD | Technically approved |
| AUDIT-001 | Completed and technically approved |
| Current Status | Current |
| Roadmap | Current |
| Development Log | Current |

## Repository Status

- PR #3 was merged into `main` at commit `60a444d`.
- E004.1 remains functionally unchanged.
- IdentityAccess persistence and its tests are aligned with the official `users` columns.
- Migration 005 provides the clean `users` definition and the legacy incremental migration 014 is no longer part of the active sequence.
- No migration or database operation was executed during this alignment.
- GitHub remains the single source of truth.

## Immediate Next Action

Phase 2B — General technical cleanup and schema rebaseline. Do not execute migrations until the clean active sequence is complete and validated in an explicitly authorized disposable environment.

## Last Updated

2026-07-31
