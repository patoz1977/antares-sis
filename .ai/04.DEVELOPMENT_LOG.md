# DEVELOPMENT LOG

## Purpose

This document is the official historical record of the project.

It records completed work in chronological order.

It intentionally excludes:

- the current status of the project (see `02.CURRENT_STATUS.md`);
- future planning (see `03.ROADMAP.md`).

This document is immutable except for the addition of new historical entries.

---

# Logging Rules

Every completed milestone shall generate a new entry.

Each entry should record:

- date;
- completed milestone;
- summary of the work performed;
- important architectural decisions;
- documentation updated;
- repository status.

Entries shall never be modified to reflect the current state of the project.

---

# Project History

## Phase 0 — Project Foundation

### Milestone E000

**Status:** Completed

#### Summary

The project was formally created.

The initial engineering methodology was established.

The repository was configured as the single source of truth.

The documentation strategy was defined.

#### Main Deliverables

- Initial repository.
- Documentation structure.
- AI documentation.
- Development workflow.
- Engineering standards.

---

## Phase 1 — Framework Foundation

### Milestone E001

**Status:** Completed

#### Summary

The base framework was implemented.

The project obtained a functional MVC foundation suitable for future business development.

#### Main Deliverables

- Bootstrap.
- Configuration.
- Router.
- Request.
- Response.
- Kernel.
- Controller.
- View Renderer.
- Layout.

#### Result

The project became ready for business implementation.

---

## Phase 2 — Domain Analysis

### Milestone E003.1

**Status:** Completed

#### Summary

Initial analysis of the enrollment business domain.

Identification of principal entities and business processes.

---

### Milestone E003.2

**Status:** Completed

#### Summary

Expansion of business rules.

Review of entity relationships.

Initial aggregate identification.

---

### Milestone E003.3

**Status:** Completed

#### Summary

Formal review of the Enrollment Domain.

Business inconsistencies requiring consolidation were identified.

Implementation was intentionally paused until the domain reaches the required maturity level.

---

## Phase 3 — Domain Consolidation

### Milestone E003.3.1

**Status:** Completed

#### Summary

A comprehensive review of the Enrollment Domain was performed.

The objective was to verify the consistency of the business model before continuing software implementation.

#### Main Findings

- Inconsistencies between business entities.
- Relationships requiring refinement.
- Missing business definitions.
- Pending catalog standardization.
- Aggregate boundaries requiring review.

#### Result

The project entered a Domain Consolidation phase before continuing implementation.

---

### Milestone E003.3.2

**Status:** Completed

#### Summary

A Domain Discovery Workshop was carried out to validate the enrollment domain from a business perspective.

#### Main Activities

- Review of business terminology.
- Validation of business concepts.
- Analysis of entity responsibilities.
- Review of enrollment workflow.
- Identification of documentation inconsistencies.

#### Result

The workshop confirmed the need for a complete domain consolidation before resuming implementation.

---

### Documentation Iteration AUDIT-001

**Date:** 2026-07-30

**Milestone:** E003.4A — Enrollment Domain Documentation Reconstruction

**Status:** Completed; pending independent technical approval

#### Summary

AUDIT-001 was applied as a phased documentation-only reconstruction. The closed Enrollment Domain Discovery was synchronized into the official architecture, tactical domain model, ADRs, catalogs, MySQL 8 database design and ERD.

#### Principal Decisions Consolidated

- User is an independent Aggregate Root that references exactly one Person.
- One database is used per institution; the schema does not repeat a tenant key.
- Enrollment uses Draft, Submitted, Completed and Cancelled.
- Memberships and assignments preserve vigency with start/end timestamps and generated active keys.
- Family resources remain inside Family; emergency contacts and authorized pickups are not Person.
- Annual BillingInformation, MedicalInformation and TransportInformation belong to Enrollment.
- Submission snapshots copy only the submitted address, emergency contacts and authorized pickups and do not reference mutable catalogs or Family resources.

#### Documentation Updated

- `00.INDEX.md`
- `01.PROJECT_CONTEXT.md`
- `02.CURRENT_STATUS.md`
- `03.ROADMAP.md`
- `04.DEVELOPMENT_LOG.md`
- `10.ARCHITECTURE.md`
- `11.DOMAIN_MODEL.md`
- `12.DATABASE_DESIGN.md`
- `13.ERD.md`
- `14.CATALOGS.md`
- `15.ENROLLMENT_DOMAIN_DISCOVERY.md`
- `20.DECISIONS.md`
- `audits/AUDIT-001.md`

#### Result

The official documentation now contains an implementation-ready physical proposal and matching ERD. The result remains subject to technical review; implementation is still paused and no approval is implied by this historical entry.

#### Repository State

Only documentation was changed. No production code, migration, test, seeder or runtime configuration was modified. Phase commits were published to `docs/enrollment-domain-clean-rebuild`; no merge was performed.

---
### Technical Review Closure AUDIT-001

**Date:** 2026-07-30

**Milestone:** E003.4A — Enrollment Domain Documentation Review

**Status:** Completed and technically approved

#### Summary

The formal technical review of the AUDIT-001 documentation reconstruction was completed.

#### Final Corrections Reviewed

Commit `9c05f14` completed the final review corrections:

- BillingAddress became nullable during Draft while remaining mandatory for Draft → Submitted;
- Grade received a unique positive `sort_order` for next-active-grade proposal;
- emergency-contact snapshots now preserve nullable Priority and materialize a unique positive SortOrder;
- User was included in the general Aggregate transaction-boundary enumeration;
- RelationshipType scope was clarified for FamilyRepresentative memberships, emergency contacts and authorized pickups.

#### Approved Baseline

Architecture, Domain Model, Decisions, Catalogs, Database Design and ERD were approved as the official Enrollment baseline for subsequent implementation planning.

#### Repository State

No production code, migration, test or runtime configuration was changed during the review or its final corrections.

#### Result

E003.4A is complete. The next activity is to define and approve an implementation milestone, including its compatibility, migration and test strategy, before code changes begin.

---

### Temporary User Lockout Policy

**Date:** 2026-07-31

**Milestone:** E004.1 — Identity & Access Baseline

**Status:** Documentary decision completed

#### Ambiguity

ADR-0013 required failed-attempt tracking and a temporary lock represented by `locked_at`, but the official documentation did not define the threshold, duration, expiration behavior or institutional configuration.

#### Decision

ADR-0016 approved a default threshold of five consecutive failed attempts and a default duration of fifteen minutes. Both values are configurable per deployment through a typed authentication policy. Attempts during an active lock do not extend it; expiration and successful authentication reset `failed_login_attempts` and `locked_at`; `USER_STATUS = DISABLED` remains independent.

#### Documents Updated

- `20.DECISIONS.md`
- `31.SECURITY_GUIDELINES.md`
- `11.DOMAIN_MODEL.md`
- `12.DATABASE_DESIGN.md`
- `13.ERD.md`
- `02.CURRENT_STATUS.md`
- `03.ROADMAP.md`
- `04.DEVELOPMENT_LOG.md`

#### Repository State

Only official documentation was changed. No application code, migration, test or runtime configuration was modified, and no migration was executed.

#### Next Step

Integrate the documentary decision and resume E004.1 — Identity & Access Baseline.

---

### Identity & Access Baseline Implementation

**Date:** 2026-07-31

**Milestone:** E004.1 — Identity & Access Baseline

**Status:** Implemented and validated; review and merge pending

#### Summary

The first modular vertical slice replaced the active legacy authentication flow through a strangler-compatible IdentityAccess module. User is now represented as an independent Aggregate Root linked to Person, while the legacy authentication service remains as a compatibility adapter.

#### Main Deliverables

- Secure local login and logout with generic external failures.
- Typed ADR-0016 policy, failed-attempt persistence, automatic lock expiration and DISABLED separation.
- Session identifier regeneration, complete logout destruction and one-time CSRF tokens.
- PDO repository, transaction, clock, hashing and security-event adapters.
- Forward-only migration 014 with deterministic legacy `locked_until` conversion.
- Automated domain, application, persistence, HTTP, real PHP session and MariaDB migration tests.

#### Compatibility and Validation

Migration 014 preserves existing users, hashes, Person relationships and USER_STATUS references. `locked_until` remains temporarily as an unused legacy column and is documented for future safe removal. Fresh and incremental Identity schema scenarios and migration idempotency passed on an isolated MariaDB 10.4 instance; no configured or production database was modified.

A full empty-database execution also exposed a pre-existing foreign-key failure in migration 012. Migrations 001–013 were not changed because E004.1 explicitly requires preserving their history; the issue remains separate technical debt.

#### Documentation Updated

- `02.CURRENT_STATUS.md`
- `03.ROADMAP.md`
- `04.DEVELOPMENT_LOG.md`
- `12.DATABASE_DESIGN.md`
- `.env.example`

#### Repository State

Implementation is isolated on `feature/e004-1-identity-access-baseline` and is ready for Pull Request review. No merge or production migration was performed.

---
### MVP Session Inactivity Policy

**Date:** 2026-07-31

**Milestone:** E004.1 — Identity & Access Baseline

**Status:** Documentary decision completed; technical corrections continue

#### Review Finding

The formal review of PR #3 identified that the general Security Guidelines required automatic session expiration by inactivity while E004.1 did not define or implement an approved inactivity policy.

#### Decision

ADR-0017 establishes that sessions do not expire automatically by inactivity during the MVP. Explicit logout, session identifier regeneration after login and complete session destruction during logout remain mandatory. A future configurable policy is outside E004.1, disabled by default and requires a separately approved milestone.

ADR-0017 does not modify ADR-0016 or the temporary User lockout policy.

#### Documents Updated

- `20.DECISIONS.md`
- `31.SECURITY_GUIDELINES.md`
- `02.CURRENT_STATUS.md`
- `03.ROADMAP.md`
- `04.DEVELOPMENT_LOG.md`

#### Repository State

Only official documentation was changed for this decision. E004.1 is not approved or complete; the remaining authentication, migration, seeder and test corrections from the formal review continue on PR #3.

---
### Identity & Access Formal Review Corrections

**Date:** 2026-07-31

**Milestone:** E004.1 — Identity & Access Baseline

**Status:** Corrections implemented; new formal review pending

#### Corrections

- User is loaded with a MariaDB-compatible row lock inside the authentication transaction and persistence requires exactly one matched row.
- Every external authentication path performs exactly one password-hash verification.
- PDO establishes an explicit UTC SQL session convention.
- Migration 014 preflights legacy identifiers before DDL, records completion of one-time backfills and rejects destructive rollback.
- AdminSeeder requires an external initial password and preserves existing password hashes and User status.
- The MariaDB integration script forbids `.env` fallback and UEAnt, requires explicit disposable authorization and cleans randomly named test databases.
- The Composer test workflow regenerates strict optimized dev autoload before executing tests.

#### Validation

`composer validate --no-check-publish`, strict optimized dev autoload, 29 local tests, three HTTP/real-session scenarios, lint of 126 tracked PHP files and `git diff --check main...HEAD` passed. The revised MariaDB scenarios were not executed because no authorized isolated server was available; no database migration was executed.

#### Documents Updated

- `02.CURRENT_STATUS.md`
- `03.ROADMAP.md`
- `04.DEVELOPMENT_LOG.md`
- `12.DATABASE_DESIGN.md`

#### Repository State

PR #3 remains draft. E004.1 is not approved or merged and requires a new independent formal review plus isolated MariaDB validation.

---
### Identity & Access Second-Review Test Hardening

**Date:** 2026-07-31

**Milestone:** E004.1 — Identity & Access Baseline

**Status:** Three MEDIUM observations corrected locally; MariaDB validation and final review pending

#### Corrections

- The MariaDB UTC scenario writes a known instant through a `-05:00` control session, reads it through the UTC `ConnectionFactory`, and verifies repository `locked_at` round-trip equality.
- Concurrent row-lock verification accepts only MariaDB 10.4 lock timeout `1205/HY000` or deadlock `1213/40001` signatures and rejects an unrelated SQL exception.
- Disposable database cleanup starts before the first creation, tracks each successful creation, attempts every drop in reverse order and reports every cleanup failure.
- A controlled partial-creation scenario verifies that a database created before an intentional failure is removed and that its unique disposable prefix leaves no database behind.

#### Validation

`composer validate --no-check-publish`, strict optimized dev autoload, 29 local tests, three HTTP/real-session scenarios, lint of 126 tracked PHP files, `git diff --check` and `git diff --check main...HEAD` passed. The hardened MariaDB suite was not executed because no `E0041_DB_*` environment was authorized and no local Docker, Podman or MariaDB tooling was available. No database connection or migration was attempted, and UEAnt was not used.

#### Documents Updated

- `02.CURRENT_STATUS.md`
- `03.ROADMAP.md`
- `04.DEVELOPMENT_LOG.md`

#### Repository State

PR #3 remains draft. E004.1 is not approved or merged; isolated MariaDB 10.4 validation and final independent review remain mandatory.

---
### Identity & Access Technical Closure

**Date:** 2026-07-31

**Milestone:** E004.1 — Identity & Access Baseline

**Status:** Technically approved; merge and controlled deployment pending

#### Result

- E004.1 passed two formal reviews.
- All BLOCKER and HIGH findings were corrected; no findings at those severities remain open.
- The three final MEDIUM observations in the MariaDB harness were corrected through UTC round-trip coverage, strict lock-error classification and safe partial-creation cleanup.
- Composer validation, strict optimized dev autoload, 29 local tests, three HTTP/real-session scenarios, lint of 126 tracked PHP files and diff checks pass.
- Full execution and continued refinement of the isolated MariaDB harness move to TEST-001 — MariaDB Integration Test Infrastructure.
- Separating TEST-001 prevents an infrastructure concern from indefinitely expanding the completed functional milestone and does not retroactively reopen E004.1.
- PR #3 is ready for merge; migration deployment remains a separate controlled operation.
- UEAnt was not used and migration 014 was not executed on an institutional or production database.

#### Repository State

E004.1 is technically approved. PR #3 may proceed to merge review without treating TEST-001 as a prerequisite. No merge or deployment was performed in this closure step.

---
### E005 Person Baseline — Phase 1 Documentation Alignment

**Date:** 2026-07-31

**Milestone:** E005 — Person Baseline

**Status:** Documentation synchronized; E005 remains in progress

#### Result

- E004.1 was merged into `main` through PR #3.
- E005 and its seven-phase sequence were approved.
- ADR-0018 approved the pre-production database rebaseline policy.
- Current status and planning documentation were synchronized.
- No code, migrations, seeders, configuration or database were modified.

---

### E005 Person Baseline — Phase 2 Technical Cleanup and Schema Rebaseline

**Date:** 2026-07-31

**Milestone:** E005 — Person Baseline

**Status:** Phase 2 completed; E005 remains in progress

#### Result

- The active migration sequence was rebuilt as a clean pre-production baseline under ADR-0018.
- Migrations 002–009 now declare the 36 approved functional tables; migration 001 and the runner-owned `migrations` table remain technical metadata.
- IdentityAccess remains aligned with the approved `users` schema.
- Incompatible legacy Person/Family controllers, services, repositories, views and routes were retired pending their approved milestone implementations.
- Seeders were reduced to approved status types and statuses. Institutional catalogs remain unseeded until their official master data exists, and optional administrator creation no longer invents a Person.
- Automated validation confirmed the exact 36-table inventory, column parity with DATABASE_DESIGN, absence of discarded legacy structures, application bootstrap, IdentityAccess behavior and HTTP authentication behavior.
- PHP lint, Composer validation, strict optimized autoload, 40 local tests and 3 HTTP/session scenarios passed.
- MariaDB execution was not attempted because no explicitly authorized disposable `E0041_DB_*` environment was available; UEAnt was not accessed.

#### Next Action

Phase 3 — Person Domain.

---

### E005 Person Baseline — Phase 3A Person Domain

**Date:** 2026-08-01

**Milestone:** E005 — Person Baseline

**Status:** Phase 3A implemented; E005 remains in progress

#### Result

- Implemented Person as an isolated Aggregate Root with no internal Entities.
- Added the approved PersonalName, Identification and ContactInformation Value Objects, plus typed Person identity and GENERAL_STATUS lifecycle.
- Enforced coherent required/optional name parts, complete optional identification, generic valid contact formats, positive Catalog references and non-future birth date.
- Added atomic identity replacement, optional contact replacement and activation/deactivation behavior.
- Added complete unit coverage for Person Domain; the accumulated local suite contains 55 passing tests plus the existing HTTP/session scenarios.
- Added no Repository, PDO, SQL, migration, seeder, application service, DTO, controller, HTTP route, view, authentication, Enrollment, Representative, Student or Family implementation.

#### Next Action

Perform technical review of Phase 3A and explicitly authorize the next E005 phase.

---

### E005 Person Baseline — Phase 3B Person Persistence

**Date:** 2026-08-01

**Milestone:** E005 — Person Baseline

**Status:** Phase 3B implemented locally; physical MariaDB validation pending; E005 remains in progress

#### Result

- Added the minimal PersonRepository contract for lookup by PersonId, lookup by Identification and save.
- Added PdoPersonRepository using ConnectionManager, prepared statements and the complete approved `persons` mapping.
- Person reconstruction validates that persisted statuses belong to GENERAL_STATUS and rejects unsupported codes.
- Save resolves exactly one GENERAL_STATUS row, persists optional fields as null, inserts a new Person without sending `id`, returns its database-generated AUTO_INCREMENT identity and updates existing rows without modifying `created_at` or starting internal transactions.
- Identification lookup delegates normalization to the physical `identification_key` expression; database uniqueness and referential errors remain visible.
- Added local SQLite persistence coverage. The accumulated local suite contains 63 passing tests plus the existing HTTP/session scenarios.
- Extended the disposable MariaDB harness with test-only technical Catalog values, real Person insert, ID and Identification lookup, update, null removal, GENERAL_STATUS mapping and normalized identification uniqueness.
- Composer validation, strict optimized autoload, PHP lint and diff checks pass. `composer test:mariadb` was not executed; no database connection, migration or UEAnt access occurred.
- Added no use case, DTO, application service, controller, HTTP route, view or implementation from Representative, Student, Family or Enrollment.

#### Next Action

Run the physical Person Persistence validation on the explicitly authorized XAMPP disposable environment and complete technical review before authorizing the next E005 phase.

---

### E005 Person Baseline — Phase 3B MariaDB Collation Alignment

**Date:** 2026-08-01

**Milestone:** E005 — Person Baseline

**Status:** MariaDB collation correction implemented locally; physical validation pending; E005 remains in progress

#### Result

- Diagnosed the Person identification lookup failure as a session/expression collation mismatch: the approved `persons` columns inherit `utf8mb4_unicode_ci`, while MariaDB 10.4 supplied `utf8mb4_general_ci` to the parameter-built expression because the PDO session declared only the character set.
- ConnectionFactory now establishes `utf8mb4` with `utf8mb4_unicode_ci` explicitly for MariaDB connections while preserving the UTC session convention.
- PdoPersonRepository compares the bare indexed `identification_key` with one prepared key parameter; no function or query-specific COLLATE is applied to the indexed column.
- Local SQLite coverage verifies case/space normalization, quoted values, prepared lookup shape and unchanged normalized uniqueness. The accumulated local suite contains 67 passing tests plus the existing HTTP/session scenarios.
- The disposable MariaDB harness verifies the session character set/collation and reports both session values plus the collations of `persons.document_number` and `persons.identification_key` if a collation error occurs, without exposing credentials.
- `composer test:mariadb` was not executed; no database connection, migration or UEAnt access occurred. Physical XAMPP validation remains pending and the phase is not approved.

#### Next Action

Run the physical Person Persistence validation on the explicitly authorized XAMPP disposable environment and complete technical review before authorizing the next E005 phase.

---

### E005 Person Baseline — Phase 3B Validation, Merge and Phase 3C Person Application

**Date:** 2026-08-01

**Milestone:** E005 — Person Baseline

**Status:** Phase 3B validated and merged; Phase 3C implemented locally; E005 remains in progress

#### Result

- PR #7 merged Person Persistence into `main` at `1c936ce` after 67 local tests, three HTTP/session scenarios and the authorized disposable XAMPP MariaDB 10.4 validation passed. All migrations and seeders, AUTO_INCREMENT Person identity, normalized identification lookup/uniqueness, update/null persistence and cleanup succeeded without accessing UEAnt or another persistent database.
- Added CreatePerson, GetPerson and UpdatePerson as separate application use cases depending only on PersonRepository.
- Added typed immutable CreatePersonInput, UpdatePersonInput and common PersonOutput DTOs containing only the approved Person data.
- Added specific Application errors for missing Person, identification owned by another Person and an invalid repository result without the expected persisted identity.
- Create and Update receive an explicit DateTimeImmutable current date, normalize it to a date before invoking Person and do not introduce a Clock abstraction.
- Identification availability is checked before save for clearer application feedback; retaining the current Person's identification is allowed and the database UNIQUE remains the final concurrency guarantee.
- Added a test-only in-memory repository and coverage for creation, complete output, generated identity, deterministic future-date rejection, lookup, complete update, optional-data removal, both status directions, duplicate handling, immutable identity and atomic domain failure without a partial save.
- The accumulated local suite contains 84 passing tests plus the existing three HTTP/session scenarios. Composer validation, strict optimized autoload, PHP lint and diff validation pass.
- No persistence, schema, migration, seeder, HTTP, authentication, IdentityAccess, Representative, Student, Family or Enrollment implementation changed. No MariaDB command was executed for Phase 3C.

#### Next Action

Complete technical review of Phase 3C and explicitly authorize the next E005 phase before adding any HTTP or delivery layer.

---

### E005 Person Baseline — Phase 3C Merge and Phase 4 Person Delivery

**Date:** 2026-08-01

**Milestone:** E005 — Person Baseline

**Status:** Phase 3C merged; Phase 4 implemented locally; E005 remains in progress

#### Result

- PR #8 merged Person Application into `main` at `9587d68`.
- Added the minimal White Label Person delivery: index, create, detail by positive ID and edit/update. No list, name search, pagination, delete, import, API or report flow was introduced.
- Added explicit temporary Person authorization for the existing authenticated `login_identifier = admin`. AuthenticationMiddleware remains first; authenticated non-admin users receive HTTP 403. No role, table, permission or authorization catalog was created.
- Added a single PersonController that adapts Request data to CreatePerson, GetPerson and UpdatePerson, validates basic HTTP shape, uses the existing CSRF and session contracts, preserves safe form input and maps known errors to non-internal messages.
- The update target is bound to server-side edit-session state so a changed hidden ID cannot select another Person.
- Added a specific PDO provider for active document types, sexes, marital statuses, education levels and GENERAL_STATUS ACTIVE/INACTIVE options. Missing required sex or status options disables persistence instead of inventing values.
- Added Person views and admin-only dashboard navigation without institution-specific names or a new visual framework.
- Added delivery coverage for authentication and admin authorization, navigation, create, detail, update, optional-data removal, ID tampering, CSRF, catalog absence, escaped output, safe errors, route middleware and absence of delete/SQL in HTTP.
- Composer validation, strict optimized autoload, 103 local tests, three HTTP/session scenarios, PHP lint and diff validation pass. The MariaDB harness was extended to validate physical form-option loading; its Phase 4 execution remains pending for the explicitly authorized disposable XAMPP environment. UEAnt was not accessed.
- Secretaría, DECE and Enfermería roles, User creation, Representative, Student, Family, Enrollment, import, reports and a definitive authorization model remain outside Phase 4.

#### Next Action

Complete technical review and merge of Phase 4. E005 remains open for the separately planned Representative and Student baseline and final validation.

---

### E005 Closure and E006 Representative & Student Baseline

**Date:** 2026-08-01

**Milestone:** E005 — Person Baseline / E006 — Representative & Student Baseline

**Status:** E005 completed; E006 Phase 1 established

#### Result

- PR #9 merged Person Delivery into `main` at `b95876e`.
- Person Domain, Persistence, Application and the minimal White Label administrator delivery are completed.
- The accumulated 103 local tests and three HTTP/session scenarios passed.
- The disposable MariaDB 10.4 validation passed for the clean baseline, Person persistence and active form-option loading; cleanup confirmed that no temporary database remained.
- E005 — Person Baseline is closed. Representative and Student are not carried as pending E005 phases.
- E006 — Representative & Student Baseline becomes the current functional milestone.
- E006 covers one stable Representative and one stable Student role per Person independently. Representative owns optional EmploymentInformation; Student owns institutional code and admission date. Both use ACTIVE/INACTIVE and support create, query and update.
- The approved sequence is documentation alignment, Representative Domain, Student Domain, Persistence, Application, administrator Delivery, and final validation/closure.
- E006 excludes Family and its memberships, Representative–Student association, User creation, bulk import, Representative Portal, Enrollment, Grade, Section, addresses, emergency contacts, authorized pickups, reports, definitive institutional roles, physical deletion, new Catalogs and unrequired schema changes.
- No code, test, migration, seeder, architecture, Domain Model, Database Design, ERD, Catalog or ADR changed during Phase 1.

#### Next Action

Review and merge the E006 Phase 1 documentation baseline before authorizing Phase 2 — Representative Domain.

---

### E006 Representative & Student Baseline — Phase 2 Representative Domain

**Date:** 2026-08-01

**Milestone:** E006 — Representative & Student Baseline

**Status:** Representative Domain implemented locally; technical review and merge pending

#### Result

- PR #10 merged the approved E006 scope, exclusions and phase sequence into `main` at `f004940`.
- Added the Representative Aggregate Root with optional persisted identity, mandatory immutable Person reference, optional EmploymentInformation and GENERAL_STATUS lifecycle.
- Added independent positive immutable RepresentativeId and Representative-owned PersonId Value Objects without coupling to Person Domain.
- Added immutable EmploymentInformation for optional occupation, company name, position, work phone and work email. Values are trimmed, empty strings become null, documented maximum lengths are enforced, email is validated and work phone remains free format.
- A completely empty EmploymentInformation is represented coherently as absence by the aggregate. Employment data is replaced as one value or removed; invalid replacement data leaves aggregate state unchanged.
- Added 14 unit tests covering identities, equality, exact limits, invalid email, optional absence, new and reconstructed aggregates, immutable identity and Person reference, replacement/removal, atomic failure, lifecycle and structural isolation.
- The accumulated local suite contains 117 passing tests plus the existing three HTTP/session scenarios.
- No Student, persistence, repository, SQL, migration, seeder, Application, DTO, service, HTTP, controller, route, view, authentication, authorization, import, Family or Enrollment implementation was added. No MariaDB command was executed.

#### Next Action

Complete technical review and merge of Phase 2 before explicitly authorizing Phase 3 — Student Domain.

---

### E006 Representative & Student Baseline — Phase 3 Student Domain

**Date:** 2026-08-01

**Milestone:** E006 — Representative & Student Baseline

**Status:** Representative Domain merged; Student Domain implemented locally; technical review and merge pending

#### Result

- PR #11 merged Representative Domain into `main` at `21e38a8` after its local validations passed.
- Added the Student Aggregate Root with optional persisted identity, mandatory immutable Person reference, required InstitutionalCode, required AdmissionDate and GENERAL_STATUS lifecycle.
- Added independent positive immutable StudentId and Student-owned PersonId Value Objects without coupling to Person or Representative Domain.
- InstitutionalCode trims boundary whitespace, rejects empty or values longer than 100 characters, preserves case and internal format, and compares by value. Its institutional uniqueness remains outside Domain.
- AdmissionDate receives both its value and an explicit current date, normalizes to midnight, accepts today, rejects future dates and compares by calendar value without reading the system clock.
- Implemented the approved administrative operation that atomically replaces InstitutionalCode and AdmissionDate while StudentId and PersonId remain immutable.
- Added 14 unit tests covering identities, required construction, code preservation and bounds, deterministic date behavior, new and reconstructed states, administrative replacement, atomic invalid input, lifecycle and structural isolation without Grade or Section.
- The accumulated local suite contains 131 passing tests plus the existing three HTTP/session scenarios.
- No persistence, repository, SQL, migration, seeder, Application, DTO, service, HTTP, controller, route, view, User, Family, Representative, Enrollment, Grade or Section implementation was added. No MariaDB command was executed.

#### Next Action

Complete technical review and merge of Phase 3 before explicitly authorizing Phase 4 — Persistence.

---

### E006 Representative & Student Baseline — Phase 4 Persistence

**Date:** 2026-08-01

**Milestone:** E006 — Representative & Student Baseline

**Status:** Student Domain merged; role Persistence implemented locally; MariaDB validation, technical review and merge pending

#### Result

- PR #12 merged Student Domain into `main` at `4e797f3`.
- Added minimal RepresentativeRepository and StudentRepository contracts using only their respective Domain types and exposing no list, delete or infrastructure representation.
- Added PdoRepresentativeRepository with prepared lookup by RepresentativeId and PersonId, optional EmploymentInformation reconstruction, exact GENERAL_STATUS resolution, AUTO_INCREMENT insertion, direct update and persisted-state verification.
- Added PdoStudentRepository with prepared lookup by StudentId, PersonId and direct indexed InstitutionalCode, strict UTC `Y-m-d` AdmissionDate reconstruction, exact GENERAL_STATUS resolution, AUTO_INCREMENT insertion, direct update and persisted-state verification.
- Neither repository modifies PersonId, opens an internal transaction, performs an existence probe, calculates identities or hides PDO integrity violations. Existing UNIQUE constraints and foreign keys remain the final concurrency and integrity guarantees.
- Added 24 local SQLite tests for contracts, lookups, complete/partial/absent reconstruction, ACTIVE/INACTIVE ownership and codes, invalid/future dates, generated identities, updates, zero-row identical state, disappearing targets, uniqueness, prepared queries, quoted codes and absence of internal transactions.
- The accumulated local suite contains 155 passing tests plus the existing three HTTP/session scenarios.
- Extended the disposable MariaDB harness with physical Representative and Student AUTO_INCREMENT, lookups, updates, nullable employment data, administrative Student data, exact GENERAL_STATUS, per-Person uniqueness, InstitutionalCode uniqueness and official-collation behavior.
- MariaDB validation was not executed because the required `E0041_DB_*` variables and explicit disposable authorization were not defined. No `.env`, UEAnt, persistent or institutional database was used.
- No Aggregate, Value Object, migration, schema, seeder, ConnectionFactory, Application, DTO, HTTP, User, Family, Enrollment, Grade or Section implementation changed.

#### Next Action

Run the Phase 4 validation on the explicitly authorized disposable XAMPP MariaDB environment, complete technical review and merge, then authorize Phase 5 — Application.

---

### E006 Representative & Student Baseline — Phase 5 Application

**Date:** 2026-08-01

**Milestone:** E006 — Representative & Student Baseline

**Status:** Role Persistence merged; role Application implemented locally; technical review and merge pending

#### Result

- PR #13 merged Representative and Student Persistence into `main` at `c960645` after its local and disposable MariaDB 10.4 validations passed and cleanup confirmed that no temporary database remained.
- Added CreateRepresentative, GetRepresentative and UpdateRepresentative using only PersonRepository and RepresentativeRepository contracts.
- Added CreateStudent, GetStudent and UpdateStudent using only PersonRepository and StudentRepository contracts.
- Added typed immutable inputs and outputs for both modules plus the approved specific not-found, missing-Person, per-Person duplicate, InstitutionalCode duplicate and invalid-persisted-result errors.
- Creation verifies the referenced persisted Person and prechecks one role per Person. Student creation and update also precheck InstitutionalCode ownership while database foreign keys and UNIQUE constraints remain final under concurrency.
- Representative replaces complete optional EmploymentInformation and maps completely empty input to absence. Student replaces complete InstitutionalCode and AdmissionDate, receiving an explicit current date normalized to midnight without reading the system clock or adding a Clock abstraction.
- Both modules apply ACTIVE or INACTIVE through Aggregate behavior, preserve immutable role and Person identities, propagate Domain invariants and require repository-generated persisted identities.
- Added two test-only in-memory role repositories and 36 Application tests covering valid creation, lookup, complete output, update, optional removal, existence, uniqueness, states, deterministic dates, atomic invalid input, generated identities and dependency isolation.
- `composer validate --no-check-publish`, strict optimized dev autoload, all 191 local tests, the three existing HTTP/session scenarios, PHP lint and `git diff --check` passed. No MariaDB command was executed for Phase 5.
- No Domain, PDO repository, ConnectionFactory, migration, schema, seeder, HTTP, User, Family, Enrollment, Grade, Section, import or report implementation changed.

#### Next Action

Complete technical review and merge of Phase 5 before explicitly authorizing Phase 6 — Administrator Delivery.

---

### E006.5 Family Membership Baseline — Phase 1 Documentation Alignment

**Date:** 2026-08-01

**Milestone:** E006.5 — Family Membership Baseline

**Status:** E006 Phase 5 merged; Family Membership documentation aligned locally; technical review and merge pending

#### Result

- PR #14 merged Representative and Student Application into `main` at `c687aa2`; E006 Phases 1–5 are completed and merged.
- Corrected the administrative planning boundary: Representative and Student are never associated directly. Family groups both roles through FamilyRepresentative and FamilyStudent.
- Recorded the approved path `Representative → FamilyRepresentative → Family → FamilyStudent → Student` and excluded any direct RepresentativeStudent table, Aggregate or use case.
- Documented the future composite creation of Person, Representative, Family and an active primary FamilyRepresentative, plus Person, Student and an active FamilyStudent. Application owns this orchestration even if Delivery later presents one form.
- Preserved the approved Family Membership invariants: at least one active representative, one active primary representative, active Family and Representative uniqueness, multiple active Family memberships for Representative, one active Family for Student, historical membership retention and no deletion of Person or role when a membership ends.
- Established E006.5 as an intermediate milestone between the merged E006 Phase 5 and the postponed Administrator Delivery, with separate Documentation, Domain, Persistence, Application, composite orchestration, Delivery integration, and validation phases.
- Limited the baseline to Family, FamilyRepresentative and FamilyStudent. Family resources, User, credentials, Representative Portal, Enrollment, Grade, Section, bulk import, reports, definitive institutional roles, physical deletion, schema changes, new catalogs and seeders remain excluded.
- Confirmed that RelationshipTypeId reuses the approved catalog and that the existing architecture, discovery, domain model, ADRs, catalogs, database design, ERD and migrations already support the planned membership boundary without requiring modification.
- Only `02.CURRENT_STATUS.md`, `03.ROADMAP.md` and this log changed. No code, tests, migrations or database command was executed.

#### Next Action

Complete technical review and merge of E006.5 Phase 1, then explicitly authorize Phase 2 — Family Membership Domain. Resume E006 Administrator Delivery only after E006.5 is completed.

---

### E006.5 Family Membership Baseline — Phase 2 Family Membership Domain

**Date:** 2026-08-01

**Milestone:** E006.5 — Family Membership Baseline

**Status:** Phase 1 merged; Family Membership Domain implemented locally; technical review and merge pending

#### Result

- PR #15 merged the approved E006.5 documentation baseline into `main` at `003c42c`.
- Added the Family Aggregate Root with optional persisted identity, required DisplayName, ACTIVE/INACTIVE status and private FamilyRepresentative and FamilyStudent collections.
- Implemented the approved atomic creation decision: every new Family starts with one unpersisted, active and primary FamilyRepresentative. No empty, draft or pending intermediate state exists, and no initial Student is required.
- Added FamilyRepresentative with optional identity, immutable RepresentativeId and RelationshipTypeId, primary flag, explicit start/end timestamps and active-state query. Added FamilyStudent with optional identity, immutable StudentId and the same temporal lifecycle.
- Added positive immutable FamilyId, FamilyRepresentativeId, FamilyStudentId, RepresentativeId, StudentId and RelationshipTypeId Value Objects, plus the required trimmed DisplayName with a 150-character limit.
- Reconstruction validates at least one active Representative, exactly one active primary Representative and no duplicate active Representative or Student within the Family. Historical ended memberships may coexist with later active memberships.
- Aggregate operations update DisplayName and lifecycle, add non-primary Representatives and Students, end only permitted memberships, expose active membership views and locate the active primary Representative. They preserve atomicity and do not implement primary replacement or cross-Aggregate rules.
- Membership timestamps are supplied explicitly, normalized to second precision without changing timezone and never read from the system clock. Ended memberships cannot reactivate or be ended twice.
- Added 29 Family Membership Domain tests covering typed identities, DisplayName, atomic creation, persisted reconstruction, Entity dates and termination, Representative and Student invariants, historical cycles, failure atomicity, collection isolation and forbidden dependencies.
- `composer validate --no-check-publish`, strict optimized dev autoload, all 220 local tests, the three existing HTTP/session scenarios, PHP lint and `git diff --check` passed. No MariaDB command was executed.
- No Persistence, repository, SQL, migration, schema, seeder, Application, DTO, service, HTTP, family resource, User, Enrollment or direct Representative–Student association was added.

#### Next Action

Complete technical review and merge of E006.5 Phase 2, then explicitly authorize Phase 3 — Family Membership Persistence. Resume E006 Administrator Delivery only after E006.5 is completed.

---

### E006.5 Family Membership Baseline — Phase 3 Family Membership Persistence

**Date:** 2026-08-01

**Milestone:** E006.5 — Family Membership Baseline

**Status:** Completed, physically validated and merged through PR #17

#### Result

- PR #16 merged Family Membership Domain into `main` at `9bf7a5c`; E006.5 Phases 1 and 2 are completed and merged.
- Added the minimal FamilyRepository contract with lookup by FamilyId, all Families with an active Representative membership, the single Family with an active Student membership, and save of the complete Aggregate.
- Added PdoFamilyRepository using one shared PDO connection and prepared fixed SQL for `families`, `family_representatives`, `family_students`, statuses and status types. It has no dependency on other repositories, Application or HTTP.
- Complete reconstruction loads every active and historical Representative and Student membership in stable `started_at`, `id` order, parses exact UTC second timestamps and delegates internal invariants to Family Domain without returning partial Aggregates.
- New Family persistence inserts the root and every membership with database-generated AUTO_INCREMENT identities, checks every affected row and reloads the complete persisted Aggregate. It does not insert generated active-key columns or technical timestamps.
- Existing Family persistence synchronizes DisplayName, GENERAL_STATUS, new memberships and permitted `ended_at` transitions. It preserves history and rejects omitted, duplicated, unknown, cross-Family, reactivated or immutably changed membership identities without DELETE, REPLACE or upsert.
- Save owns a local transaction only when no outer transaction exists. It commits or rolls back only its own boundary and otherwise participates without commit, rollback, savepoint, TransactionManager or Unit of Work.
- Representative membership lookup returns all complete Families deterministically and interprets active as `ended_at IS NULL`, independently of FamilyStatus. Student lookup returns null or one complete Family and reports multiple active results as persisted corruption.
- Added 26 SQLite tests covering the exact contract, active/inactive and historical reconstruction, invalid statuses and dates, corrupt Aggregate data, Representative and Student searches, AUTO_INCREMENT identities, atomic rollback, outer transactions, updates, immutable fields, omissions, ownership, zero-row comparisons, prepared SQL and dependency isolation.
- Extended the disposable MariaDB harness with Family AUTO_INCREMENT creation, active primary persistence, complete reload, Representative multi-Family lookup, Student active-Family lookup and later reassignment, history, active uniqueness, primary uniqueness, RelationshipType FK, UTC seconds, exact GENERAL_STATUS, immutable-field preservation and rollback without orphan Family rows.
- `composer validate --no-check-publish`, strict optimized dev autoload, all 246 local tests with 0 failures, the three existing HTTP/session scenarios, PHP lint and `git diff --check` passed.
- The authorized disposable MariaDB 10.4 validation passed after migrations 001–009 and seeders executed successfully.
- The physical runner reported `PASS MySQL Family atomic AUTO_INCREMENT creation and complete Aggregate reconstruction`, `PASS MySQL Family Representative and Student active lookups and historical membership`, `PASS MySQL Family physical uniqueness UTC status mapping and transactional rollback` and `PASS MySQL partial disposable database creation cleanup`.
- The first physical run failed only because the harness inspection connection inherited `@@session.time_zone = SYSTEM` and read `TIMESTAMP` through `@@system_time_zone = America/Bogota`. Production connections already applied `SET time_zone = '+00:00'` and PdoFamilyRepository already formatted writes in UTC, so no production correction was required.
- The harness inspection session was aligned to `+00:00`; the detailed timezone diagnostics and strict UTC comparison were retained, and the repeated physical validation passed.
- Partial disposable database creation cleanup and final cleanup passed. No `antares_e0065_family_tmp%` database remained.
- No Family Domain behavior, migration, schema, seeder, existing repository, ConnectionManager, Application, DTO, service, HTTP, family resource, User, Enrollment or direct Representative–Student association changed.

#### Next Action

PR #17 merged Family Membership Persistence into `main` at `8e659b5`. Phase 4 — Family Membership Application was then authorized. Resume E006 Administrator Delivery only after E006.5 is completed.

---

### E006.5 Family Membership Baseline — Phase 4 Family Membership Application

**Date:** 2026-08-04

**Milestone:** E006.5 — Family Membership Baseline

**Status:** Completed and merged through PR #18

#### Result

- PR #17 merged the physically validated Family Membership Persistence baseline into `main` at `8e659b5`; E006.5 Phases 1–3 are completed and merged.
- Added CreateFamily, GetFamily, GetFamilyMembership, AddRepresentativeToFamily, AddStudentToFamily, EndRepresentativeMembership and EndStudentMembership as focused Application use cases without a generic service or facade.
- Added immutable create, add and end input DTOs plus one complete FamilyOutput representation with nested Representative and Student membership outputs for active and historical state. No Domain Entity is exposed.
- Added the specific FamilyNotFound, RepresentativeNotFoundForFamily, StudentNotFoundForFamily, RelationshipTypeNotFound, StudentAlreadyHasActiveFamily and InvalidPersistedFamilyResult errors.
- Representative and Student existence is checked through their owner repository contracts before constructing the corresponding Family Value Objects. RelationshipType existence is checked through the minimal `RelationshipTypeLookup::exists(int): bool` port without a production PDO implementation or generic catalog abstraction.
- AddStudentToFamily rejects any existing active Family consistently, whether it is the same or another Family. Ended history permits a later membership cycle, while the physical uniqueness constraint remains the final concurrency guarantee.
- Membership start and end timestamps are explicit `DateTimeImmutable` inputs. Application does not read the system clock, change the received timezone or create a Clock abstraction; Domain preserves second precision and Persistence remains responsible for UTC conversion.
- Persisted membership verification locates the active membership by the added typed identity in the returned Aggregate and requires its generated ID, without relying on array order, consecutive IDs or initial values.
- Added one exact in-memory FamilyRepository, one minimal RelationshipTypeLookup fake and 32 Application tests covering valid and invalid creation, complete queries, external validation, active-Student protection, Representative and Student additions, membership termination, history, persisted identity guards, at-most-one save and dependency isolation.
- `composer validate --no-check-publish`, strict optimized dev autoload, PHP lint over all 26 changed PHP files, all 278 local tests with 0 failures, the three existing HTTP/session scenarios and `git diff --check` passed. No MariaDB command was executed because Phase 4 changes neither Persistence nor schema.
- Application opens no transaction and imports no PDO, concrete repository, Infrastructure, HTTP, Controller, Session, View or Person dependency. No Domain, Persistence, repository contract, migration, schema, seeder or HTTP code changed.
- Composite orchestration, Person/Representative/Student creation, external transactions, RelationshipType PDO integration, Delivery, User, Family resources, Enrollment, reports, primary replacement, Student transfer and direct Representative–Student association remain excluded.

#### Next Action

PR #18 merged Family Membership Application into `main` at `82accdd`. Phase 5 — Composite Administrative Orchestration was then authorized. Resume E006 Administrator Delivery only through the planned E006.5 delivery phase.

---

### E006.5 Family Membership Baseline — Phase 5 Composite Administrative Orchestration

**Date:** 2026-08-04

**Milestone:** E006.5 — Family Membership Baseline

**Status:** Phases 1–4 merged; composite orchestration implemented and validated locally and physically; technical review and merge pending

#### Result

- PR #18 merged Family Membership Application into `main` at `82accdd`; E006.5 Phases 1–4 are completed and merged.
- Added the minimal `TransactionRunner::run(callable): mixed` Core Application contract and `PdoTransactionRunner`, which uses the shared ConnectionManager connection, rejects a pre-existing transaction, checks begin/commit/rollback results, returns the exact callback result and rethrows the original failure after rollback.
- Added CreateRepresentativeFamily to coordinate CreatePerson, CreateRepresentative and CreateFamily. Family and its first active primary Representative membership remain atomically created by the existing CreateFamily case.
- Added CreateStudentInFamily to verify the target through GetFamily before any creation, then coordinate CreatePerson, CreateStudent and AddStudentToFamily without creating or transferring a Family.
- Both orchestrators require positive persisted identities between steps and return immutable composite DTOs that contain the existing PersonOutput, RepresentativeOutput or StudentOutput and FamilyOutput. They do not duplicate Aggregate construction, business validation or persistence logic.
- `today` is passed explicitly to Person and Student calendar validation. Membership `startedAt` is passed independently, preserves its received timezone and is never derived from `today` or the system clock.
- Business, Domain and persistence exceptions propagate unchanged. The transaction boundary has no retry, nested transaction, savepoint, Unit of Work or generic CompositeOperationFailed error.
- Added SQLite PdoTransactionRunner tests for execution inside the transaction, exact return value, commit, rollback, same-exception propagation, disappearing writes, pre-existing transaction rejection, ownership and absence of savepoints.
- Added a test-only snapshot TransactionRunner that restores the Person, Representative, Student and Family in-memory repositories after failure. Composite coverage verifies both complete successful flows and rollback for invalid Person, duplicate identification, role failures, invalid persisted results, RelationshipType, active Student Family, membership failures and simulated physical restrictions.
- Extended the disposable MariaDB harness with real PdoTransactionRunner, PDO repositories, existing use cases and both orchestrators. It verifies complete Representative and Student commits plus rollback of all inserted rows and exact preservation of an existing Family after a failure following FamilyStudent persistence.
- `composer validate --no-check-publish`, strict optimized dev autoload with 176 classes, PHP lint over all 17 changed PHP files, all 289 local tests with 0 failures, the three HTTP/session scenarios and isolation audits passed.
- The authorized disposable MariaDB 10.4 validation passed after migrations 001–009 and seeders executed successfully, including the clean 36-table baseline, approved statuses, AdminSeeder preservation, UTC User locking, concurrent row-lock classification and the existing Person, Representative, Student and Family persistence scenarios.
- The composite Representative scenario approved atomic creation and commit of Person, Representative, Family and its active primary membership. Its deliberate RelationshipType failure rolled back every inserted row without residual data.
- The composite Student scenario approved atomic creation and commit of Person, Student and FamilyStudent in an existing Family. Its deliberate post-persistence failure rolled back Person, Student and membership completely while preserving the exact pre-existing Family state.
- Partial-creation and final disposable cleanup passed. `SHOW DATABASES LIKE 'antares_e0065_orchestration_tmp%';` returned no rows, so no temporary database remained.
- The first physical attempt failed only because `admissionDate` in the new Student fixtures was later than the real UTC execution date. The harness alone was corrected to fixed `2026-08-01` admission dates with explicit `2026-08-04` validation date; repeated physical validation passed and production required no correction.
- No existing Domain, Application case, DTO, repository, Persistence, ConnectionFactory, ConnectionManager, migration, schema, seeder or HTTP implementation changed. User, Enrollment, Family resources, automatic Student transfer and direct Representative–Student association remain excluded.

#### Next Action

Complete technical review and merge of Phase 5, then explicitly authorize Phase 6 — Administrator Delivery Integration.

---

### E006.5 Family Membership Baseline — Phase 6 Administrator Delivery Integration

**Date:** 2026-08-09

**Milestone:** E006.5 — Family Membership Baseline

**Status:** Phases 1–5 merged; Administrator Delivery implemented and validated locally and physically; final review and merge pending

#### Result

- PR #19 merged the locally and physically validated Composite Administrative Orchestration into `main` at `f8fedec`; E006.5 Phases 1–5 are completed and merged.
- Added exactly six `/families` routes for the administrative entry, composite Representative/Family creation, complete Family detail and composite Student addition. AuthenticationMiddleware runs before the exact temporary `admin` authorization gate.
- Added FamilyController with explicit dependencies on CreateRepresentativeFamily, CreateStudentInFamily, GetFamily, the existing CSRF/session services and delivery option providers. POST handlers invoke only the approved composite orchestrators and do not own SQL, repositories or transaction boundaries.
- Added active RelationshipType lookup and Family form-option PDO adapters over the shared ConnectionManager. Options expose safe ordered labels and exact ACTIVE/INACTIVE FamilyStatus values; empty required catalogs fail closed.
- Added White Label composite forms for Person + Representative + Family and Person + Student + FamilyStudent, Family entry/detail/not-found views and authenticated dashboard navigation. No institution, User field, credentials, portal, generic administration resource or personal data absent from FamilyOutput was introduced.
- Every POST validates CSRF, scalar shape, positive identities, exact calendar dates, exact HTML timestamps, email, coherent document pairs, provider-backed catalogs and typed statuses. Entered scalar values are preserved safely and every rendered value is escaped.
- Student delivery stores the authoritative positive Family ID in session on GET, pulls it on POST and treats the hidden browser ID only as a consistency check. Expiry, invalidity or tampering never selects a target Family for orchestration.
- Known Person, Representative, Student and Family conflicts map to safe 422 or 404 responses. Unexpected Throwable, PDOException and RuntimeException are not caught; the existing composite transaction guarantees total rollback.
- Added Family Delivery tests for authorization, middleware order, navigation, both composite flows, CSRF, validation, catalog readiness, session-bound identity, known conflicts, rollback, safe 404, escaping, exact route scope and architecture boundaries. SQLite coverage validates both new PDO adapters, active filtering, stable ordering, empty catalogs and exact states.
- Updated the MariaDB harness to replace its test-only RelationshipType lookup with PdoRelationshipTypeLookup and to validate PdoFamilyFormOptionsProvider against active/inactive physical rows and exact Family statuses. Existing real repositories, PdoTransactionRunner, composite commit/rollback, UTC session and disposable cleanup coverage remain intact.
- `composer validate --no-check-publish`, strict optimized PSR dev autoload, PHP lint and `git diff --check` passed. All 302 local tests completed with 0 failures and the three HTTP/session scenarios passed.
- On 2026-08-09, the authorized disposable MariaDB 10.4 validation passed after migrations 001–009 and seeders executed successfully. The clean 36-table baseline, approved statuses, AdminSeeder preservation, UTC User locking, concurrent row-lock classification and the existing Person, Representative, Student and Family physical scenarios remained approved.
- PdoRelationshipTypeLookup and PdoFamilyFormOptionsProvider passed with physical catalog rows. Only active RelationshipTypes were exposed, inactive rows were excluded and the exact ACTIVE/INACTIVE FamilyStatus options were preserved.
- The Composite Representative flow physically committed Person, Representative, Family and its active primary membership, while the deliberate missing-RelationshipType scenario rolled back every partial row. The Composite Student flow physically committed Person, Student and FamilyStudent in the existing Family, while its deliberate failure rolled back every write and preserved the original Family.
- UTC session handling and all previous physical uniqueness, status and transactional restrictions remained approved. Partial disposable creation cleanup passed, final cleanup passed, and `SHOW DATABASES LIKE 'antares_e0065_delivery_tmp%';` returned no rows.
- No production correction was required as a result of the physical execution, and no credentials were recorded.
- No existing Domain, Application case, orchestration DTO, repository, Persistence implementation, TransactionRunner, ConnectionFactory, ConnectionManager, migration, schema or seeder changed. User, credentials, Enrollment, extra Family resources and direct Representative–Student association remain excluded.

#### Next Action

Complete final review and merge of Phase 6, then authorize Phase 7 — Validation and Closure.

---

### E006 and E006.5 — Validation and Closure

**Date:** 2026-08-09

**Milestone:** E006 — Representative & Student Baseline / E006.5 — Family Membership Baseline

**Status:** E006 and E006.5 completed

#### Result

- PR #20 merged the locally and physically validated Administrator Delivery Integration into `main` at `c7a4fe191c9bae215691a4777304c7085e2cb7e6`. Git and GitHub independently confirmed that PR #10 through PR #20 are merged into `main` in the approved E006/E006.5 sequence.
- The final audit ran against updated `main` and reviewed the applicable project context, architecture, discovery, Domain Model, decisions, catalogs, physical design, ERD, standards, security, database rules and workflow before evaluating the integrated implementation.
- Representative and Student retain independent immutable Person references, their approved Value Objects and ACTIVE/INACTIVE lifecycle, focused repositories and create/query/update Application cases. They duplicate no Person data and introduce no direct relationship, User, Enrollment, Grade or Section.
- Family remains the Aggregate Root for FamilyRepresentative and FamilyStudent history. Atomic creation, at least one active Representative, exactly one active primary Representative, active-member uniqueness, immutable membership fields, explicit dates and preserved history remain enforced without physical deletion or a RepresentativeStudent concept.
- Persistence uses database-managed AUTO_INCREMENT identities, `lastInsertId()`, fixed prepared statements, exact GENERAL_STATUS resolution and UTC sessions. Family save owns one transaction only when needed, participates in the orchestration transaction otherwise, preserves memberships, and adds no savepoint, Unit of Work or generic repository.
- Family Application still contains exactly seven approved non-orchestration use cases, and composite orchestration still contains exactly the two approved Representative and Student flows. `TransactionRunner` remains minimal; successful operations commit completely, failures roll back completely and propagate the original exception.
- Delivery still exposes exactly six protected `/families` routes. Authentication precedes the temporary exact `admin` gate, every POST uses CSRF, the Student target Family is bound to server-side session state, known failures are rendered safely, unexpected failures reach the global handler, and all output is escaped.
- Wiring resolves the shared ConnectionManager, PdoTransactionRunner, Person, Representative, Student and Family repositories, RelationshipType lookup, Family form options, both orchestrators, FamilyController and its middleware without manually created parallel PDO connections.
- The clean functional baseline remains 36 tables plus runner metadata, with only migrations 001–009. Representatives, students, families and their membership tables remain aligned with Database Design and ERD; MariaDB-managed generated active keys and their UNIQUE/FK guarantees remain intact, with no direct Representative–Student table.
- Security and White Label review approved authentication, temporary authorization, CSRF, prepared statements, server-side identity binding, output escaping, safe error boundaries and absence of mass assignment, credentials or institution-specific implementation. The exact `admin` gate remains an accepted MVP boundary, not the definitive Secretaría, DECE or Enfermería role model.
- The final local validation passed: `composer validate --no-check-publish`; optimized strict PSR dev autoload with 183 classes; 302 tests with 0 failures; three HTTP/session scenarios; PHP lint on all 114 required files; and `git diff --check`. The only warnings were non-blocking Git working-copy notices that LF will be converted to CRLF.
- No additional MariaDB closure run was executed because none of the required `E0041_DB_*` variables was defined. No `.env`, UEAnt, institutional or persistent database was used. The approved Phase 6 MariaDB 10.4 evidence remains the physical closure evidence: migrations 001–009 and seeders passed, catalog adapters and exact statuses passed, both composite commit and deliberate rollback scenarios passed, UTC and physical restrictions passed, partial and final cleanup passed, and no `antares_e0065_delivery_tmp%` database remained.
- The audit found no functional, architectural, persistence, security, test, schema or documentation inconsistency blocking closure. Excluded User/credentials, Representative Portal, definitive institutional roles, extra Family resources, Enrollment, Grade, Section, import, reports, notifications, jobs, events, functional auditing, physical deletion, Student transfer, primary replacement and generic Family administration remain outside these completed milestones.
- E006.5 is formally completed because Phases 1–6 are merged and coherent, Phase 7 approved the integrated baseline, current local validation passed and its physical MariaDB evidence remains valid. E006 is also formally completed because its Domain, Persistence and Application phases are merged, Administrator Delivery is satisfied through E006.5, this audit supplies Validation and Closure, and no in-scope functional phase remains.

#### Next Action

Review and merge the closure documentation. Because the current roadmap does not define an unequivocal next functional milestone after E006/E006.5, select and authorize it through a separate planning decision without expanding this closure phase.

---

### E007 Representative Access Baseline — Phase 1 Documentation and Functional Alignment

**Date:** 2026-08-09

**Milestone:** E007 — Representative Access Baseline

**Status:** Phase 1 documented; technical review and merge pending

#### Result

- PR #21 merged the E006/E006.5 Validation and Closure documentation into `main` at `94f87fba2ffbbc9a011aa9b0acd938f8ed24abb3`, formally completing both prior milestones before E007 began.
- Selected E007 as the current functional milestone and E008 Family Resources as the proposed next milestone after E007, without defining or authorizing the E008 baseline.
- Audited the current IdentityAccess, Person, Representative and Family implementation, transaction boundary, wiring, migration 005 and relevant tests without changing them.
- Confirmed that User currently supports authentication, status inspection, failed-attempt tracking, temporary locking and successful-access state over an existing persisted identity. Its constructor requires a positive UserId; UserId, PersonId, LoginIdentifier and PasswordHash are immutable, and no User provisioning or credential mutation use case exists.
- Confirmed that UserRepository currently provides lookup by login identifier, locked lookup by login identifier, lookup by UserId and save. PdoUserRepository has no INSERT or PersonId lookup and its UPDATE persists only failed-login, lock and last-access state.
- Confirmed that the current PasswordHasher contract verifies credentials but does not yet expose secure hash generation; that capability belongs to E007 Phase 2.
- Confirmed that UpdatePerson can replace or remove Identification through PersonRepository alone and currently has no User-aware synchronization or outer cross-Aggregate transaction.
- Approved UserId as the stable technical identity and the current Person DocumentNumber as the visible Representative LoginIdentifier. A Representative User requires complete Person Identification, and a DocumentNumber change must synchronize Person and User atomically without changing User identity, Person ownership, password, status, failed-login state or relationships.
- Assigned the synchronization to Application over one TransactionRunner boundary with explicit User Domain behavior and participating Person/User repositories. Controller SQL, Person persistence side effects and repository-owned business rules are prohibited; every failure rolls back both Aggregates.
- Preserved global normalized-login uniqueness with understandable prevalidation and the physical UNIQUE constraint as the final concurrency guarantee.
- Approved administrative provisioning for an existing Representative and manual initial or replacement passwords with a five-character minimum, secure hashing and no old-password, first-login change or activation requirement. Readable credentials are never persisted, returned, emailed, logged, reported or exported.
- Confirmed permanently that Student has no User and no Portal in the MVP. Representative Forgot Password remains approved but intentionally undefined until E007 Phase 6.
- Recorded ADR-0019 for Representative User identity and synchronized document-based login, aligned the discovery and Domain Model, clarified the minimum password policy in Security Guidelines, and established the seven E007 phases and Phase 2 planned scope.
- Confirmed that the existing physical schema supports the approved decisions without table, column, catalog or migration changes: users already has database-generated UserId, PersonId, LoginIdentifier, normalized LoginIdentifier, PasswordHash, status and the required per-Person and normalized-login UNIQUE constraints.
- No production code, Domain, Application, Persistence, HTTP, routes, views, tests, migrations or seeders changed. Composer tests and MariaDB were intentionally not executed because Phase 1 is documentation-only.

#### Next Action

Complete technical review and merge of E007 Phase 1, then explicitly authorize Phase 2 — Representative User Provisioning & Administrative Credential Management. Do not implement Phases 2–7 from this branch.

---

### E007 Representative Access Baseline — Phase 2 Representative User Provisioning & Administrative Credential Management

**Date:** 2026-08-10

**Milestone:** E007 — Representative Access Baseline

**Status:** Phase 1 merged through PR #22; Phase 2 implemented, locally validated and physically validated on disposable MariaDB 10.4; final review and merge pending

#### Result

- Confirmed PR #22 merged Phase 1 and ADR-0019 into `main` at `0f04952e5758cb1935efe456608869bfd1fc9433`; the existing users schema already supports Phase 2 and no migration, table, column, index, catalog, trigger or seeder changed.
- Extended User to distinguish new and persisted identities and to change LoginIdentifier or PasswordHash explicitly while preserving UserId, PersonId, status and authentication state. Existing authentication now rejects an unpersisted User without weakening lockout, session or status behavior.
- Extended UserRepository with Person lookup and persisted-Aggregate save semantics. PdoUserRepository now performs prepared AUTO_INCREMENT insertion, exact USER_STATUS resolution, complete reconstruction and approved-field updates while preserving PersonId, accepting only verified identical zero-row updates and participating in the existing outer transaction boundary.
- Extended PasswordHasher with secure generation through `password_hash(..., PASSWORD_DEFAULT)` and introduced the reusable five-character Representative password policy without additional composition rules.
- Implemented CreateRepresentativeUser and ChangeRepresentativeUserPassword with typed inputs, specific errors and a safe output that excludes plain text and PasswordHash. Provisioning validates Representative, Person, complete Identification, one User per Person and global normalized-login availability before deriving the username exclusively from DocumentNumber.
- Implemented UpdatePersonWithRepresentativeUserSync around the existing UpdatePerson and TransactionRunner. Optional Identification remains unchanged for Persons without a Representative User; Representative Users cannot lose complete Identification, and DocumentNumber/LoginIdentifier changes commit or roll back together while preserving User identity, credential, status and authentication state.
- Routed existing Person update Delivery through the synchronizer and added only the minimal White Label Representative User administration: GET manage, POST create and POST password replacement. All three routes use AuthenticationMiddleware followed by the existing exact `admin` gate; POSTs validate CSRF and bind RepresentativeId to server-side session state. Passwords are never restored, rendered, logged or stored in session.
- Added Domain/authentication, password policy/hasher, SQLite persistence, Application, orchestration, Delivery and Person Delivery coverage. The MariaDB harness now validates physical User AUTO_INCREMENT creation, findByPersonId, normalized and per-Person uniqueness, password hashing, exact USER_STATUS, password-change state preservation, atomic document/login synchronization, rollback, new-login authentication, old-login removal and existing admin compatibility.
- `composer validate --no-check-publish`, strict optimized dev autoload with 200 classes, all 341 local tests with 0 failures, the three HTTP/session scenarios, PHP lint over all 39 new or modified PHP files and `git diff --check` passed.
- On 2026-08-10, the authorized disposable MariaDB 10.4 validation passed after migrations 001–009 and seeders executed successfully.

##### Representative User provisioning

- Real INSERT omitted UserId and MariaDB generated a positive AUTO_INCREMENT identity. Complete reload, findByPersonId, normalized login, physically stored password hash and exact USER_STATUS passed.
- The physical constraints approved one User per Person and global normalized-login uniqueness.

##### Administrative password change

- Administrative replacement stored a new hash while preserving UserId, PersonId, LoginIdentifier, UserStatus, failed_login_attempts, locked_at and last_access_at exactly.

##### Person/User synchronization

- Person.DocumentNumber and User.LoginIdentifier changed atomically while preserving UserId, password, status and complete authentication state.
- A login conflict produced total rollback: Person retained its previous DocumentNumber and User retained its previous LoginIdentifier. A later simulated failure after the Person write also restored both persisted states.
- After commit, authentication succeeded through the new DocumentNumber and the previous login no longer resolved.

##### Compatibility

- AdminSeeder remained valid and the existing administrator User retained compatible authentication credentials and status.

##### Cleanup

- Partial disposable-database cleanup and final cleanup passed. `SHOW DATABASES LIKE 'antares_e007_phase2_tmp%';` returned no rows, and no production correction was required.
- Forgot Password, reset tokens, Representative Portal, Family Context Authorization, Family selector, Student User/Portal, general User listing, deletion, enable/disable, unlock, credential export/delivery, definitive institutional roles, generic RBAC, MFA and E007 Phases 3–7 were not implemented.

#### Next Action

Complete final technical review and merge of Phase 2, then explicitly authorize Phase 3 — Representative Access Resolution.

---

### E007 Representative Access Baseline — Phase 3 Representative Access Resolution

**Date:** 2026-08-10

**Milestone:** E007 — Representative Access Baseline

**Status:** Phase 2 merged through PR #23; Phase 3 implemented and validated locally and physically on MariaDB 10.4; final review and merge pending

#### Result

- Confirmed that PR #23 merged the locally and physically validated Phase 2 baseline into `main` at `878ddf9e481fc8f0a7ed47cf86b5d0731ffc9614` before creating `feature/e007-representative-access-resolution`.
- Added readonly `AuthenticatedRepresentative` with only UserId, PersonId, RepresentativeId and LoginIdentifier, and added `GetAuthenticatedRepresentative` under IdentityAccess Application.
- Implemented the exact server-side chain `Session authenticatedUserId → GetAuthenticatedUser → AuthenticatedUser.personId → RepresentativeRepository.findByPersonId()`. The use case accepts no client identity and converts the independent module PersonId values explicitly by scalar value.
- Preserved fail-closed behavior: no session, a deleted or disabled User, and an authenticated User without Representative return absence. A Representative without a persisted positive identity or reconstructed for another Person is rejected explicitly.
- Did not infer an ACTIVE/INACTIVE authorization rule for RepresentativeStatus because no official document defines one. Family membership authorization remains exclusively assigned to Phase 4.
- Registered the use case through the existing container wiring for `GetAuthenticatedUser` and `RepresentativeRepository`, without a new provider, repository, connection or middleware.
- Added `tests/RepresentativeAccessResolutionTest.php` and registered it in the local runner. Coverage confirms exact identity, server-derived Person lookup, safe absence, corrupt-result rejection, disabled/deleted User handling, no mutation, no external IDs, module isolation, container resolution, no Family dependency, no Representative session state and no Portal route.
- Extended the MariaDB harness to resolve an authenticated Representative through productive repositories, compare complete User and Representative rows strictly before and after resolution, and prove safe absence for a physically persisted enabled User whose Person has no Representative. Existing administrator compatibility and disposable cleanup remain part of the accumulated harness.
- Normalized line endings only while reading `routes/web.php` in the existing Representative User route test. This preserves every route and middleware assertion and removes a Windows CRLF-only false failure; no route changed.
- Local validation passed: `composer validate --no-check-publish`; optimized strict PSR dev autoload with 203 classes; 351 tests with 0 failures; three HTTP/session scenarios; PHP lint on every new or modified PHP file; and `git diff --check`.
- On 2026-08-10, the user executed the authorized disposable MariaDB 10.4 validation through `composer test:mariadb` with the `antares_e007_phase3_tmp` prefix. The execution completed without errors.
- The physical Phase 3 scenario approved the exact `Authenticated User → Person → Representative` resolution, including UserId, PersonId, RepresentativeId and the persisted LoginIdentifier. An authenticated User whose Person has no Representative produced safe absence.
- Complete User and Representative snapshots remained strictly identical before and after resolution. The capability was physically confirmed as read-only and produced no persisted mutation.
- The accumulated physical regression remained approved: migrations 001–009, the exact 36-table domain baseline plus migration metadata, approved status seed baseline, AdminSeeder compatibility, UTC locked-at persistence, concurrent User row locking, Person, Representative, Student and Family persistence, Family Delivery, both composite flows and all Representative User Phase 2 scenarios passed.
- Partial disposable-database creation cleanup and final cleanup passed. `SHOW DATABASES LIKE 'antares_e007_phase3_tmp%';` returned no rows, so no disposable database remained.
- No FamilyRepository, FamilyRepresentative, FamilyId, Family selection, session RepresentativeId, Infrastructure dependency, PDO use, SQL join, transaction, HTTP, Controller, route, view, Portal, migration, schema, catalog or seeder was added or modified. Phase 4 Family Context Authorization and Phases 5–7 remain excluded.

#### Files

- Created `app/IdentityAccess/Application/AuthenticatedRepresentative.php`.
- Created `app/IdentityAccess/Application/GetAuthenticatedRepresentative.php`.
- Created `tests/RepresentativeAccessResolutionTest.php`.
- Modified `bootstrap/app.php`, `tests/run.php`, `tests/MysqlMigrationIntegrationTest.php` and `tests/RepresentativeUserDeliveryTest.php`.
- Synchronized `.ai/02.CURRENT_STATUS.md`, `.ai/03.ROADMAP.md` and `.ai/04.DEVELOPMENT_LOG.md`.

#### Next Action

Complete final technical review and merge of Phase 3 through a Pull Request. Authorize Phase 4 — Family Context Authorization only after that merge.

---

### E007 Representative Access Baseline — Phase 4 Family Context Authorization

**Date:** 2026-08-10

**Milestone:** E007 — Representative Access Baseline

**Status:** Phase 3 merged through PR #24; Phase 4 implemented and validated locally and physically on MariaDB 10.4; final review and merge pending

#### Result

- Confirmed PR #24 merged the locally and physically validated Phase 3 baseline into `main` at `04ebf87da316bf2a0afe7d3da292151f8172a7de` before creating `feature/e007-family-context-authorization`.
- Audited FamilyRepository and PdoFamilyRepository before implementation. `findActiveByRepresentativeId()` selects only memberships with `ended_at IS NULL`, orders by FamilyId and intentionally does not filter FamilyStatus; SQLite coverage already confirms an INACTIVE Family with active membership remains in the result. This established semantic was preserved.
- Added `GetAuthorizedFamilies` to compose `GetAuthenticatedRepresentative` with FamilyRepository, explicitly convert RepresentativeId by value, validate positive persisted Family identities, reject duplicates or incoherent membership results, and return only FamilyId and DisplayName.
- Added `ResolveFamilyContext` with explicit zero/one/multiple behavior. Zero authorized Families fail closed without context, one auto-selects its only Family, and multiple require explicit selection unless the session proposal still belongs to the current authorized set. `isPrimary` is never used to choose a Family.
- Added `SelectAuthorizedFamily` for both initial selection and change. It accepts only FamilyId, re-resolves the authenticated Representative and current authorized set, rejects nonexistent, historical or other-Representative Families without changing session, and returns only the authorized context identity.
- Added readonly AuthorizedFamily, AuthorizedFamilySet, FamilyContext and RepresentativeFamilyAccess outputs. They expose no password, authentication state, Aggregate, membership Entity, Student data, historical membership or Family resources.
- Extended SessionManager minimally with `get()` and `remove()` while preserving `put()` and `pull()`. RepresentativeFamilyContextSession encapsulates the private `representative_family_context_id` key and stores only a positive integer FamilyId proposal.
- Every context read revalidates User, Representative and active FamilyRepresentative membership. Invalid or stale session values are removed; zero, one and multiple remaining-Family cases are reevaluated. PhpSessionManager removes the prior Family context before login regeneration so another User cannot inherit it, while unrelated session values remain untouched. Existing destroy behavior already clears the complete session on logout.
- Added `tests/FamilyContextAuthorizationTest.php` with 17 cases covering anonymous/admin absence, empty/one/multiple authorized sets, exact stable identities and names, INACTIVE Family inclusion, historical/other-Representative exclusion, corrupted repository results, automatic context, selection/change, invalid selection without session mutation, stale zero/one/multiple reevaluation, positive-only session state, login/logout isolation, architecture, wiring and safe output invariants.
- Extended the MariaDB harness with productive Repository and Application composition for one and two active Families, deterministic authorized sets, auto-context, selection and change, rejection of another Representative's Family, ended-membership stale invalidation, historical exclusion, administrator absence and strict before/after User, Representative, Family and FamilyRepresentative snapshots proving authorization itself is read-only.
- Registered the new cases through the existing container. No second ConnectionManager, provider, repository, middleware or service locator was introduced.
- Local validation passed: `composer validate --no-check-publish`; optimized strict PSR dev autoload with 212 classes; 368 tests with 0 failures; three HTTP/session scenarios; PHP lint over every new or modified PHP file; and `git diff --check`.
- On 2026-08-10, the authorized disposable MariaDB 10.4 execution of `composer test:mariadb` completed after migrations 001–009, StatusTypeSeeder, StatusSeeder and AdminSeeder. No credential was recorded.
- The accumulated Representative access scenario passed with `PASS MySQL authenticated Representative access resolution read-only identity and fail-closed behavior`.
- The Phase 4 scenario passed with `PASS MySQL Representative Family context authorization selection stale invalidation and read-only behavior`. Physical coverage approved one-Family auto-resolution, multiple-Family explicit selection, authorized selection and change, rejection of another Representative's Family, ended-membership stale invalidation, historical membership exclusion, administrator absence and current membership revalidation.
- Strict before/after User, Representative, Family and FamilyRepresentative snapshots confirmed that context authorization and session selection remained read-only for persisted Aggregates. The accumulated physical regression also remained approved across the 36-table baseline plus migration metadata, status seed baseline, AdminSeeder, UTC, Person, Representative, Student, Family, Family Delivery, both composite flows, Representative User provisioning, administrative password change and atomic DocumentNumber/LoginIdentifier synchronization and rollback.
- Partial disposable-database creation cleanup passed. Final cleanup was then confirmed manually: `SHOW DATABASES LIKE 'antares_e007_phase4_tmp%';` returned no rows, so no temporary database with that prefix remained.
- No RepresentativeStatus or FamilyStatus denial was added. No Family Domain, Family Persistence, Representative Domain, IdentityAccess Domain, HTTP, Controller, route, view, Portal, Enrollment, Family resource, transaction, migration, schema, catalog or seeder changed. Phases 5–7 remain excluded.

#### Files

- Created the Family authorization use cases, safe context DTOs, focused session-context helper and one specific Application exception under `app/IdentityAccess/Application`.
- Created `tests/FamilyContextAuthorizationTest.php`.
- Modified only SessionManager/PhpSessionManager for persistent safe context operations, `bootstrap/app.php`, `tests/IdentityAccessTest.php`, `tests/MysqlMigrationIntegrationTest.php` and `tests/run.php`, plus the three synchronized project-status documents.

#### Next Action

Complete final technical review and merge of Phase 4 through a Pull Request. Authorize Phase 5 — Representative Portal Entry only after that merge.

---

### E007 Representative Access Baseline — Phase 5 Representative Portal Entry

**Date:** 2026-08-10

**Milestone:** E007 — Representative Access Baseline

**Status:** Phase 4 merged through PR #25; Phase 5 implemented and validated locally; final review and merge pending

#### Result

- Confirmed PR #25 merged the locally and physically validated Phase 4 baseline into `main` at `7564bc1b870c17b81f63cb5f26ef37c55704c1b9` before creating `feature/e007-representative-portal-entry` from that exact commit.
- Audited the official E007 access chain, Phase 3 identity resolution, Phase 4 Family context authorization, routing, middleware, session, CSRF, view and bootstrap boundaries before implementation. No undocumented business or security rule was introduced.
- Added exactly two authenticated Portal routes: `GET /representative` for entry and `POST /representative/family` for CSRF-protected selection or change. Neither route uses the temporary administrator middleware.
- Added `RepresentativePortalController` as a thin Delivery adapter over `GetAuthenticatedRepresentative`, `ResolveFamilyContext` and `SelectAuthorizedFamily`. It contains no repository, PDO, SQL, transaction, Domain or persistence logic and accepts no client PersonId or RepresentativeId.
- Updated the authenticated root dispatch so a resolved Representative redirects to `/representative`; an authenticated User without Representative identity retains the existing administrator dashboard behavior.
- Implemented the approved zero/one/multiple states: no Representative or no authorized Family fails closed with a safe 403; one Family renders directly; multiple Families render only the current authorized set and require explicit selection. A valid selection or change redirects with 303.
- Preserved Phase 4 as the sole authorization authority. Every render and POST revalidates the complete User, Representative and active-membership chain. Invalid CSRF or input, nonexistent Family, another Representative's Family, historical membership and membership ended between render and submit fail closed without granting or retaining unauthorized context.
- Added three escaped White Label views for the Portal entry, no-Family state and forbidden state. They expose only safe Family display names and the existing logout action; no institution name, internal Representative/Person identity, Student data, Enrollment, Family resource, generic administration framework or Forgot Password mechanism was added.
- Added 16 focused Delivery tests covering exact routes and middleware, authentication redirects, administrator and former-Representative isolation, zero/one/multiple Families, escaping, authorized selection/change, invalid CSRF and scalar input, unauthorized/historical Families, stale zero/one/multiple and TOCTOU behavior, login routing, session regeneration isolation, architecture, wiring and excluded capabilities.
- Preserved all existing tests, including the three real HTTP/session scenarios. Local validation passed: `composer validate --no-check-publish`; optimized strict PSR dev autoload with 213 classes; 384 tests with 0 failures; three HTTP/session scenarios; PHP lint over every new or modified PHP file; and `git diff --check`.
- Did not execute `composer test:mariadb`. Phase 5 changes only HTTP/session Delivery and container wiring; no migration, schema, repository, Persistence implementation, PDO connection, MariaDB harness or physical invariant changed. The approved Phase 4 MariaDB 10.4 evidence therefore remains the applicable physical baseline.
- No Domain, Application use case, Persistence, repository contract, migration, seeder, schema, Family resource, Student, Enrollment, document acceptance, submission, report, import, payment, Student Portal or Forgot Password implementation changed or was added.

#### Files

- Created `app/IdentityAccess/Http/RepresentativePortalController.php` and the three focused views under `resources/views/representative-portal`.
- Created `tests/RepresentativePortalDeliveryTest.php`.
- Modified only the existing authentication root dispatch, route/container wiring, directly affected delivery/bootstrap test construction and obsolete phase-boundary assertions, plus the three synchronized project-status documents.

#### Next Action

Complete final technical review and merge of Phase 5 through a Pull Request. Authorize Phase 6 — Forgot Password only afterward and begin by discovering its still-unresolved mechanism.

---

### E007 Representative Access Baseline — Phase 6 Forgot Password Assistance & Representative Email Invariant

**Date:** 2026-08-10

**Milestone:** E007 — Representative Access Baseline

**Status:** Phase 5 merged through PR #26; Phase 6 implemented and validated locally and physically on disposable MariaDB 10.4; only final review and merge pending

#### Result

- Confirmed PR #26 merged Phase 5 Representative Portal Entry into `main` at `118747114e6534c0353d7cbde1ea951cd0b6b3f1` before creating `feature/e007-forgot-password-assistance` from that exact synchronized baseline.
- Approved ADR-0020 before productive implementation. Generic Person email remains optional, while every Person with Representative role must retain a valid `Person.ContactInformation.email` regardless of User existence. Optional `Representative.EmploymentInformation.workEmail` never satisfies the invariant, and `persons.email` remains physically nullable because the rule depends on the cross-Aggregate role.
- Added the specific `RepresentativeRequiresContactEmail` Application error. `CreateRepresentative` rejects missing personal email before role persistence, `UpdatePersonWithRepresentativeUserSync` rejects removal for a Representative with or without User before any write, and `CreateRepresentativeUser` fails closed when historical Representative data lacks email.
- Preserved all ADR-0019 behavior. A valid Representative email change leaves Representative identity and memberships unchanged and preserves UserId, PersonId, LoginIdentifier when DocumentNumber is unchanged, PasswordHash, UserStatus, failed attempts, lock and last access. Identification removal and atomic DocumentNumber/LoginIdentifier synchronization retain their prior protection and rollback behavior.
- Made personal email required only in the composite Representative/Family form. Delivery validates required trimmed email format, renders safe 422 errors and relies on `CreateRepresentative` as the final Application guarantee. A bypassed missing-email input rolls back Person, Representative, Family and FamilyRepresentative through the existing outer TransactionRunner.
- Added public read-only `GET /forgot-password` without authentication, POST or CSRF because it performs no state change. Login links to a minimal White Label page that asks the Representative to contact Secretaría and returns to login without requesting username, document or email.
- Assisted recovery is definitive for the MVP. Secretaría verifies identity externally and uses the existing admin-protected `ChangeRepresentativeUserPassword`; the public flow performs no User, Person or Representative lookup and cannot enumerate accounts.
- Added no mail integration, SMTP/provider abstraction, token, OTP, verification code, reset link, reset request persistence, automated password change, recovery-specific expiration or rate limiting. No readable password, credentials or account data is exposed or logged.
- Added focused Representative Application, composite orchestration/Delivery, Person update, Representative User provisioning/Delivery and Forgot Password Delivery tests. The matrix covers missing ContactInformation, phone-only data, work-email-only data, required form behavior, safe 422 mapping, total rollback, non-Representative optional email, Representative with and without User, valid replacement, complete User state preservation and historical provisioning rejection.
- Extended `MysqlMigrationIntegrationTest.php` to physically cover valid Representative creation with personal email, rejection before role persistence without it, work-email non-substitution, complete composite rollback, Representative email protection with and without User, valid Representative User email replacement with complete authentication-state preservation, defensive provisioning and accumulated regression/cleanup. No public Forgot Password HTTP scenario was added to MariaDB.
- Local validation passed: `composer validate --no-check-publish`; optimized strict PSR dev autoload with 214 classes; 395 tests with 0 failures; three HTTP/session scenarios; PHP lint over every new or modified PHP file; and `git diff --check`.
- The user executed the authorized `composer test:mariadb` validation against disposable MariaDB 10.4 using host `127.0.0.1`, port `3306`, user `antares_test`, prefix `antares_e007_phase6_tmp` and `E0041_DB_ALLOW_DISPOSABLE=1`. No password is recorded. Migrations 001–009, StatusTypeSeeder, StatusSeeder and AdminSeeder executed successfully, and the complete harness finished without failures.
- The accumulated physical regression approved authenticated Representative read-only identity resolution and fail-closed behavior; Family-context authorization, selection, stale invalidation and read-only behavior; the exact 36-table domain baseline plus migration metadata; status seeds; AdminSeeder credential/status preservation; UTC locked-at persistence; concurrent User row locking with MariaDB-specific error classification; and complete Person, Representative, Student, Family, Family Delivery and composite Student persistence behavior.
- Phase 6 specifically passed `Representative personal email invariant and work-email non-substitution`, `Composite Representative Person role Family atomic commit and rollback`, `Representative User email-gated AUTO_INCREMENT provisioning lookup hashing and physical uniqueness`, `Representative administrative password change preserves authentication state`, and `Representative email document login synchronization conflict rollback and authentication`.
- The physical evidence confirms that generic Person email remains optional; Representative requires valid personal email; `workEmail` does not satisfy the invariant; composite Representative/Family writes remain atomic; inconsistent provisioning fails closed while preserving database-generated identity, hashing and uniqueness; administrative password changes preserve authentication state; and email enforcement remains coherent with atomic DocumentNumber/LoginIdentifier synchronization, rollback and authentication. No schema, migration, seeder or Persistence correction was required.
- Partial harness cleanup passed, final manual cleanup passed and `SHOW DATABASES LIKE 'antares_e007_phase6_tmp%';` returned no rows. No temporary Phase 6 database remained.
- Public Forgot Password remains assistance-only through read-only `GET /forgot-password`: it accepts no username, document or email, performs no account lookup or state change and implements no automatic reset, mail, token, OTP, code or link. It required no independent MariaDB scenario because it has no Persistence access. Secretaría continues to use the existing administrative password change after external identity verification.
- No Person or Representative Domain, repository, PDO implementation, SQL join, migration, schema, table, column, index, FK, trigger, catalog, seeder, Family resource, Student, Enrollment, definitive institutional role, RBAC or Phase 7 capability was changed or added.

#### Files

- Created `app/Representative/Application/Exception/RepresentativeRequiresContactEmail.php`, `resources/views/auth/forgot-password.php` and `tests/ForgotPasswordAssistanceTest.php`.
- Modified only the directly affected Representative/IdentityAccess Application orchestration, existing Family/Person/Representative User Delivery, authentication route/view/controller, focused tests, MariaDB harness and the seven authorized documentation files.

#### Next Action

Complete final review of Phase 6, create/review and merge its Pull Request, and only after that merge authorize Phase 7 — Validation and Closure. Phase 7 remains planned, not started and not yet authorized.

---

### E007 Representative Access Baseline — Phase 7 Validation and Closure

**Date:** 2026-08-10

**Milestone:** E007 — Representative Access Baseline

**Status:** Completed; integrated baseline approved for formal closure

#### Result

- Confirmed that PR #27 merged Phase 6 into `main` at `23e32006af77c5649f36cabfd942ea44efde1193`. PR #22–#27 are merged and their merge commits are ancestors of the audited `main` baseline.
- Audited the complete server-side chain `User → Person → Representative → active FamilyRepresentative → authorized Family → Representative Portal`, including safe absence, persisted-identity coherence, current-membership revalidation and stale-context invalidation.
- Approved Representative User provisioning, database-generated stable UserId, one User per Person, document-derived globally normalized login, secure PasswordHash persistence and the exact five-character minimum without additional composition rules.
- Approved administrative password replacement with complete preservation of UserId, PersonId, LoginIdentifier, UserStatus, failed attempts, LockedAt and LastAccessAt; no readable credential is persisted, returned, logged, emailed, reported or exported.
- Approved the ADR-0019 atomic Person.DocumentNumber/User.LoginIdentifier synchronization, including complete Identification protection, one outer TransactionRunner boundary, unchanged identity/authentication state, conflict rollback and exclusive authentication through the new login after commit.
- Approved the ADR-0020 Representative personal-email invariant during role creation, composite creation, Person update with or without User and defensive User provisioning. Generic Person email remains optional and optional workEmail never substitutes personal email.
- Approved server-side Representative access resolution and Family authorization: historical membership does not authorize, client FamilyId never grants authority, zero/one/multiple states remain exact, session stores only the minimal FamilyId proposal and login regeneration prevents context inheritance.
- Approved the exact authenticated Portal routes `GET /representative` and CSRF-protected `POST /representative/family`, thin Delivery, escaped White Label views, safe errors, existing logout and isolation from administrative routes.
- Approved the definitive MVP Forgot Password assistance through public read-only `GET /forgot-password`: no account data, lookup, enumeration, state change, mail, token, OTP, code, reset persistence or autonomous password creation is present.
- Security and architecture remain aligned: `password_hash` with `PASSWORD_DEFAULT`, prepared PDO statements, generic authentication responses, ADR-0016 lockout, CSRF, session regeneration/destruction, server-side identities, output escaping, module boundaries, specific repositories and shared-connection TransactionRunner coordination all remain intact.
- White Label review found no institution-specific productive name, domain, email, catalog identity or functional rule. Schema/model parity remains at migrations 001–009, 36 functional tables plus migration metadata, database-generated UserId, physical per-Person and normalized-login uniqueness, exact USER_STATUS/GENERAL_STATUS and no recovery, alias, OTP or trigger additions.
- Confirmed that E007 added none of its approved exclusions: Student User/Portal, Family resources, Enrollment/Documents/Submission, annual information, Grade/Section administration, imports, reports, payments, definitive institutional RBAC, MFA, automated recovery, credential delivery/export or direct RepresentativeStudent association.
- Final local validation passed: Composer validation; strict optimized dev autoload with 214 classes; 395 tests with 0 failures; three HTTP/session scenarios; PHP lint on all 282 tracked PHP files; and `git diff --check`.
- All six `E0041_DB_*` variables were absent, so MariaDB was not rerun and no `.env`, UEAnt, institutional or persistent database was used. The approved disposable MariaDB 10.4 Phase 6 run remains applicable because the PR #27 head and merge tree have no product, Persistence, schema, migration, seeder, test or physical-harness delta after that execution.
- Reused physical evidence includes migrations 001–009, all seeders, Representative email enforcement, provisioning, password-state preservation, document/login synchronization, access resolution, Family context, accumulated persistence regression, partial cleanup and final cleanup. `SHOW DATABASES LIKE 'antares_e007_phase6_tmp%';` had returned no rows.
- Reconciled two obsolete pending-decision sentences in Domain Model and Enrollment Domain Discovery with the already approved ADR-0020 assisted-recovery decision; ADR-0019 and ADR-0020 themselves were not modified.
- No product code, Domain, Application, Infrastructure, HTTP, bootstrap, routes, views, tests, composer files, migration, seeder or physical schema changed during Phase 7.
- Formal decision: E007 can be closed. Phases 1–6 are merged, the integrated implementation satisfies ADR-0019/ADR-0020 and the approved access baseline, validation passes, physical evidence remains valid and no blocking incompatibility exists.

#### Next Action

Review and merge the E007 closure documentation. E008 — Family Resources remains only the proposed next milestone and requires a separate documentation and authorization decision; no E008 phase starts here.

---

### E008 Family Resources — Phase 1 Documentation and Functional Alignment

**Date:** 2026-08-10

**Milestone:** E008 — Family Resources

**Status:** Baseline established locally; disposable MariaDB 10.4 validation and merge pending

#### Context and Decision

- Confirmed that PR #28 merged the E007 Validation and Closure documentation into `main` at `0b6fdab202142237d83d30e72f8c9938685ab6da`; E007 is formally completed.
- Comparative planning after E007 selected Family Resources as the next functional milestone because reusable addresses, emergency contacts and authorized pickups are direct prerequisites for Representative Portal management, future Enrollment validation and the future Submission Snapshot.
- Authorized E008 with seven controlled phases and preserved a strict boundary: Phase 1 aligns documentation and the clean physical baseline; no Family Resources Domain, Persistence, Application or Delivery was implemented.
- Approved ADR-0021. FamilyAddress removes ProvinceId, CantonId and ParishId; MainStreet remains required, the other approved textual fields remain optional, and optional Geolocation retains the valid Latitude/Longitude pair and bounds.
- FamilyAddress no longer consumes geographic catalogs or requires geographic selectors. Province, Canton and Parish remain intact in the physical inventory and retain their own hierarchy.
- SubmittedAddressSnapshot now contains only Label and the address values that exist in FamilyAddress; its obsolete Province, Canton and Parish codes and names were removed without introducing derived values.
- ADR-0018 permits correction of the pre-production clean baseline before real data. No migration 010, ALTER TABLE, drop migration or backfill was created.

#### Baseline and Tests

- Synchronized Enrollment Domain Discovery, Domain Model, ADRs, Catalogs, Database Design and ERD.
- Updated migration 006 by removing only the three geographic FamilyAddress columns, their three indexes and their three FKs while preserving the Family/status relationships, unique `(id, family_id)` key and coordinate checks.
- Updated migration 009 only to keep the already-declared future `snapshot_addresses` table in parity with ADR-0021 by removing the six geographic code/name fields that no longer have a source.
- Added exact static checks for FamilyAddress and SubmittedAddressSnapshot columns, FamilyAddress keys and constraints, and the adjusted 60-FK baseline. The functional inventory remains exactly 36 tables.
- Extended the MariaDB harness only with structural inspection of exact address columns, absence of geographic FamilyAddress FKs/indexes and preservation of the coordinate checks. No FamilyAddress business scenario was added.
- Local validation passed with 214 optimized autoload classes, 396 tests with 0 failures and three HTTP/session scenarios; modified PHP lint and `git diff --check` passed during the phase workflow.
- All six `E0041_DB_*` variables were absent. MariaDB was not executed; no credentials were invented and no `.env`, UEAnt, institutional or persistent database was used. A future authorized run should use the disposable prefix `antares_e008_phase1_tmp`.

#### Approved E008 Phases

1. Documentation and Functional Alignment.
2. Family Resources Domain.
3. Family Resources Persistence.
4. Family Resources Application.
5. Administrator Delivery.
6. Representative Portal Integration.
7. Validation and Closure.

#### Exclusions

Enrollment and its lifecycle, annual information, Institutional Documents, DocumentAcceptance, Submission and snapshot implementation, autosave, review/completion operations, academic administration, reports, import, payments, Student access, definitive institutional RBAC, TEST-001 and geographic FamilyAddress selectors remain outside E008 Phase 1.

#### Next Action

Review the E008 Phase 1 baseline, execute the disposable MariaDB 10.4 validation when its explicit environment is available, and merge the future PR. Only after that merge may Phase 2 — Family Resources Domain be authorized.

---

### E008 Family Resources — Phase 1 MariaDB 10.4 Physical Validation

**Date:** 2026-08-10

**Milestone:** E008 — Family Resources

**Status:** Physical validation completed; final review, PR and merge pending

#### Result

- Executed the already-approved `composer test:mariadb` workflow against disposable MariaDB 10.4 with prefix `antares_e008_phase1_tmp`.
- Migrations 001–009, StatusTypeSeeder, StatusSeeder, AdminSeeder, the complete MariaDB harness and accumulated physical regression passed.
- Physical inspection confirmed that `family_addresses` contains exactly the fields approved by ADR-0021; ProvinceId, CantonId and ParishId and their retired indexes/FKs are absent.
- FamilyAddress preserves its Family and Status relationships, unique `(id, family_id)` key, optional Latitude/Longitude pair and the approved coordinate-pair, latitude-range and longitude-range checks.
- Physical inspection confirmed that `snapshot_addresses` is aligned with simplified FamilyAddress and contains no `province_code`, `province_name`, `canton_code`, `canton_name`, `parish_code` or `parish_name` columns.
- The physical baseline retains exactly 36 functional tables plus migration metadata and 60 FKs. Province, Canton and Parish remain present and preserve their internal hierarchy; they are only decoupled from FamilyAddress.
- No migration 010 exists. No Family Resources Domain, Persistence, Application or Delivery capability was implemented, and no Phase 2 resource was added under `app/Family`.
- Partial cleanup passed and final manual cleanup passed. `SHOW DATABASES LIKE 'antares_e008_phase1_tmp%';` returned no rows, confirming that no disposable database remained.
- E008 Phase 1 is implemented, documented, locally validated and physically validated. E008 remains in progress; Phase 2 — Family Resources Domain remains planned, not started and not authorized.

#### Next Action

Complete final review, create the E008 Phase 1 PR and merge the validated baseline. Only after that merge may Phase 2 be authorized.

---

### E008 Family Resources — Phase 2 Family Resources Domain

**Date:** 2026-08-10

**Milestone:** E008 — Family Resources

**Status:** Implemented and locally validated; review, PR and merge pending

#### Baseline and Scope

- Confirmed that PR #29 merged Phase 1 into `main` at `57a62415a6d292c87264617b20e1bf25be100bf9`; the Phase 2 branch was created from that exact clean baseline.
- Extended Family as the sole Aggregate Root with FamilyAddress, RepresentativeAddressAssignment, StudentAddressAssignment, FamilyEmergencyContact, EmergencyContactAssignment, FamilyAuthorizedPickup and AuthorizedPickupAssignment.
- Added separate positive nullable identities for all seven persisted Entities and a Family-local DocumentTypeId reference. Domain generates no identity and assumes no AUTO_INCREMENT sequence.
- Added AddressLabel, immutable simplified Address, Geolocation at DECIMAL(10,7) precision, FamilyResourceName, Family-local emergency/pickup contact information, PickupIdentification and EmergencyContactPriority. FamilyResourceStatus shares only the approved ACTIVE/INACTIVE resource semantics.
- Preserved ADR-0021: MainStreet is the only required address component; StreetNumber, SecondaryStreet, Sector, Reference and the complete Latitude/Longitude pair are optional; no Province, Canton or Parish concept entered Domain.

#### Aggregate Behavior and Invariants

- Family creates, updates, activates and safely deactivates resources; active assignments must be ended explicitly before resource deactivation. No hard delete or cascade was introduced.
- Address assignment replacement ends the previous distinct active assignment at the new explicit start instant before appending the replacement. Reassigning the same already-active resource is conservatively rejected to avoid artificial history.
- New assignments require a known active Family member and a known active resource in the same Aggregate. Address uniqueness, emergency Contact+Student uniqueness, positive optional Priority uniqueness and Pickup+Student uniqueness are enforced while valid ended history remains reusable.
- All assignments use explicit DateTimeImmutable values, normalize to seconds without changing timezone, keep identity/resource/member/start immutable, permit only null-to-ended transition and cannot reactivate.
- Family reconstruction accepts and clones all nine collections and fails closed on wrong Entity types, duplicate persisted identities, unknown references, inactive references for active assignments, duplicate active assignments, duplicate active Priority and invalid temporal history.
- Existing Family Membership creation, primary Representative, active uniqueness, history, lifecycle and collection encapsulation remain covered and passing.

#### Tests and Validation

- Added 31 focused Family Resources Domain tests covering Value Objects, schema limits, ADR-0021 exclusions, resource identity/lifecycle/update, sharing, replacement, membership and same-Family enforcement, temporal history, pickup identification pairing, reconstruction corruption and architecture boundaries.
- Local validation passed with 238 optimized autoload classes, 427 tests with 0 failures and three HTTP/session scenarios. Composer validation, complete modified-PHP lint and `git diff --check` passed.
- The installed primary `C:\xampp\php\php.exe` could not start because its deployment lacks required runtime files. Validation used the intact PHP 8.2.12 backup shipped in the same XAMPP installation with its own mbstring, OpenSSL and PDO SQLite extensions; no project or system runtime configuration was persisted.
- MariaDB was not executed because Phase 2 changes no Repository, Persistence implementation, migration, schema, seeder or physical harness. Phase 1 remains the approved physical baseline, and Phase 3 retains mandatory disposable MariaDB validation.
- No Application use case/DTO, HTTP/Delivery route/controller/view, IdentityAccess, Enrollment, Submission or external lookup was added.

#### Next Action

Review and merge the future Phase 2 PR. Only after that merge may Phase 3 — Family Resources Persistence be separately authorized.

---

### E008 Family Resources — Phase 3 Family Resources Persistence

**Date:** 2026-08-11

**Milestone:** E008 — Family Resources

**Status:** Implemented and validated locally and physically on disposable MariaDB 10.4; review, PR and merge pending

#### Baseline and Scope

- Confirmed that PR #30 merged Phase 2 Family Resources Domain into `main` at `19cdfd0781ba0dc9b8db6de1df9e8a780963befb`; the Phase 3 branch was created from that exact clean baseline.
- Preserved Family as the sole Aggregate Root and the exact four-method FamilyRepository contract. No repository per child Entity, Domain change, Application, Delivery, migration, schema, seeder or new table was introduced.
- Extended `PdoFamilyRepository` to persist and reconstruct FamilyAddress, RepresentativeAddressAssignment, StudentAddressAssignment, FamilyEmergencyContact, EmergencyContactAssignment, FamilyAuthorizedPickup and AuthorizedPickupAssignment together with existing Family membership history.
- All seven new Entity insertions omit `id`, require a positive database `lastInsertId()` and return the complete reconstructed Aggregate. No ID is calculated, no `MAX(id)` or sequence assumption is used, and no generated uniqueness column is written.
- Complete reads are deterministic: resources order by identity and temporal assignments by `started_at, id`. GENERAL_STATUS resolves exactly through StatusType/Status and persisted unsupported or wrong-type states fail closed. Timestamps are formatted and parsed as exact UTC seconds.

#### Synchronization and Transactions

- Update synchronization loads and indexes every persisted row. Persisted identities must exist and belong to the same Family, duplicate identities fail, and omitting persisted resources or assignment history is rejected. No DELETE, REPLACE, cascade synchronization or soft delete was added.
- Resource identity and Family ownership remain immutable while approved resource fields and ACTIVE/INACTIVE status update exactly. Assignment resource/member/start and emergency Priority remain immutable; `ended_at` permits only null-to-ended or identical persisted state, never reactivation or changed history.
- Resources are written before their assignments. The repository owns one transaction only when no outer transaction exists and otherwise participates without commit, rollback, nested transaction, savepoint or retry.

#### Tests and Validation

- Added `FamilyResourcesPersistenceTest.php` and extended the faithful SQLite Family schema with all seven tables, composite ownership, active partial uniqueness and applicable checks. Coverage includes atomic creation, complete roundtrip, AUTO_INCREMENT gaps without sequence assumptions, UTF-8/apostrophes, exact Geolocation, statuses, updates, temporal ending, omission, unknown/cross-Family identity, disappearing rows, corrupted status, immutable assignments, identical writes and owned/outer rollback.
- Extended the MariaDB harness to inspect AUTO_INCREMENT on all seven physical tables and exercise complete productive roundtrip, exact UTC seconds, generated active uniqueness, composite same-Family FK, pickup/priority checks, owned rollback and outer rollback. Existing migration, seeder and cleanup protections remain unchanged.
- Composer validation and strict optimized dev autoload passed with 238 classes. The complete local suite passed with 437 tests, 0 failures and three HTTP/session scenarios; modified PHP lint and `git diff --check` passed.
- Executed the approved `composer test:mariadb` workflow on disposable MariaDB 10.4. Migrations 001–009, StatusTypeSeeder, StatusSeeder and AdminSeeder completed successfully.
- The exact `PASS MySQL Family Resources complete roundtrip AUTO_INCREMENT UTC constraints and rollback` scenario passed together with the accumulated physical regression. Physical validation confirmed complete Family Resources persistence/reconstitution, database-generated AUTO_INCREMENT identities, UTC, generated uniqueness, composite ownership, checks, owned rollback and outer rollback.
- Partial cleanup passed and final manual cleanup passed. `SHOW DATABASES LIKE 'antares_e008_phase3_tmp%';` returned no rows, so no disposable E008 Phase 3 database remains. No secret or credential is recorded.
- Phase 3 has no remaining technical validation. It is pending only final review, PR and merge; E008 remains in progress and Phase 4 remains planned, not started and unauthorized.

#### Next Action

Complete final Phase 3 review, create its PR and merge the locally and physically validated implementation. Phase 4 — Family Resources Application remains planned, not started and is not authorized by this phase.

---

### E008 Family Resources — Phase 4 Family Resources Application

**Date:** 2026-08-11

**Milestone:** E008 — Family Resources

**Status:** Phase 3 merged through PR #31; Phase 4 implemented and validated locally; final review, PR and merge pending

#### Baseline and Scope

- Confirmed that PR #31 merged the locally and physically validated Phase 3 Family Resources Persistence into `main` at `913b2c25418ce91be69acb54a62bfd379dff57d4`; the Phase 4 branch was created from that exact synchronized clean baseline.
- Added `GetFamilyResources` plus 20 explicit mutable cases: create/update/activate/deactivate for FamilyAddress, FamilyEmergencyContact and FamilyAuthorizedPickup; assign/end Representative and Student address history; and assign/end emergency-contact and authorized-pickup history.
- Added 20 readonly typed intention inputs and immutable scalar outputs for Family identity/display plus all seven resource and assignment collections, including active and ended history, positive persisted IDs and explicit `DateTimeImmutable` timestamps. Existing `FamilyOutput` remains unchanged.
- Preserved Family as the sole Aggregate Root. Application loads Family, constructs approved Value Objects, invokes one Domain operation, performs exactly one `FamilyRepository::save()` on success, validates the same FamilyId plus the requested persisted child/state and returns no Domain Entity or mutable collection.

#### Reference Validation and Boundaries

- Reused the existing minimal `RelationshipTypeLookup` and existing `RelationshipTypeNotFound` error for emergency contacts and authorized pickups.
- Added only `DocumentTypeLookup::exists()` and `DocumentTypeNotFound` for optional pickup identification. Phase 4 includes a test fake but no PDO adapter or productive bootstrap registration; that composition dependency remains for Phase 5 Delivery.
- All mutation times are explicit inputs. No implicit clock, `now`, date helper, session, authenticated Representative, authorization rule, TransactionRunner, cross-Aggregate write or Representative/Student repository was introduced.
- Domain continues to own all invariants: valid geolocation pair/bounds, membership and same-Family references, active-resource requirements, address replacement/history, emergency priority uniqueness, pickup identification pairing and explicit assignment ending. Domain exceptions propagate unchanged.
- No Family Domain, Family Persistence, repository contract, migration, seeder, schema, MariaDB harness, HTTP, controller, route, view, IdentityAccess, Enrollment, Submission or Delivery implementation changed.

#### Tests and Validation

- Added 10 focused Application scenarios covering complete query output and missing Family, address CRUD/lifecycle and Representative/Student replacement history, emergency and pickup CRUD/lifecycle/assignment flows, external lookup failures, Domain invariant propagation, explicit second-precision times, one-save/zero-save guarantees and safe unwired architecture.
- Extended the exact in-memory FamilyRepository to generate non-sequential identities and reconstitute all seven child collections. Negative matrices reject a wrong returned FamilyId, every child category without persisted identity and a repository result that omits the requested mutation.
- Composer validation and strict optimized dev autoload passed with 291 classes. The complete local suite passed with 447 tests, 0 failures and three HTTP/session scenarios; complete modified-PHP lint and `git diff --check` passed.
- `composer test:mariadb` was not executed because Phase 4 changes neither Persistence nor schema and adds no productive reference adapter. The PR #31 Phase 3 MariaDB 10.4 evidence remains the approved physical baseline.

#### Next Action

Complete final Phase 4 review, create its PR and merge the locally validated implementation. Phase 5 — Administrator Delivery remains planned, not started and must be separately authorized; it must supply productive DocumentType lookup composition before wiring pickup create/update cases.

---

### E008 Family Resources — Phase 5 Administrator Delivery

**Date:** 2026-08-11

**Milestone:** E008 — Family Resources

**Status:** Phase 4 merged through PR #32; Phase 5 implemented and validated locally and physically on disposable MariaDB 10.4; final review, PR and merge pending

#### Baseline and Scope

- Confirmed that PR #32 merged Phase 4 Family Resources Application into `main` at `57aa645acfd1197828e2658f4aa4a39afdd0cd50`; the Phase 5 branch was created from that exact synchronized clean baseline.
- Added the White Label administrator resource page linked from Family detail, with addresses, Representative/Student address assignments, emergency contacts, authorized pickups and complete active/ended history. The view escapes all output, needs no JavaScript and contains no institution-specific identity.
- Added exactly one GET and 20 explicit POST routes protected in order by AuthenticationMiddleware and the existing FamilyAdministrationMiddleware temporary `admin` gate. No Representative Portal route or definitive RBAC was added.
- Bound every POST to the positive FamilyId pulled from `_family_resources_trusted_family_id`; browser FamilyId is only a tamper check. Child/resource selections are revalidated against current `GetFamilyResources`/`GetFamilyMembership` outputs and are never stored as session authority.
- Reused all 21 Phase 4 cases. Controller code performs only scalar/format/catalog validation, CSRF, safe error mapping and PRG; it contains no PDO, SQL, FamilyRepository, Domain construction, transaction or business rule.

#### Catalogs, Security and Boundaries

- Added prepared `PdoDocumentTypeLookup` plus `PdoFamilyResourceFormOptionsProvider` backed by the shared ConnectionManager. RelationshipType and DocumentType options include only active rows ordered by name/id and expose only typed id/code/name values.
- Empty RelationshipType options disable dependent forms. Empty DocumentType options still permit an Authorized Pickup without identification; document type and number remain an all-or-nothing pair when supplied. Pickup assignment does not require identification because that future Submission rule remains excluded.
- All 20 mutations require CSRF before a write, accept explicit `datetime-local` values interpreted in UTC, preserve safe scalar values on recoverable errors and return safe 404/422 responses for documented failures. No credentials, status physical IDs, Enrollment, Submission, hard delete, AJAX, geographic selectors, new middleware or extra session authority were introduced.
- Domain, `FamilyRepository`, `PdoFamilyRepository`, schema, migrations and seeders remain unchanged. The MariaDB harness changed only to validate the new productive lookup/options adapters and one real Family Resource Application write through existing persistence.

#### Tests and Validation

- Added focused Delivery coverage for exact routes/middleware, admin denial, trusted Family context, tampering, expired/deleted context, CSRF across all resource groups, active catalogs, safe validation/errors, escaping, empty catalogs, every address/contact/pickup lifecycle command, assignments and ended history.
- SQLite adapter coverage approves active/inactive/unknown/non-positive DocumentType lookup, prepared SQL, propagated PDO failure, ordered exact options and empty results. Existing Phase 4 tests were updated only from intentionally unwired to productively wired status and existing Family Delivery route/view counts were expanded for the approved scope.
- Composer validation and strict optimized dev autoload pass with 297 classes. The complete local suite passes with 456 tests, 0 failures and three HTTP/session scenarios; complete modified-PHP lint and `git diff --check` pass.
- The authorized `composer test:mariadb` run used a disposable MariaDB 10.4 database prefix `antares_e008_phase5_tmp`. Migrations 001–009, StatusTypeSeeder, StatusSeeder and AdminSeeder completed, followed by the accumulated physical regression.
- The exact `PASS MySQL Family Resources complete roundtrip AUTO_INCREMENT UTC constraints and rollback` and `PASS MySQL Family delivery active catalogs and Application persistence` scenarios passed. The evidence confirms the productive DocumentType lookup behavior covered for active, inactive and unknown values, active RelationshipType/DocumentType options in defined order, real Administrator Delivery/Application integration through `PdoFamilyRepository`, complete Family Resources creation/persistence/reconstruction, AUTO_INCREMENT, UTC, physical constraints and rollback.
- The harness reported `PASS MySQL partial disposable database creation cleanup`. A residual temporary database from an identity scenario was detected and removed manually. The final `SHOW DATABASES LIKE 'antares_e008_phase5_tmp%';` returned no rows; physical cleanup is complete and no Phase 5 temporary database remains.
- Phase 5 has no remaining technical validation. It is pending only final review, PR and merge; E008 remains in progress and Phase 6 remains planned, not started and unauthorized.

#### Next Action

Complete final review, create the Phase 5 PR and merge the locally and physically validated implementation. Phase 6 — Representative Portal Integration remains planned, not started and not authorized.

---

### E008 Family Resources — Phase 6 Representative Portal Integration

**Date:** 2026-08-11

**Milestone:** E008 — Family Resources

**Status:** Phase 5 merged through PR #33; Phase 6 implemented and validated locally; review, PR and merge pending

#### Baseline and Scope

- Confirmed that PR #33 merged the locally and physically validated Phase 5 Administrator Delivery into `main` at `7f38ffb5da5a7e7716bad63b5bec5cd0af6446a9`; the Phase 6 branch was created from that exact synchronized clean baseline.
- Added a separate White Label Representative Family Resources controller and page, linked from `/representative` only when an effective Family context exists. Exactly one GET and 20 explicit POST routes under `/representative/resources` use AuthenticationMiddleware without the temporary administrator gate.
- Reused all 21 Phase 4 Application cases, `GetFamilyResources`, `GetFamilyMembership`, the existing active resource form options and E007 `ResolveFamilyContext`. No new Family Resource use case, repository, transaction boundary, role model or business rule was introduced.

#### Authorization and Delivery

- Every GET and POST revalidates the full E007 server-side User → Person → Representative → active FamilyRepresentative → selected Family chain. Session retains only `representative_family_context_id` as the existing revalidated selection proposal; no PersonId, RepresentativeId, StudentId, child ID, DTO, Aggregate or resource list is stored.
- Every POST compares its positive hidden FamilyId with the freshly resolved effective Family before processing any child/resource ID. A context switch or stale membership fails closed without Application mutation, even when child IDs coincide numerically across Families.
- Representative address assignment and ending always derive RepresentativeId from the current context. The page renders only the actor's own address history, ignores browser attempts to choose another Representative and never exposes another Representative's assignment or private Person data.
- The Portal rejects update or deactivation of an Address with an active assignment to another Representative using the generic approved message. This Delivery authorization check does not alter Domain invariants or disclose who uses the Address.
- Related Students come only from active FamilyStudent memberships in the current Family. GetStudent then GetPerson supplies escaped human labels after membership authorization; historical and other-Family Students cannot receive address, emergency-contact or pickup operations.
- Address, emergency-contact and authorized-pickup resource lifecycle and assignment commands reuse the approved active RelationshipType/DocumentType options, explicit timestamps and optional paired pickup identification. Every mutation uses CSRF and PRG; known failures map to safe messages and unexpected failures remain visible to the global handler.
- Output escapes Family, Student and resource values, requires no JavaScript and remains institution-neutral. Administrator routes and their temporary gate remain unchanged; Phase 6 adds no Enrollment, Submission, personal-information editing, geographic selectors, hard delete or new session authority.

#### Tests and Validation

- Added 11 focused scenarios covering the exact routes/middleware, full E007 entry matrix, stale anti-TOCTOU context with colliding IDs, self Representative authority/history/privacy, protection of another Representative's active Address, active related Student labels and boundaries, every resource/assignment family, active catalogs, CSRF, Family switching, session isolation, admin isolation, escaping and architecture exclusions.
- Existing E007 Representative Portal, Family Context Authorization, administrator Family Resources Delivery/Application/Persistence, login/logout, Person Delivery and Representative User regressions remain passing.
- Composer validation passed. Strict optimized dev autoload generated 299 classes. The complete local suite passed with 467 tests, 0 failures and three HTTP/session scenarios; all new/modified PHP files passed lint and `git diff --check` passed.
- MariaDB was not rerun because Phase 6 changes only Delivery, routes, bootstrap composition and tests. It adds no Persistence, PDO adapter, schema, migration, seeder or physical harness delta; the merged Phase 5 disposable MariaDB 10.4 evidence remains applicable.
- Phase 7 — Validation and Closure was not started and remains unauthorized.

#### Next Action

Complete final review, create the Phase 6 PR and merge the locally validated implementation. Only after that merge may Phase 7 — Validation and Closure be separately authorized.

---

### E008 Family Resources — Phase 6.1 Representative Portal Authorization Correction

**Date:** 2026-08-12

**Milestone:** E008 — Family Resources

**Status:** Phase 6 merged through PR #34; corrective Phase 6.1 implemented and locally validated; review, PR and merge pending

#### Audit Blockers and Scope

- Confirmed that PR #34 merged Phase 6 into `main` at `166788e037fc96f8f336c980142d622ae2a7aee1`. The first Phase 7 audit did not authorize E008 closure.
- The audit found two blockers: contextual authorization and safe-history projection were implemented in `RepresentativeFamilyResourceController`, contrary to Architecture and ADR-0011; and the route test normalized CRLF only for some assertions, causing 467 tests with one failure on Windows.
- Phase 6.1 changes no business rule, Domain, FamilyRepository, Persistence, PDO adapter, schema, migration, seeder, physical harness, route, administrator authorization or session authority.

#### Application Authorization Boundary

- Added `Family/Application/RepresentativeResources` as an explicit orchestration/authorization layer over E007 `ResolveFamilyContext` and the existing 21 Phase 4 Family Resource cases. It is not a new bounded context and does not copy Aggregate mutation behavior.
- `GetRepresentativeFamilyResources` takes no actor/browser input. It fails closed without an effective authorized Family, loads Family resources and membership, derives only active FamilyStudent identities before GetStudent/GetPerson label resolution, and returns a readonly filtered Portal projection.
- The projection exposes only authorized Family display/resources, active Student options, own Representative Address history and assignment histories for authorized Students. Other Representative history and unrestricted membership arrays never reach Controller or View.
- Three capability services cover Address, Emergency Contact and Authorized Pickup. Every command re-resolves context, compares `expectedFamilyId` with the effective Family, authorizes child/resource identity, derives self Representative internally where required and delegates to the existing Phase 4 input/case.
- Address update/deactivation now checks active use by another Representative in Application and preserves the same generic non-enumerating external message. Domain continues to own lifecycle, uniqueness, temporal, same-Family, priority and pickup-identification invariants.
- The Controller now contains only request parsing, structural validation, CSRF, active form-option handling, Application invocation, PRG, rendering and safe exception-to-HTTP mapping. CSRF remains before Application authorization; the session still stores only E007 `representative_family_context_id` as a revalidated Family proposal.

#### Tests and Validation

- Added 13 focused Application authorization scenarios for missing actor/Family/selection, safe read filtering, selected/stale/colliding Family context, self Representative, other-Representative protection, active/historical/other-Family Students, inaccessible resources and complete authorized Address/Emergency/Pickup delegation.
- Preserved the 11 Representative Delivery scenarios for exact routes/middleware, CSRF, PRG, E007 context, stale forms, cross-Family collisions, privacy, escaping, admin isolation and safe errors. Delivery architecture assertions now require only the new Application boundary and prohibit authorization helpers/loaders in the Controller.
- The route test normalizes line endings once immediately after reading and verifies identical behavior for LF and CRLF. It still requires exactly 21 Representative endpoints, 21 AuthenticationMiddleware occurrences and absence of administrator middleware.
- Strict optimized dev autoload generates 312 classes. The complete local suite passes with 480 tests, 0 failures and three HTTP/session scenarios. Full lint passes for all 388 existing tracked/new PHP files, and the working diff check passes.
- MariaDB was not rerun because there is no Domain, Persistence, PDO adapter, schema, migration, seeder or physical-harness delta. The approved Phase 5 disposable MariaDB 10.4 evidence and complete cleanup remain applicable; no `.env`, UEAnt or institutional database was used.
- E008 remains in progress. Phase 7 has not been rerun and must be repeated only after Phase 6.1 is reviewed and merged. E009 remains not started.

#### Next Action

Review and merge Phase 6.1 through a separate Pull Request, then authorize a fresh Phase 7 Validation and Closure audit. Do not close E008 or start E009 in this corrective phase.

---

### E008 Family Resources — Phase 7 Validation and Closure — Second Audit

**Date:** 2026-08-12

**Milestone:** E008 — Family Resources

**Status:** Integrated baseline approved and formally closed; closure documentation pending review and merge

#### Source and Merge Verification

- Started from clean, synchronized `main` at `57cc6cfdd8a3e99b2bcdcddd0b7ffd3f4fc1da20`, the PR #35 merge of Phase 6.1. PR #29–#35 merge ancestry was verified from the published repository history.
- Compared PR #34 at `166788e037fc96f8f336c980142d622ae2a7aee1` with PR #35 and confirmed that the corrective delta is limited to the approved Representative Application authorization boundary, Delivery refactor, LF/CRLF test correction, wiring and status documentation.

#### Re-audit Result

- The first blocker is resolved: Representative Family context, actor/resource authorization, related-Student checks, another-Representative Address protection and safe history projection now belong to `Family/Application/RepresentativeResources`. The Controller retains HTTP parsing, structural validation, CSRF, Application invocation, PRG, rendering and safe known-error mapping.
- The second blocker is resolved: the Representative route test normalizes source text once and explicitly proves equivalent LF and CRLF behavior while preserving the exact one-GET/twenty-POST inventory and middleware assertions.
- Re-audited Family as the sole Aggregate Root with its nine approved collections and seven resource Entities; complete reconstruction invariants, lifecycle and temporal history remain coherent with ADR-0021 and the official Domain documentation.
- Re-audited the four-method FamilyRepository and complete-Aggregate PDO persistence: database-generated identities, strict UTC seconds, exact GENERAL_STATUS mapping, immutable history, same-Family ownership, prepared SQL and owned/outer transaction behavior remain intact.
- Re-audited the 21 Phase 4 cases, Administrator Delivery and Representative Portal Delivery. Successful commands retain one Aggregate mutation and one save, failed verification remains closed, routes and middleware are exact, CSRF precedes mutation, output is escaped and no new session authority or role model was introduced.
- Security, privacy and White Label checks approved selected-Family isolation, stale/colliding identifiers, another-Representative history privacy, active related-Student authority, generic external errors and absence of hard-coded institutional identity.
- Schema parity remains exactly 36 functional tables plus migration metadata, active migrations 001–009 and 60 foreign keys. No migration 010, resource child repository, hard delete, Enrollment, Submission, Student Portal or other excluded capability was introduced.
- Findings were classified as: no remaining functional, security, architecture or persistence blocker; status/roadmap/log synchronization corrected here; Controller size retained only as a non-blocking maintainability observation because responsibilities and testability now conform to the approved boundary.

#### Validation

- `composer validate --no-check-publish` passed.
- Strict optimized development autoload generated 312 classes.
- The complete suite passed with 480 tests, 0 failures and all three HTTP/session scenarios passing.
- PHP lint passed for all 388 tracked PHP files. Working-tree and accumulated diff checks passed.
- The approved disposable MariaDB 10.4 Phase 5 evidence was reused only after exact Git comparison confirmed no later change to Family Domain/Persistence, database migrations/seeders, database core or `MysqlMigrationIntegrationTest`.
- Reused physical evidence includes migrations 001–009, StatusTypeSeeder, StatusSeeder, AdminSeeder, accumulated regression, complete Family Resources roundtrip, productive Delivery/Application integration, database-managed AUTO_INCREMENT, UTC, constraints, owned/outer rollback and partial/final cleanup. The final `SHOW DATABASES LIKE 'antares_e008_phase5_tmp%';` returned no rows.
- MariaDB was not rerun, no credentials were invented or recorded, and no `.env`, UEAnt, institutional or persistent database was used.

#### Closure Decision

- E008 is completed. Phases 1–6.1 are merged, the second Phase 7 audit resolved the prior findings, all required validation passes and no in-scope blocking incompatibility remains.
- E009 remains planned only. This closure does not authorize or start Institutional Documents.

#### Next Action

Review and merge the E008 closure documentation. Authorize E009 separately if and when its documentation and functional-alignment phase is approved.

---

### E009 Institutional Acknowledgements — Phase 1 Documentation and Functional Alignment

**Date:** 2026-08-13

**Milestone:** E009 — Institutional Acknowledgements

**Status:** Implemented and locally validated; disposable MariaDB 10.4 validation, review, PR and merge pending

#### Source and Authorization

- Started from clean synchronized `main` at `d128f7dd3dbcf25ba0833af934fe743927143a55`, the PR #36 merge of E008 Phase 7 closure. E008 is formally completed and E009 was still planned/not started before this branch.
- Created `docs/e009-institutional-acknowledgements-baseline` from that exact SHA and followed the official Enrollment documentation order.
- Verified ADR-0018 remains approved and applicable: no first environment with real data is declared, all recorded MariaDB runs are disposable, and current safeguards still prohibit institutional or production targets. Direct correction of migrations 007/008 is therefore authorized; no migration 010 or evolutionary backfill was created.

#### Decision and Domain Alignment

- Approved ADR-0022 and formally renamed the milestone to Institutional Acknowledgements while retaining `institutional_documents` only as the existing logical technical context name.
- Replaced SIS-managed documents, versions and Enrollment-owned DocumentAcceptance with configurable `AcknowledgementRequirement` and historical `RepresentativeAcknowledgementCompletion` containing its individual `RepresentativeAcknowledgement` set.
- AcknowledgementRequirement is an independent configurable Aggregate Root by AcademicPeriod. Completion is a separate historical Aggregate Root, unique by Representative + AcademicPeriod, and protects atomic creation of its complete server-resolved set.
- Zero requirements is valid and satisfies the stage without artificial Completion. Existing Completion remains valid after requirements are added/deactivated or URLs/configuration change and is never repeated by Student, Enrollment, Reopening or Resubmission.
- Before first acknowledgement, Title, URL, OfficialReference and Status may change; afterwards Title and OfficialReference are immutable while URL and Status remain mutable. INACTIVE prevents new confirmations and preserves history; hard delete is excluded.
- The SIS keeps only required Title/URL, optional OfficialReference and GENERAL_STATUS. It stores no PDF, file, content, hash, publication or version and does not claim signature, legal acceptance, substitute consent or proof of exact content.
- Enrollment owns no acknowledgement data. Future integration uses an explicit satisfaction boundary: zero applicable requirements or existing Completion satisfies the stage; otherwise it is pending.
- Representative Portal is documented as permanent family self-service. Completing Enrollment does not end access, transfer Family Resources into Enrollment or rewrite a Submission snapshot after later edits.

#### Physical Baseline

- Migration 007 now creates `acknowledgement_requirements`, `representative_acknowledgement_completions` and `representative_acknowledgements` directly. Migration 008 creates Enrollment without `enrollment_document_acceptances`.
- Removed `institutional_documents`, `institutional_document_versions`, `document_requirements`, `enrollment_document_acceptances`, version/file/publication fields and `is_required` from the active target baseline.
- The functional inventory changes from 36 to 35 tables and the FK count from 60 to 58. Migrations remain exactly 001–009, snapshot cascades remain exactly three and all other 55 FKs restrict deletion.
- Physical constraints protect AUTO_INCREMENT identities, nonblank Title/URL/optional reference, one Completion per Representative + AcademicPeriod, one Requirement per Completion and same-AcademicPeriod ownership through two composite FKs. RepresentativeId is not duplicated in individual acknowledgements; AcademicPeriodId is materialized only to enforce those composite relationships.
- `acknowledgement_requirements` references AcademicPeriod and GENERAL_STATUS-compatible Status. Completion references Representative/AcademicPeriod and stores explicit UTC `completed_at`; individual acknowledgements reference only Completion, Requirement and the period required for physical ownership integrity.
- No seeder was changed and no institutional requirement or example document was invented.

#### Tests and Validation

- Schema coverage now asserts the exact 35-table inventory, 58 FKs, all columns/types/nullability, required keys/checks/composite FKs, absence of every old table/storage/version column, Enrollment independence and unchanged migration sequence.
- The MariaDB harness is ready to inspect exact tables/columns/FKs, AUTO_INCREMENT, text lengths/nullability, GENERAL_STATUS, zero-requirement validity, Completion uniqueness, acknowledgement uniqueness, cross-period rejection, exact UTC seconds, rollback and cleanup without adding E009 Application behavior.
- `composer validate --no-check-publish` passed. Strict optimized development autoload generated 312 classes.
- The complete local suite passed with 481 tests, 0 failures and all three HTTP/session scenarios passing. Modified PHP lint and diff checks are part of the final branch validation.
- All six `E0041_DB_*` variables were absent. MariaDB was not executed; no `.env`, UEAnt, institutional database, persistent database or invented credential was used. Physical validation remains mandatory before merge with an E009 disposable prefix such as `antares_e009_phase1_tmp`.
- No E009 Domain PHP, Repository, PDO adapter, Application case, Controller, route, view, Administrator UI, Representative UI, Enrollment/Submission implementation, file storage or Phase 2 work was added.

#### Next Action

Run `composer test:mariadb` with the explicit disposable E009 Phase 1 environment, confirm no residual `antares_e009_phase1_tmp%` database, then review and merge Phase 1. Authorize Phase 2 separately.

---

# Architectural Decisions Recorded

The following long-term engineering decisions were established during the project evolution:

- GitHub is the project's single source of truth.
- The `.ai` directory contains the official documentation.
- Documentation evolves together with implementation.
- Business rules precede technical implementation.
- Domain consolidation takes priority over feature development.
- Long-term maintainability has priority over implementation speed.

---

# Documentation Evolution

During the recorded milestones, the following documentation has been created or significantly evolved:

- PROJECT_CONTEXT
- CURRENT_STATUS
- ROADMAP
- DEVELOPMENT_LOG
- ARCHITECTURE
- DOMAIN_MODEL
- DATABASE_DESIGN
- ERD
- CATALOGS
- DECISIONS
- SECURITY_GUIDELINES
- CODING_STANDARDS

---

# Repository Evolution

The repository evolved through the following major stages:

1. Repository creation.
2. Documentation foundation.
3. Framework implementation.
4. Initial domain modelling.
5. Enrollment domain review.
6. Domain Discovery Workshop.
7. Domain Consolidation.

---

# Current Historical Boundary

This document intentionally ends with the completion of the Domain Discovery Workshop.

Any work performed after that point shall be recorded as new historical entries once completed.

The current state of the project is intentionally **not** described here.

Future work is intentionally **not** described here.

Those responsibilities belong respectively to:

- `02.CURRENT_STATUS.md`
- `03.ROADMAP.md`

---

# Document Evolution

**Version:** 3.0

This document is the official historical record of the project.

It shall only grow by appending new completed milestones.

Existing historical entries should not be rewritten except to correct factual inaccuracies or improve clarity without changing the recorded history.
